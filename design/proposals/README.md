# design/proposals — agent-generated first-version design directions

Greppable summary: founder-directed (2026-07-15, Session Contract #7) first
version of the brief's three design directions, generated by the build agent
instead of Google Stitch — a LOGGED DEVIATION from the brief's HOW-TO-RUN
(founder: "You are to do the first version"); PART A constraints, PART B
leans, and PART C rubric of `docs/design/ONE_LIVE_MASTER_DESIGN_BRIEF_v2.4.md`
are otherwise honored verbatim. `design/inbox/` stays reserved for any
founder-side tool exports. STATUS: PROPOSAL — the founder chooses the
direction (rubric-scored, ties broken by signature strength); the chosen
direction is then implemented in `web/` at Step 9 with an evaluator pass
against the brief's rubric.

## The three directions (self-contained HTML, open in any browser)

| File | Name | Lean (PART B) | Signature element |
|---|---|---|---|
| `direction-1-marquee.html` | **MARQUEE** | warm / analog-human | The Doorlight — uniform tungsten glow on each card's leading edge; the feed as a street of slightly-open doors |
| `direction-2-current.html` | **CURRENT** | electric / kinetic | The Pulse — one citywide heartbeat line under the masthead (never per-listing) + current-cut card corner |
| `direction-3-setlist.html` | **SETLIST** | editorial / monumental | The Time Rule + Edition Masthead — the night as a dated printed setlist ("No. 197"), gone tomorrow |

All three include the three mandated screens (Tonight feed · Filter panel ·
Event Detail) and honor the ratified trust-display rules: **no
badges/"verified"/"confirmed" anywhere**; uncertainty = one quiet icon that
is a REAL ≥44px control (a `<details>` disclosure) opening the one-tap-
dismissible sheet with the venue's own site linked — at the point where
uncertainty is shown (feed card) AND on the detail screen; Spark Lines with attribution incl. the tier-C machine-drafted
register (italic, one shade quieter, small ✳, "— first notes", same
dismissible-sheet pattern); Emotion Glyphs as self-rendered inline SVGs with
aria-labels (no platform emoji, no banned rating-glyphs, uniform placement so
no visible hierarchy between listings); verbatim copy strings ("Tonight in
Austin", "Less chaos. Real shows.", "Something off?"); WCAG 2.2 AA contrast
targets and ≥44px touch targets; date tabs; Free/Ticketed; "Hear it".
Fixture artists are FICTIONAL (no facts asserted about real acts); Austin
venue names appear as setting only. Typography uses licensed-face
specifications (Fraunces / Space Grotesk / Archivo) with system fallbacks in
the mockups — faces are licensed at implementation, not embedded here.

## PART C rubric — generator self-scores (honest; the founder's scoring decides)

| Criterion (1–5) | MARQUEE | CURRENT | SETLIST |
|---|---|---|---|
| 1 · 10-second answer on the feed | 4 | **5** | 4 |
| 2 · Night-sidewalk legibility | 4 | **5** | 4 |
| 3 · Trust-by-craft (zero badges, feels dependable) | **5** | 4 | **5** |
| 4 · Distinctiveness vs named competitors | 4 | 4 | **5** |
| 5 · Emotional fidelity (anticipation + calm certainty) | **5** | 4 | 4 |
| 6 · Accessibility evident in comps | 4 | **5** | 3 |
| 7 · Survivability (year 3, other cities, light mode) | 4 | 4 | **5** |
| 8 · Click-pull (curiosity gap + white-hat reflection test) | **5** | 4 | 4 |
| **Total** | **35** | **35** | **34** |

Honest notes: CURRENT wins pure utility (time-first cards, highest contrast,
tabular numerals) but runs closest to "energetic promo" territory — its
discipline is the single Pulse line. MARQUEE is the most literal translation
of the brief's §3 sidewalk moment and the strongest door/curiosity metaphor;
its risk is drifting toward nostalgia-kitsch if the warmth is overdone at
implementation. SETLIST is the most ownable and ages best (and is the only
direction whose light mode is a structural advantage, not a port); its known
weakness is scored honestly — condensed uppercase + italic serif needs
careful sizing to hold AA and dyslexia-friendly reading, fixable at
implementation. The totals land in a near-tie by design honesty, not by
design: MARQUEE and CURRENT tie at 35 with SETLIST one point back — real
tradeoffs, no direction dominates. Per the rubric, ties break on signature-
element strength: whichever signature the founder finds most ownable wins.

## Po battery run (chartered for design-direction work)

Seed 20260716, random word "scaffold", all operators P1–P8.6 with ≥2
movements each. Harvest adopted INTO the mockups: the **first-door rule**
(P1 escape "no screens" → the top card alone must fully answer the night —
all three feeds open with a complete, decision-ready first card); **fixed
control geography** (P4 distortion "tap before seeing" → Hear-it and filter
controls sit at identical positions across all screens and directions —
muscle memory as calm); **the Doorlight** (P5 wishful "the phone glows like
the venue"); **the Time Rule** (P7 "scaffold" → structure kept visible);
**uniform glyph sky-corner** (P8.6 → emotional weather sits in the same
"sky" position on every card, never a hierarchy). Recorded as candidates,
NOT built: "quiet night" sparse-market presentation spec (P3↓ — ties to
research-note H12, design at Step 7+) and the "walk the street" sequential
3-second preview autoplay (P6 absurd — deferred: autoplay is attention-tax
territory and needs the white-hat reflection test with real users). Dead
ends logged: P2 reversal ("the interface designs the night") and P8.3
(10,000× scaffolding) produced no adoptable movement. Nothing po-generated
entered memory, candidate data, or user-facing copy; spark lines/glyphs in
the mockups are fixture content for FICTIONAL artists.

## Early-test results (2026-07-15 — run LOCALLY in the build-session sandbox)

Scope stated precisely so no green claim outruns its evidence: these ran in
the Claude Code session container, NOT in CI — this diff touches no `web/`
files, so the CI web gate correctly reports "not applicable" for it. The
same checks re-run mechanically in CI the moment Step 9 touches `web/`.

- Mockup rendering: all three files rendered in headless Chromium
  (`chromium --headless --screenshot file://…`); the committed evidence is
  in `renders/` (one PNG per direction, regenerated after every mockup
  edit). The `<details>` uncertainty sheets work without JavaScript.
- Web app smoke for Step 9 (`web/`, unchanged by this PR): `npx tsc
  --noEmit` exit 0 · `npm test` 25/25 · `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
  pk_test_… npm run build` green; `/tonight` prerendered static, 184 kB
  first-load JS. Captured output: `renders/web-smoke-2026-07-15.log`.
  Anyone can reproduce with those three commands from `web/`.

## Deferral (recorded, R-014)

Light mode ships as palette specifications here, not as rendered comps —
rendering three full light variants for two directions that will be
discarded fails least-costly-method-first. Objective trigger in
`docs/RECORD.md` R-014: the founder picks a direction → the Step 9
implementation PR ships the WINNER's light-mode render + visual-regression
baseline (light + dark), closing the gap where it matters.

## What happens next

1. Founder picks a direction (or directs a hybrid/iteration) — rubric
   scores above are an input, not the decision.
2. Step 9 implements the chosen direction in `web/` (Next.js + Tailwind,
   licensed faces), with an evaluator pass against the brief's 8-criterion
   rubric; deltas from the brief logged, never silent.
3. `tools/visual_regression.py` baselines capture from the first deployed
   preview (closes R-002).
