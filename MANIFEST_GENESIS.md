# ONE LIVE — Repo Docs Package (2026-07-12)
Unzip at the repo root. Contents merge into the existing tree:
- CLAUDE.md → repo root (Claude Code reads it every session; contains the full document index)
- docs/source/ → the two canonical inputs of this session (WORLD_CLASS bar, MASTER doc) — keep as read-only reference
- docs/strategy/ → deep review v1.1 (PROPOSAL), autonomous charter + API manifest, Emotion & Vibe Layer spec (PROPOSAL)
- docs/design/ → Master Design Brief v2.4 (ratified canon for all UI work) + reference prototype + archived v1s
- docs/ops/ → Claude Code kickoff prompt (Session Contract #1) + this session's change-log entries (append to docs/ONE_LIVE_CHANGE_LOG.md)
NOT included (lives outside the repo by design): credentials/keys (docs/KEYS.md to be created by founder), Stitch outputs (drop into design/inbox/ when generated).
Ratification state: brief v2.4 ratified via founder edits; §10–§15, taxonomy, and G-gaps remain PROPOSALS.
