# Tickets (restated 2026-09-09)

Run continuously. Each miss uses the 15-step Process.
CAPCOG is the test view filter, not a ticket and not the map.

## A — Populate
Write happenings from validated doors into the catalog.
Existence = one validated door + title (or listing URL).
Date and place are holes, not holds.
Bar: desk-ingest writes the pack. Not done until those rows can show.

## B — Show the union (next)
100% of that locale’s counted aggregator union is on 1live.co.
Same title + when + place = one.
Less than 100% is wrong.
This is the ingest-to-showing gap. Work this now.
Bar: open 1live.co, count. Green job is not B.

## C — Card
Print title / when / place / kind / via plus the other card slots when a door printed them.
Holes allowed. Do not invent a clock.

## D — Place is a query
Locale = device or typed search. CAPCOG is the default test filter only.
Show everywhere and a typed place must not delete catalog rows.

## E — Gather
A new place starts a bounded hunt (FIND_AGGREGATORS) then ingest. Search hit is a lead.

## F — Pack completeness
Every pack door has a catalog path. No door left without a row machinery.

## G — World
Every locale, every kind. Roles, not Austin brands.

Next: B. Apply FL-005 (validated door promotes on 1). Count 1live.co.
