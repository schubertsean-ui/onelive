# FL-002

Failure: ingest dies after a long walk.
Why: a name the job imports does not exist (`complete_year`).
Success: the walk imports a real name and finishes.

Apply: export `complete_year` from the module the job already imports. Do not invent a second year helper.

Scope: universal. Any locale pack walk.
