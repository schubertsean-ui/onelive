# FL-007 — wrapper instead of deletion

- Symptom: live-miss / catalog-hole
- Cause: workaround
- Failure: law says door + title publishes. desk_publish.py still holds on date/place. A wrapper (ticket_a_apply) was added instead of deleting those lines.
- Ban: if a function sets hold_reason, edit THAT function. Do not add a second module that clears it later. Do not write a doc that pretends it is gone.
- Keep holds: no title, fixture, class D, unofficial social alone.
- Auto-apply: yes. Same failure → delete the hold in the file that assigns it.
