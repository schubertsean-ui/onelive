# FL-010

Failure: Chronicle printed Hays City Store and 6 p.m. The write log has those words. https://1live.co/tonight still shows Place to be confirmed, plus two Dave Orr cards (1:00 PM and 6:00 PM).

Why, in order (do not skip):
1. tools/desk_ingest.py recorded a hole-fill as a candidate and left the public row alone. That was called corroboration. Filling a blank Place is a fill.
2. After the candidate held Place, the next ingest skipped: already public, same statement. The EVENT row the card reads was still empty.
3. Tonight prints venue.name through event.venue_id (web/lib/promoted.ts). Writing event.venue_name does not move the card. event has no venue_name column.
4. ingest_key includes the clock, so naive UTC 18:00 and Chicago 18:00-05:00 became two cards.

Success: one Dave Orr Band card on https://1live.co/tonight says 6:00 PM and Hays City Store. No 1:00 PM card.

Apply
- tools/desk_ingest.py fill_published_holes: resolve_venue_id + UPDATE event.venue_id where venue_id is null.
- Skip path (already public, same statement): still fill if fresh.place.
- Same listing URL + same date + tz-only clock: rewrite that row. Do not open a second card.
- Test: tests/test_fl010_writes_venue_id.py must fail until resolve_venue_id is in desk_ingest.py.

Do not start ingest until that test is green on master.
Green ingest is not Success. Step 11 is the live card.
