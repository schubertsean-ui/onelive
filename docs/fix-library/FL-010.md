# FL-010

Failure: Chronicle printed Hays City Store and 6 p.m. The write log has those words. https://1live.co/tonight still shows Place to be confirmed, plus two Dave Orr cards (1:00 PM and 6:00 PM).

Why, in order (do not skip):
1. tools/desk_ingest.py recorded a hole-fill as a candidate and left the public row alone. That was called corroboration. Filling a blank Place is a fill.
2. After the candidate held Place, the next ingest skipped: already public, same statement. The EVENT row the card reads was still empty.
3. Tonight prints venue.name through event.venue_id (web/lib/promoted.ts). Writing event.venue_name does not move the card. event has no venue_name column.
4. ingest_key includes Place and the clock. Naive UTC 18:00 and Chicago 18:00-05:00 became two cards. The Hays-keyed row is not the two empty-Place cards Tonight shows.
5. fill only wrote venue_id when venue_id was null. A placeholder venue (Unknown Venue) made the fill a no-op.

Success: one Dave Orr Band card on https://1live.co/tonight says 6:00 PM and Hays City Store. No 1:00 PM card.

Apply
- fill_published_holes: resolve_venue_id from the name before the first comma. Replace null OR placeholder venue_id. Also fill other scheduled rows with the same title and the same local date.
- Then cancel tz-ghost siblings (same title, same local date, 4–6 hour clock offset or no Place).
- Skip path: fill place+clock even when the statement already matches.
- Test: tests/test_fl010_writes_venue_id.py and tests/test_fl010_hole_fill.py.

Do not start ingest until resolve_venue_id + placeholder replace + sibling title+date are in tools/desk_ingest.py on master.
Green ingest is not Success. Step 11 is the live card.
