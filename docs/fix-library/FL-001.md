# FL-001

Failure: Today N below that locale’s aggregator today union.
Why: a missing end was treated as over (invented duration).
Success: a happening with a start on that locale day and no end stays on Today until that locale day ends. No clock is invented.

Apply in web/lib/feed.ts using web/lib/when.ts.
- Delete ASSUMED_MS.
- eventTiming: no end → on-now while startDate == today; ended only after that locale day.
- Date-only uses FL-003.
