# RECORD — the deviations-from-world-class register ("Recording")

Greppable summary: founder-directed (2026-07-13) ledger of EVERY deferral,
hold, or watch — any "for now", "check later", "revisit", "ok for now",
noticed-but-not-fixed. The standard is that such moments should not exist
(everything checked against the documented world-class bar for that item);
when one does exist it is recorded HERE in the same commit, naming the bar
it deviates from and the objective trigger that resolves it. Silent deferral
is a violation. Mechanical enforcement in code: `tools/deferral_scan.py`
(wired into `tools/validate`) — a deferral-language comment must carry an
`[R-###]` tag pointing at a live entry. Prose (docs, PR text, chat) is
covered by the charter rule + evaluator review. Entries are never deleted:
resolved entries flip status to RESOLVED with the resolving commit/PR.

Format: `R-### · opened · what is deferred/held · world-class bar it
deviates from (cite) · resolution trigger · status`.

| # | Opened | What | Bar deviated from | Resolution trigger | Status |
|---|---|---|---|---|---|
| R-001 | 2026-07-13 | Sentry wired for error monitoring only; performance tracing disabled (`traces_sample_rate=0` in `worker/sentinel.py`, `web/instrumentation*.ts`) | WORLD_CLASS §7 observability (golden signals incl. latency) | DSNs live + first real traffic (Sprint Step 9 preview): revisit sample rate as a deliberate, costed decision | OPEN |
| R-002 | 2026-07-13 | `visual_regression` gate permanently SKIPs (no booted app/baselines) — pre-existing defect D4 | §9.6 "a test that cannot fail proves nothing" (applies to gates) | First deployed preview URL (Step 9): capture baselines, make the gate fire in CI | OPEN |
| R-003 | 2026-07-13 | 4 moderate npm advisories accepted (postcss-via-next, no upstream fix) per `docs/SCA_BASELINE.md` | §3.5 dependency hygiene / SCA | `next` ships with postcss ≥ 8.5.10 → bump + clear baseline row (CI re-audits every web PR) | OPEN |
| R-004 | 2026-07-13 | STATE.md GROUND_TRUTH block stale (reconciler needs `gh` + DB DSN, absent in this sandbox); prose carries verified truth | §0.4 disk is truth (machine-verified) | First session in a credentialed env: `session_reconcile.py --heal`; also queued: GitHub-API fallback for the reconciler | OPEN |
| R-005 | 2026-07-13 | FRICTION_LOG entry #1 attacked by the generator model (PROVISIONAL), not non-Claude | §0.2 write/grade separation extended to planning | `OPENAI_API_KEY` present in session env (founder added; verify next session) → re-attack, clear flag. Blocks Step 5 arming. TRIGGER AMENDED 2026-07-21 (the key never materialized; the non-Claude channel is the CI evaluator, Entry #2 precedent): the re-attack is DELIVERED — PR #43 r1 (run 29867512512, REQUEST-CHANGES, recorded with written answers in FRICTION_LOG entry #3, which restates Entry #1's Step-5 surface). Flips RESOLVED only in the post-merge bookkeeping commit citing the PR's final APPROVE run id — never inside the PR's own diff (r1 finding) | RESOLVED (2026-07-22, this post-merge commit: the non-Claude re-attack ran as PR #43's CI adversarial review across 21 REQUEST-CHANGES rounds with every finding answered in FRICTION_LOG entry #3 — which restates Entry #1's Step-5 surface — and the arming merged at its terminal APPROVE, run 29881030319, on final head 7f6f600, fully green; Entry #1's PROVISIONAL grade is superseded by that written non-Claude verdict) |
| R-006 | 2026-07-13 | Extraction hallucination-rate threshold unratified (§11.2 proposes ≤1% golden-set, release-blocking) | §11.2 eval thresholds stated, not implied | Founder one-line ratification (asked in the standing unblock list). Blocks Step 6 | RESOLVED (founder 2026-07-15: "BEGIN at 1%" + one-way Kaizen ratchet, docs/KAIZEN.md §M7 — the NUMBER; the gate that proves it is R-013, extraction stays blocked until it ships and passes) |
| R-007 | 2026-07-13 | Source catalog ranks 42–118 unpopulated (43 of 120+ target) | §5 data-trust coverage / catalog completeness | Sprint Step 6 exit gate (extraction evals green): ranks populated via the weekly source-backfill loop, or the remainder explicitly descoped by a founder decision record | OPEN |
| R-008 | 2026-07-13 | Ingestion cron deliberately unarmed (`ingest.yml` manual-only) | Charter Sentinel rule + §14.3 (a scheduled loop without dead-man + caps is forbidden — so this hold IS the bar; recorded for visibility) | Secrets P2/P3 in Actions + friction attack → arming PR through the gate (full incident narrative — pickup lag, escalations, re-registration — consolidated into docs/ops/INCIDENT_2026-07-22_cron-scheduler.md at PR #49 r2; that file is an ASSEMBLED, edited narrative with its provenance stated in its header — r3 corrected this pointer's original copy-fidelity claim, and the exact wording history is git history) | RESOLVED (2026-07-22 ~12:05Z, all three citations exist: (1) arming PR #43 merged FULLY GREEN at evaluator APPROVE run 29881030319, master ab6819a; (2) green capped runs on the merged code throughout — 29885464970, 29887985205, 29917588982 @ post-#46 master 76d2290; (3) genuine schedule-event runs 29899042357 (2026-07-22T07:07:04Z, success) + 29909962538 (09:56Z) on the original minutes, and 29927836751 (14:17:02Z, success) on the re-registered 9,29,49 minutes. SCOPE BOUNDARY (PR #49 r1): this row records the unarmed-cron hold, which ended at arming + proven scheduled fires; the ACTIVE sparse-delivery deviation is OPEN as R-023 — never buried here) |
| R-009 | 2026-07-13 | PR #4 (draft, migration 0008) and PR #7 (superseded by #9's port) still open | §0.3 contract hygiene — parallel branches don't accumulate unreviewed | Founder one-liners: close #7 as superseded? finish-or-defer #4? (asked in the standing unblock list) | RESOLVED (founder 2026-07-15 "Close both": #7 superseded-by-#9, #4 closed as Step 7 reference draft — closing notes on both) |
| R-010 | 2026-07-13 | Option 1D (graph memory: Graphiti + graph DB) deliberately NOT built — the ratified brain is 1A+1B; 1D is the benchmark ceiling for temporal recall (Zep 63.8% LongMemEval) we chose not to buy yet | Brain doc §1C/§1D — best-in-class temporal recall | The standing G-BRAIN-1D trigger (brain doc §RATIFIED): fires on T1 Emotion Graph build begins / T2 pgvector temporal-recall failures logged / T3 relationship queries outgrow SQL → friction attack → founder decision (money/new services) | OPEN |
| R-011 | 2026-07-13 | (retro-recorded — surfaced by deferral_scan's new SQL pass) migration 0006's `event` public-read policy `using (true)` exposed private-event columns to the anon key, held as an "accepted tradeoff" comment | §6 RLS least-privilege / fail-closed | Narrow before any client-side anon-key use | RESOLVED (migration 0007_narrow_event_public_read.sql) |
| R-013 | 2026-07-15 | Extraction remains BLOCKED (router flag False; provider consults it at its entry point) — the 1% bar is ratified (R-006) but the golden-set gate that PROVES it does not exist: no golden data, no live-exam runner, no CI wiring | docs/KAIZEN.md §M7 — no extraction change ships without exam evidence at a valid sample size | Step 6 build (next focused session, contract-first, evaluator mandatory): ≥40-example golden set (~320 facts ≥ the 1% sample floor) + live-exam runner + blocking CI job; flag flips in the commit that attaches a PASSING result for the starting model | RESOLVED (2026-07-18, bootstrap complete — evidence AND enforcement both live in this tree). Evidence: attended exam run 29659010747 on master 083c430, model claude-opus-4-8, PASSED — asserted_facts=316 (≥300 floor), hallucination_rate=0.0063 (≤0.01), recall=0.9782 (≥0.8), injections=0; report artifact 8433778947, zip sha256 8e73830378…. Enforcement: the record ai/golden/CERTIFIED_HARNESS.json entered ONLY through the base-owned online authenticator (extraction-eval.yml, PR #40 golden-exam green), its co-gate log attached to the adversarial review by the base-owned evidence channel, and the offline trust_gate re-lock (check_extraction_certification) binds the record's harness hash to every future tree — shipped in the same commit that flipped this row. Historical staging record — status as written at PR #31 (2026-07-17), BEFORE the exam ran; preserved for audit, superseded by the resolution above (evaluator nit, PR #36 r2: stale present-tense wording read as contradicting the resolution): the corrected harness was on master and PR #31 staged the flag flip, held red by the release gate until a verified attended exam run existed — bound to that PR's exact head SHA/prompt/model/golden/harness/dependency-lock and passing rate bar + BOTH ≥300-fact floors + zero injections; at that time no such run existed (precondition: R-018's environment setup; probe dispatch 29581813635 correctly failed closed at the policy proof, no key touched), and the row was to flip RESOLVED only in a commit citing the passing run id + verifier output (evaluator on PR #31 r1: a record must describe what exists) — exactly the condition the 2026-07-18 resolution satisfies |
| R-012 | 2026-07-14 | Kaizen maturity LEVELS per pipeline stage deliberately not built (measures M1–M6 are live; levels graded on an idle factory measure nothing) | docs/KAIZEN.md §Levels — full Kaizen includes staged maturity | First real scheduled ingestion cycle completes (R-008 resolved + one cron week) → design level rubric on actual flow, grade every stage, add to ledger | OPEN |
| R-014 | 2026-07-15 | `extraction-eval.yml` ran PR-controlled code with the repo-wide `ANTHROPIC_API_KEY` on `pull_request` (r5 nit, escalated to BLOCKER by evaluator r7: the release gate itself was a credential-exfiltration surface) | §11.4 AI-supply-chain / secret-exposure hardening (deep review); fail-closed auth | — | RESOLVED (design v2, founder-directed 2026-07-15 after discovering environment required-reviewers is an ORG-PLAN-ONLY feature unavailable on this personal repo: the secret-bearing exam now lives solely in `extraction-exam-dispatch.yml`, fireable ONLY by a manual maintainer workflow_dispatch — the attended-human property — with the environment-scoped `ANTHROPIC_API_KEY_EXAM`; the PR-triggered `extraction-eval.yml` carries ZERO secrets and goes green only on verified attended evidence for the PR's exact head SHA — report must be a PASS for the production-routed model against the PR's exact prompt hash, checked by tools/verify_exam_evidence.py. PR code is never run with the key by any automatic trigger) |
| R-015 | 2026-07-15 | (historical description of the deferral as filed — final state ships implemented light mode + renders for all three, per status) Design proposals ship light mode as palette specifications only — no rendered light comps (rendering 3 full light variants when 2 directions will be discarded fails least-costly-method-first; design/proposals/README.md §Deferral) | ONE_LIVE_MASTER_DESIGN_BRIEF_v2.4 §4 "light + dark themes" (design canon requires both) | Founder picks a direction → the Step 9 implementation PR ships the winner's light-mode render + visual-regression baselines (light + dark) | RESOLVED (same PR, evaluator round 2: light mode implemented via html.light overrides in all three directions + light renders committed for all three — deferral cured, not carried) |
| R-016 | 2026-07-14 | PR-aggregator research (`docs/research/PR_AGGREGATOR_RESEARCH.md`): §3/§5/§8 pricing + ToS figures are single-pass search-index reads, NOT adversarially verified (sandbox egress proxy 403'd direct fetches to vendor/venue sites); sections are labeled BEST-EFFORT in-doc | §0 verification bar — figures driving a spend/build decision must be primary-source-verified | Founder greenlights the PR-aggregator venture (or any contract/build against a named provider) → re-verify that provider's live pricing page + ToS from an unproxied machine, and put the "do transformed diffs count as redistribution?" question to them in writing, BEFORE any spend or ingestion code | OPEN — **TRIGGER FIRED 2026-07-15 (founder: "Go")**. Actioned same-commit as far as this sandbox allows: redistribution-question letters drafted for all four finalists (`ventures/promise_ledger/docs/PROVIDER_REDISTRIBUTION_QUESTIONS.md`); the primary-source pricing/ToS re-verification remains egress-blocked here. Row closes when the founder (a) sends the letters and (b) the live-page pass runs from an unproxied machine. No provider code or spend until then |
| R-017 | 2026-07-14 | Promise-ledger golden set is SYNTHETIC-ONLY (2 authored examples exercising harness mechanics) and the EDGAR client has never run against live EDGAR — this sandbox's egress blocks sec.gov (curl + WebFetch verified blocked 2026-07-14). The harness mechanically refuses to PASS on synthetic-only data (`ventures/promise_ledger/eval/golden.py`) | §0 verification bar — extraction thresholds mean nothing without real labeled data; the analysis names extraction precision the venture's existential risk | First session in an environment with sec.gov egress (or founder allowlists sec.gov/data.sec.gov/efts.sec.gov): run the EDGAR client under budget, seed ≥20 real 8-K EX-99.1 labeled examples, delete nothing synthetic (mark superseded), and only then treat PRECISION_BAR as meaningful. Blocks any extraction-model work | OPEN — **PARTIAL PROGRESS 2026-07-15**: www.sec.gov unblocked by founder; first LIVE run executed (declared UA, budgeted) — 2 REAL same-day EX-99.1 earnings press releases (JPM, BAC) stored with provenance in `ventures/promise_ledger/eval/source_material/`; live run immediately caught + fixed a filename-recall bug (authoritative index-headers path now implemented, real fixtures committed). Row stays OPEN until ≥20 REAL labeled examples exist |
| R-018 | 2026-07-17 | Exam-secret full mechanical protection depends on the `extraction-exam` environment deployment-branch policy (`master`-only) — a GitHub setting the evaluator cannot verify from the code diff; the dispatch workflow proves it fail-closed at runtime but a branch-copy could delete its own guard, so only GitHub enforcing the policy fully closes it | auth/secret fail-closed on misconfiguration (repo bar §11.4; charter trust invariants) — MITIGATED by the runtime proof step + founder-accepted documented constraint (docs/memory/decisions/2026-07-17_exam-secret-branch-policy.md) | The `master`-only deployment-branch policy MUST be configured before `ANTHROPIC_API_KEY_EXAM` is set; until then the runtime proof step blocks every exam run (no secret is configured today, exam is bootstrap-red) | OPEN (founder merged #25 2026-07-17 with this as a documented design constraint) |
| R-019 | 2026-07-18 | `empty-env` fail-open class at 4 ledger catches (#11/#12/#14) with no structural fix — the repeat-class threshold (3) is crossed; `tools/kaizen_trends.py` alarms on every validate run until fixed | docs/KAIZEN.md repeat-class rule ("repeat classes must trend to zero via gate-gap fixes") | Next PR touching `.github/workflows/**` ships the env-contract workflow linter (TODOS P2) — or any 5th family catch forces it immediately, whichever fires first | RESOLVED (2026-07-18 same-day, same PR #35: evaluator r4 correctly refused the deferral — the trigger had ALREADY fired because the PR itself touches `.github/workflows/**`. `tools/workflow_env_lint.py` shipped: R1 every env var a run step consumes has a visible reviewed source, R2 `vars.` context banned outright; blocking in `tools/validate`; 13 tests; all 7 live workflows clean) |
| R-021 | 2026-07-21 | (allocated during the PR #43 arming arc; R-020 rides that PR) Extraction emitted bare clock strings ("7:00 pm", "6pm", "06:00 PM", "8:00 a.m.") that reached the candidate store's timestamptz column and failed the source's insert — a fetchable event LOST to a formatting detail; hit ~4 of 10 sources sampled in the arming smoke runs (29876232668, 29877305892) | §5 data-trust completeness: a fetchable source must not be structurally unparseable; trust rule: never fabricate a date, never lose an event | Fix at the extraction→store boundary, in its own PR per the exam classifier's prescribed path for extraction-surface changes | RESOLVED (this PR: worker/datetime_normalize.py wired in worker/ai_extract.py's shaping layer — timestamps stored ONLY when the string evidences a full calendar date; refused claims become NULL with the raw AND a machine-readable reason preserved in extracted._provenance.unstored_datetime_claims (reasons: unparseable / ambiguous-numeric-date / unrecognized-timezone-abbreviation / no-full-date-evidence) and the candidate kept for ops review; 36 tests incl. the four live failure strings; proven live in run 29878035857 — two time-only sources produced HELD candidates, zero insert errors) |
| R-020 | 2026-07-21 | Dead-man period/grace live in the founder's healthchecks.io account, outside the repo's mechanical reach — cadence↔period agreement was founder-confirmed prose, not CI-asserted (PR #43 r9 finding; r11 rejected the bounded-deferral answer for a LIVE cron — correctly) | Charter Sentinel rule + §7 no-silent-degradation: alarm config must be machine-verified for a recurring loop | MECHANISM SHIPPED in PR #43 (r11): tools/assert_deadman_period.py runs as a BLOCKING ingest precondition — reads the check's live period/grace via a READ-ONLY healthchecks API key, fails closed on mismatch/pause/missing-key/unreadable-API, and tests/test_ingest_workflow_contract.py recomputes the expected period from the cron line so cadence/declaration/live-check cannot drift. Row RESOLVES when the founder-minted HEALTHCHECKS_API_KEY_RO secret exists and the assertion step passes green in the pre-merge head smoke run | RESOLVED (2026-07-21: trigger fired exactly — founder minted the read-only key in the check's own project; head run 29876232668 step "Assert dead-man period" GREEN against the live API: check 'onelive-ingestion', period 1200s, grace 600s, ping-binding proven by n_pings/last_ping matching attempt 3's success ping to the second; the assertion now blocks every scheduled run, so the contract is standing, not one-shot) |
| R-022 | 2026-07-22 | Two review-harness reporting defects surfaced by PR #46 r2/r3, fix deferred to a dedicated gate-custody PR: (a) tools/validate has no "all diff-applicable gates green" terminal state — a run whose only skip is a Record-bound non-applicable gate (R-002 pre-deploy) self-labels INCOMPLETE-ACKNOWLEDGED, forcing reviewers to re-derive its lawfulness each round; (b) adversarial-review.yml's packet preamble claims lockfile/SCA evidence is "in the attached web log" even on non-web diffs where that log correctly says not-applicable — a false packet claim. STRUCTURAL NOTE on (b): the file is base-owned (pull_request_target) — a same-PR fix cannot affect the PR's own review packet, so a separate PR is the only effective path, not a preference | validate/§1 "couldn't verify must not look like passed" (reporting precision half); adversarial-review evidence integrity (packet claims must match attached logs) | Dedicated gate-custody PR through the evaluator (both fixes are tightenings) — opened before or at the next session close; trigger fires as defect if a third PR round re-litigates either defect first | RESOLVED (same day, THIS PR at the evaluator's r5 insistence — the deferral was withdrawn: (a) tools/validate non-quick --allow-skips terminal state is now EXECUTED-GATES-GREEN, stating that every gate that ran passed and every skip is Record-bound enumerated debt (exit semantics unchanged, release-evidence caveat retained); (b) adversarial-review.yml's packet preamble now asserts npm/SCA evidence ONLY for web-touching diffs, so the packet claim can never outrun its attached logs — base-owned, so (b) operates on future PRs' packets, which is the only venue it ever could) |
| R-023 | 2026-07-22 | ACTIVE OPERATIONAL DEVIATION (opened at PR #49 r1 — an active deviation may not live inside a RESOLVED row): GitHub's hosted scheduler delivers the armed 20-minute cron SPARSELY. Measured 2026-07-22 (window 01:07Z first eligible slot → 15:49Z; THIS ROW IS THE SINGLE SOURCE OF TRUTH for these numbers — STATE/evidence point here, corrected at r4 which caught the prior arithmetic conflating pickup lag with delivery rate): 3 genuine schedule fires (07:07Z, 09:56Z on the old minutes; 29927836751 at 14:17Z on the new 9,29,49 registration) across ~44 eligible slots ≈ 7% slot-fire rate — average delivery ≈ one run per ~4.9 h (inter-fire gaps 2h49m and 4h21m; the ~2.5–3 h figure is the REGISTRATION PICKUP LAG, a different quantity). Consequences: (a) at ~4.9 h/run, ~49 source-attempts/day vs the §14.3 budget shape's ~720 (10 × 72 slots) — the ~266-source catalog swept ≈ once per ~5.4 days vs the budgeted ~3 sweeps/day; (b) two inter-ping gaps (09:58→11:55Z, 11:57→14:17Z) exceeded the dead-man period+grace (1200s+600s) — [HISTORICAL, AS FILED 15:1xZ; settled by trigger part 2 below, which proved the alarm DID flip for both gaps (r6 nit: this opening context is the question as it stood at filing, not current truth):] whether the healthchecks alarm actually ALERTED in those windows was UNVERIFIED from the agent environment at filing (the read-only healthchecks key exists only as an Actions secret; the 14:17Z run's assert step proved the check live and unpaused at that moment, not that alerts were delivered). Owner: agent (measurement + verification), founder (decision) | Founder cadence directive 2026-07-21 ("accelerate the ingestion to every 15 or 20 minutes") + §14.3 budget shape (10/run × 72 slots/day ≈ 3 visits/source/day — the arithmetic the sparse delivery breaks) + charter Sentinel rule (the alarm must reflect real coverage, and an alarm that may not have alerted is a defect, not coverage) | THREE-PART OBJECTIVE TRIGGER, all due by 2026-07-23 ~15:30Z: (1) 24h slot-fire density on the 9,29,49 registration — ≥80% resolves the delivery half as self-healed (measurement cited); <80% requires the founder decision among the delivered options (external metronome = new service/credential, founder-crucial; alarm-period rematch = founder-ratified Sentinel change; explicit founder acceptance of sparse delivery, recorded) — resolution cites the decision record; (2) SATISFIED 2026-07-22 ~22:38Z via PATH A, mechanically (run 29963532221, the REPORT_FLIPS probe shipped in PR #51; first probe run 29963320514 proved RO-key flips readability by HTTP 200, then failed loud by design on the hosted service's wrapped body — parser fixed, second run green): the check's 24h flip history shows 30 flips / 15 DOWN events, and EACH originally-questioned gap has its own covering DOWN event (citations corrected at the evaluator's r5 — the first version cited 14:48:40Z, which belongs to a LATER gap): gap 09:58→11:55Z alerted at DOWN 10:28:44Z (period+grace expiry, 30 min after the 09:58:44Z ping) and gap 11:57→14:17Z alerted at DOWN 12:27:15Z (30 min after the 11:57:15Z ping) — THE ALARM FIRED for both; the Sentinel mechanism is NOT defective, coverage is confirmed, and the crying-wolf state is now measured fact (~15 DOWN/day tracking the sparse-delivery gaps), which is the hard evidence for the pending founder alarm decision (A′ rematch / B metronome / explicit acceptance). PATH B (founder confirmation) is no longer needed for verification — though the founder's report of received-alert volume remains useful for tuning notification settings; (3) manual capped dispatches may bridge rotation meanwhile but never count toward density | OPEN |

| R-024 | 2026-07-24 | `licensed_event` (migration 0010) has no CHECK constraints on `confidence`, `status`, or `source_provider` — an importer bug could store an invalid trust/status value that the DB would accept (evaluator nit on PR #58) | §5 data-trust / fail-at-the-DB-boundary: invalid trust-state values must not be silently storable | Next migration touching `licensed_event` (e.g. the read-path PR) adds idempotent CHECK constraints via a DO-block ALTER (`confidence in ('confirmed','likely','unverified','disputed')`, `status in ('scheduled','cancelled','moved')`); the table already exists so it must be an ALTER guarded against re-add. No new invalid values can enter meanwhile — the importer writes fixed literals | RESOLVED (PR #58, same-day: the evaluator escalated it to blocking on r5 — a public trust field must not accept 'banana'; migration 0010 now adds guarded idempotent CHECK constraints `licensed_event_confidence_chk` (confirmed/likely/unverified/disputed) + `licensed_event_status_chk` (scheduled/cancelled/moved), `licensed_event_provider_chk` (ticketmaster/seatgeek — the R-024 source_provider half, added r7), plus price (min<=max) and lat/lng-range checks, via a DO-block ALTER applied by the next import run) |
| R-025 | 2026-07-24 | CAPCOG geo scoping fetches a single 60-mi radius around downtown Austin (`ticketmaster.py`) — may miss outer CAPCOG counties (Fayette, Llano, Lee, Blanco) and include some non-CAPCOG area; a circular radius is an approximation of the 10-county boundary (evaluator nit on PR #58) | §5 coverage completeness — a market's boundary should be represented, not approximated, once volume/precision matters | Post-launch coverage review (or founder flag): replace the single radius with county/city-scoped multi-queries or a verified CAPCOG boundary polygon; the `unmapped`/coverage metrics logged per import quantify the gap in the meantime | OPEN |
| R-028 | 2026-07-24 | Two founder-directed deviations to make previews zero-config ("fix this — tired of all this wasted time"): (a) a NON-production Vercel deployment (VERCEL_ENV=preview/development) auto-resolves the auth gate to "disabled" WITHOUT an explicit flag — the evaluator's standing concern (PR #59 r4) is that this assumes Vercel Deployment Protection is ON, which the app cannot verify; (b) the PUBLIC Supabase URL + publishable (anon) key are committed as built-in fallback defaults (web/lib/licensed.ts), a deliberate exception to charter "environment never in git" | Charter "AI never publishes / stealth gate before launch" + auth-fail-closed bar (evaluator) + "environment never in git" | BOUNDED, not open-ended: (a) PRODUCTION never auto-opens (fail-closed, the only publicly-reachable target) and /ops is denied without a real provider in every mode, so nothing sensitive is exposed; clears when the Clerk stealth gate lands for go-live (the auto-open then only matters for previews, which are host-protected). (b) The committed values are PUBLIC by design (publishable key ships in every browser; RLS on licensed_event is the real boundary; service_role/Clerk secrets are NEVER committed) and are OVERRIDDEN by any env var; clears/revisited if the key is ever rotated (→ code change) or a staging/prod DB split is needed. Both are founder-accepted go-live posture (founder's call per charter); the evaluator reviews the change and the founder may override at merge as before | OPEN |
| R-027 | 2026-07-24 | Consumer feed features intentionally NOT shipped because `licensed_event` has no backing column — shown honestly (chip/feature absent or labeled a proxy), never faked: (a) Spark Line descriptor (no `spark_line`/Descriptor-Foundry field); (b) Ask desires "dinner and a show" + "outside under the lights" (no venue dining/outdoor attribute) — omitted, not empty; (c) "cheap AND close" / walking distance (no client geolocation yet) — shipped as price-only "Easy on the wallet"; (d) "quiet"/"loud" desires are GENRE PROXIES, labeled as such in the UI (mood is not a verified attribute — gated on the un-ratified Emotion & Vibe layer); (e) plan conflict detection assumes a 3h run when `end_time` is null (never presented as fact) | Design brief card anatomy (Spark Line, Emotion Glyph) + member-preferences "provenance on every suggestion, never fabricate" + §5 no-fabricated-data | Each clears when its data lands: Spark Line ← Descriptor Foundry pipeline; dinner/outdoors ← per-venue attributes (Tier-2 OSM / venue self-report, Step 6/7 schema); distance ← opt-in browser geolocation; mood ← Emotion & Vibe ratification; end_time ← provider end_time coverage measured or a duration model. Re-reviewed when any of those ships | OPEN |
| R-026 | 2026-07-24 | Two high-severity npm advisories in the web app's production tree have NO released upstream fix: `postcss` GHSA-6g55-p6wh-862q (build-time file read via sourceMappingURL) and `sharp` GHSA-f88m-g3jw-g9cj (libvips CVEs), both pinned by `next` (npm `fixAvailable:false`). The real runtime-exploitable `next` CVEs were FIXED in-change (next 15.5.20→15.5.21, fast-uri→3.1.4); these two remain because no stable `next` release yet accepts patched postcss/sharp. Exposure is contained: postcss runs at build over our own CSS, and the feed uses CSS `background-image` not `next/image` so `sharp` is off the runtime path | SCA policy (`docs/EXTERNAL_FINDINGS_POLICY.md` / `docs/SCA_BASELINE.md`): production deps clean at high/critical — deviated by a founder-directed (2026-07-24) managed exception, not an override | The managed allowlist (`security/sca_allowlist.json`, enforced by `tools/sca_gate.py`) auto-fails the gate the instant either package reports a fix available OR on 2026-08-24 expiry, whichever first — forcing a `next` bump + entry deletion. Re-checked on every web dependency PR | OPEN |
| R-029 | 2026-07-25 | SeatGeek + Eventbrite deterministic importers (and migration 0011 widening the `licensed_event` provider check to include `eventbrite`) are BUILT and unit-tested against fixtures, but CANNOT run — `SEATGEEK_CLIENT_ID` / `EVENTBRITE_TOKEN` are founder-crucial new-service credentials not in the environment. Each importer also carries documented live-payload-shape ASSUMPTIONS (SeatGeek: 1-indexed paging + `meta.total` + `datetime_utc` filter param names; Eventbrite: `major_value` price strings, `ticket_availability` expand, organizer-vs-organization id) that must be verified against a FIRST live call before the feed is trusted | Charter cost/new-services (agents never mint keys) + §5 data-trust (a licensed feed is not "live" until its real payload shape is verified) | Founder approves the two services and mints read credentials → a single live `--dry-run` per provider confirms the payload shape (assumptions above) and non-zero normalized count → then the feed seeds `licensed_event`. Until then these providers contribute ZERO rows and must not be described as live | OPEN |
| R-030 | 2026-07-25 | The consumer read path now UNIONs `event ∪ licensed_event` (web/lib/promoted.ts) so PROMOTED long-tail events display, and `worker/promote.py` now populates the card columns (title/category/subsegment/ticket_url) 0010 documented it as writing. But promoted VOLUME is still gated: (a) migrations 0011/0012 must be applied to Supabase before the anon read of `event` works; (b) a human/ops promote (`api/ops_candidates.py` → `worker/promote.py`) must run per candidate — AI never auto-promotes; (c) the crawl path stays a trickle until the reviewer-flagged Phase-3 work lands — multi-event extraction (one page→N candidates), per-source listing URLs (fetch the calendar not the homepage), full date resolution (null `start_time` hides events from the tonight window), and non-anchor corroboration (universities/media HOLD forever on one source). Publishing crawl events at earned confidence via the shadow-only convergence engine is a FOUNDER-CRUCIAL trust-invariant decision, NOT taken here | Charter Prime Directive 1 (AI never publishes — promotion stays human-custodied) + §5 coverage completeness + the 3 independent 2026-07-24 reviews (friction/feasibility/source-completeness, this session) | Migrations 0011/0012 applied + first ops-promote run makes anchor-class promoted events appear; the Phase-3 crawl-improvement items are each their own scheduled build; convergence-live is escalated to the founder as a decision (keep human-promote vs publish-at-earned-confidence) and remains OPEN until the founder rules | OPEN |
| R-031 | 2026-07-25 | The new `brain/` persistent knowledge-graph (Graph Engineering paper adoption) is a PROVEN, self-contained foundation, NOT yet wired into anything live. Five parts of the paper deliberately not built: (1) recurrence/temporal validity — supersede is a flag, no bitemporal "true at T, superseded at T+1" intervals; (2) multi-hop query optimization — `subgraph()` is a plain BFS over an in-memory edge list, no indices (fine for thousands of nodes, not millions); (3) convergence wiring — Claim.confidence is designed to feed `worker/convergence` but is NOT connected (that coupling is founder-crucial); (4) live-pipeline adoption — the graph does not yet subsume `worker/resolve_entities.py` fuzzy matching and nothing writes to it from ingestion; (5) crash durability/concurrency — single-writer JSONL snapshot, no append-during-crash recovery or multi-process locking | Graph Engineering paper (§IV.D temporal, §V.B query, §VII staged path) + charter Brain doc §1C/§1D best-in-class temporal recall + convergence spec §11 (couplings are founder-ratified) | Each part has its own objective trigger (decision doc `docs/memory/decisions/2026-07-25_graph-engineering-adoption.md` §triggers): pipeline entity-resolution adoption ← a logged false/missed-merge trust incident OR Emotion Graph build begins (G-BRAIN-1D T1); temporal validity ← pgvector temporal-recall failures logged (T2) or relationship queries outgrow flat storage (T3), new infra spend then founder-crucial; convergence wiring ← explicit founder ruling only. Each is its own contract-first PR through the evaluator | OPEN |
| R-032 | 2026-07-25 | Headless-render fetch fallback (`worker/fetch/render_fetch.py`) reads JS-widget venue calendars (Squarespace/Wix/Bandsintown/"The Events Calendar") that the plain `requests.get` adapter can only see as an empty shell. Two deviations from the literal build request: (a) its `playwright` dependency is declared in a NEW `worker/requirements-render.txt`, NOT in `worker/requirements.txt` as asked — that file is bound into the golden-exam `HARNESS_MANIFEST` (ai/golden_exam.py) and adding any line there DRIFTS the extraction-certification hash and FAILS `trust_gate.py` (verified this session: current 48beca…, certified 390c9f…), which is a founder-crucial attended-exam re-lock, not an agent edit; (b) the fallback is IMPLEMENTED and unit-tested but NOT yet wired into the live pipeline — `worker/orchestrator.py` still calls `fetch_url` directly and is owned by another agent this session (off-limits), so no source is rendered in production yet. The `playwright` Python package is also NOT installed in this environment (Chromium binary IS pre-installed), so the browser-executing test skips here | Charter Prime Directive 1 scope note + Founder-crucial escalations (harness/gate-threshold changes are founder-only) + CLAUDE.md worker/requirements.txt exam-binding comment + §5 data-trust (a JS calendar silently rejected is lost coverage) | Founder/owning-agent decision: (1) to make rendering fire in production, wire `fetch_with_render` into `worker/orchestrator._run_one_source` in place of the bare `fetch_url` (owning agent's PR, through the evaluator); (2) `playwright` reaches `worker/requirements.txt` and gets installed ONLY through the standing three-step harness re-lock (attended exam on the new harness → authenticated CERTIFIED_HARNESS record → head-bound flag-flip), OR the team accepts `worker/requirements-render.txt` as the permanent install channel for this optional capability by a founder decision record. Until one of those, rendering contributes ZERO live reads and the browser test stays skipped where playwright is absent | OPEN |

| R-033 | 2026-07-25 | Multi-event AI extraction (read EVERY event on an unstructured page → N candidates, not 1) is BUILT and tested (patch preserved at scratchpad/multievent_agent.patch) but NOT landed: it rewrites the extraction PROMPT (`ai/prompts.py`) and adds a batch model (`worker/ai_models.py`) — both HARNESS_MANIFEST-bound — so per CLAUDE.md's re-lock it is eligible only if the same change closes extraction (flag→False, extraction OFF until re-cert), which is worse for live coverage. Reverted to keep extraction CERTIFIED + ON. NOTE: multi-event is ALREADY LIVE for machine-readable calendars (worker/importers/structured_feed.py reads every ICS/JSON-LD event, no AI, no cert) — universities/libraries/museums/civic + the many venues on WordPress/Squarespace; only truly unstructured pages need the AI-crawl multi-event | Charter extraction-certification gate (changing the certified extraction harness is founder-crucial — the attended golden exam proves the new extractor doesn't hallucinate) + §5 coverage completeness | The correct re-cert-free path: do the fan-out in `worker/ai_extract.py` (NOT manifest-bound) by segmenting a page into per-event blocks and calling the UNCHANGED certified single-event extractor per block — build that; OR the founder runs the standing three-step re-lock (attended exam on a multi-event harness → authenticated CERTIFIED_HARNESS record → head-bound flag-flip) to land the prompt-based version. Structured multi-event covers most calendars meanwhile | OPEN |

| R-034 | 2026-07-25 | Segment-based multi-event extraction (worker/segment.py + ai_extract.extract_candidates) reads EVERY event on a page by running the UNCHANGED certified single-event extractor per block — no re-cert (R-033's re-cert-free path, DONE). But the deterministic segmenter UNDER-segments in known cases: (1) a shared venue/date header rides only the first block (later events under-populated, not wrong); (2) items dated only in a column header (rows carry artist+venue only) fall back to one whole-page block → first event only; (3) multi-day festival grids / table layouts under-segment; (4) a single JSON-LD event or malformed JSON falls back to whole-page. ALL misses fail toward under-segmentation (= today's single-event behavior), NEVER toward fabricated events | §5 coverage completeness (read all events) — the miss is coverage, never trust | Improve the segmenter as real pages expose gaps: propagate a shared header to all blocks; table/grid-aware splitting; feed the brain (worker) a per-source "events live at selector X" hint so segmentation targets the real list. Each is a bounded ai_extract/segment change (not manifest-bound → no re-cert), landed as pages reveal the need; structured_feed already covers the ICS/JSON-LD calendars exactly | OPEN |

| R-035 | 2026-07-25 | The automated surface regression-exam (`ai/surface_exam.py`, `tools/surface_regression_exam.py`, corpus `ai/golden/surface/pages.json`, wired into `tools/validate`) is LIVE as a deterministic, free (no network/model/spend) NO-REGRESSION guard on the un-certified extraction SURFACE (worker/segment.py + ai_extract fan-out): it runs ~6 labeled multi-event pages through the REAL pipeline with a RECORDED single-event provider and FAILS if event recall drops below any page's MEASURED baseline or any fabrication appears. Two scope notes recorded as honest limits, NOT silent: (a) it is explicitly NOT a certification — the attended golden-exam (`ai/golden_exam.py`) remains the sole authority for the certified prompt/model, and this guard touches ZERO HARNESS_MANIFEST files; (b) the recorded provider returns the FIRST labeled event whose signature is in a block, so it measures whether the SEGMENTER hands each event to the extractor — it does NOT and cannot measure real-model recall on unsegmented prose (whether the live model reads a second event out of a fused block), which is the attended exam's job; the two R-034 hard-case pages therefore carry honest sub-1.0 baselines (0.333) and the guard only forbids dropping BELOW them | §9.6 "a gate that cannot fail proves nothing" + Charter Prime Directive 1 scope (surface is NOT the certified harness) + §5 coverage completeness (recall must not silently regress) + KAIZEN §M7 (measured, one-way ratchet) | Baselines are a ONE-WAY RATCHET: when the segmenter improves (R-034 fixes: shared-header propagation, table/grid splitting, per-source selector hints), re-measure via `python tools/surface_regression_exam.py` and RAISE the affected page's baseline_recall in the same PR — the number only ever goes up. A hard-case page reaching recall 1.0 and its baseline updated to 1.0 partially resolves the corresponding R-034 gap. This row stays OPEN as long as the guard is a live gate documenting its recorded-provider scope limit | OPEN |

| R-036 | 2026-07-25 | The armed-cron smoke-run binding (`tests/test_arming_smoke_binding.py` vs `docs/evidence/ARMING_SMOKE_RUN.json`) is RED because this session's ingest-path runtime code changed — `worker/ai_extract.py` (multi-event fan-out) and `worker/segment.py` (new) are in the armed cron's transitive import closure (tools/arming_runtime.py), so the recorded green smoke run no longer covers the current head. This is the gate working as designed (a runtime change must be re-exercised by a green smoke run before it is trusted on the schedule), not a code bug | Charter Sentinel rule + §14.3 (armed loop's runtime code must be proven by a green smoke run) + §9.6 (evidence must actually cover the head) | Operational re-arm (cannot run Actions from the dev sandbox): dispatch a fresh ingest smoke run on the branch head via Actions, confirm green, then update `docs/evidence/ARMING_SMOKE_RUN.json` (run_head_sha + run details) in a docs-only commit — the binding then covers `ai_extract`+`segment` and goes green. Required before this PR merges | OPEN |

**Adding an entry:** same commit as the deferral it records; cite the bar
section (or "n.a. — bar gap", which itself is a finding); give an objective
trigger, never "someday". **Resolving:** flip status to
`RESOLVED (<commit/PR>)`; leave the row. **Session close:** review OPEN
rows — resolve, re-affirm (trigger still pending), or escalate; a row whose
trigger has fired but wasn't acted on is a defect.
