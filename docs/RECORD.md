# RECORD — the deviations-from-world-class register ("Recording")

Greppable summary: founder-directed (2026-07-13) ledger of EVERY deferral,
hold, or watch — any "for now", "check later", "revisit", "ok for now",
noticed-but-not-fixed. The standard is that such moments should not exist
(everything checked against the documented world-class bar for that item);
when one does exist it is recorded HERE in the same commit, naming the bar
it deviates from and the objective trigger that resolves it. Silent deferral
is a violation. Mechanical enforcement in code: `tools/deferral_scan.py`
(wired into `tools/validate`) — a deferral-language comment must carry an
`[R-###]` tag pointing at a live entry. Prose (docs, PR text, chat) is
covered by the charter rule + evaluator review. Entries are never deleted:
resolved entries flip status to RESOLVED with the resolving commit/PR.

Format: `R-### · opened · what is deferred/held · world-class bar it
deviates from (cite) · resolution trigger · status`.

| # | Opened | What | Bar deviated from | Resolution trigger | Status |
|---|---|---|---|---|---|
| R-001 | 2026-07-13 | Sentry wired for error monitoring only; performance tracing disabled (`traces_sample_rate=0` in `worker/sentinel.py`, `web/instrumentation*.ts`) | WORLD_CLASS §7 observability (golden signals incl. latency) | DSNs live + first real traffic (Sprint Step 9 preview): revisit sample rate as a deliberate, costed decision | OPEN |
| R-002 | 2026-07-13 | `visual_regression` gate permanently SKIPs (no booted app/baselines) — pre-existing defect D4 | §9.6 "a test that cannot fail proves nothing" (applies to gates) | First deployed preview URL (Step 9): capture baselines, make the gate fire in CI | RESOLVED (2026-08-03, UI/UX session: the trigger's INTENT — a gate that can actually fire — is satisfied deterministically rather than against the live URL, because live data changes nightly and a pixel gate over it would cry wolf (§9.6 cuts both ways). Baselines committed (`tests/visual_baselines/tonight-*.png`, 4 pages) captured via `tools/visual_check.sh` from a local production boot in SYNTHETIC QA fixture mode (`web/qa/fixtures.ts` — fictional data, frozen clock, visible banner, fail-closed off in deployments); determinism proven by an independent boot+capture comparing 0/329160 pixels different; the gate fires in CI on every web-touching PR via `.github/workflows/visual-regression.yml` (Chromium pinned to the baselines' build 1194 = Playwright 1.56.0), first firing on this change's own PR; `tools/validate` now RUNS the check when the environment has a browser + web deps. Browserless environments' skip is the narrower R-068) |
| R-003 | 2026-07-13 | 4 moderate npm advisories accepted (postcss-via-next, no upstream fix) per `docs/SCA_BASELINE.md` | §3.5 dependency hygiene / SCA | `next` ships with postcss ≥ 8.5.10 → bump + clear baseline row (CI re-audits every web PR) | RESOLVED 2026-07-31 — trigger fired: `web/package.json` now pins an `overrides` bump forcing postcss ≥ 8.5.12 (resolves 8.5.25, since `next` still pins the vulnerable 8.4.31), so `npm audit --omit=dev` is clean at ALL severities incl. the moderate postcss XSS row (GHSA-qx2v-qp2m-jg93). SCA_BASELINE moderate row cleared. |
| R-004 | 2026-07-13 | STATE.md GROUND_TRUTH block stale (reconciler needs `gh` + DB DSN, absent in this sandbox); prose carries verified truth | §0.4 disk is truth (machine-verified) | First session in a credentialed env: `session_reconcile.py --heal`; also queued: GitHub-API fallback for the reconciler | OPEN |
| R-005 | 2026-07-13 | FRICTION_LOG entry #1 attacked by the generator model (PROVISIONAL), not non-Claude | §0.2 write/grade separation extended to planning | `OPENAI_API_KEY` present in session env (founder added; verify next session) → re-attack, clear flag. Blocks Step 5 arming. TRIGGER AMENDED 2026-07-21 (the key never materialized; the non-Claude channel is the CI evaluator, Entry #2 precedent): the re-attack is DELIVERED — PR #43 r1 (run 29867512512, REQUEST-CHANGES, recorded with written answers in FRICTION_LOG entry #3, which restates Entry #1's Step-5 surface). Flips RESOLVED only in the post-merge bookkeeping commit citing the PR's final APPROVE run id — never inside the PR's own diff (r1 finding) | RESOLVED (2026-07-22, this post-merge commit: the non-Claude re-attack ran as PR #43's CI adversarial review across 21 REQUEST-CHANGES rounds with every finding answered in FRICTION_LOG entry #3 — which restates Entry #1's Step-5 surface — and the arming merged at its terminal APPROVE, run 29881030319, on final head 7f6f600, fully green; Entry #1's PROVISIONAL grade is superseded by that written non-Claude verdict) |
| R-006 | 2026-07-13 | Extraction hallucination-rate threshold unratified (§11.2 proposes ≤1% golden-set, release-blocking) | §11.2 eval thresholds stated, not implied | Founder one-line ratification (asked in the standing unblock list). Blocks Step 6 | RESOLVED (founder 2026-07-15: "BEGIN at 1%" + one-way Kaizen ratchet, docs/KAIZEN.md §M7 — the NUMBER; the gate that proves it is R-013, extraction stays blocked until it ships and passes) |
| R-007 | 2026-07-13 | Source catalog ranks 42–118 unpopulated (43 of 120+ target) | §5 data-trust coverage / catalog completeness | Sprint Step 6 exit gate (extraction evals green): ranks populated via the weekly source-backfill loop, or the remainder explicitly descoped by a founder decision record | OPEN |
| R-008 | 2026-07-13 | Ingestion cron deliberately unarmed (`ingest.yml` manual-only) | Charter Sentinel rule + §14.3 (a scheduled loop without dead-man + caps is forbidden — so this hold IS the bar; recorded for visibility) | Secrets P2/P3 in Actions + friction attack → arming PR through the gate (full incident narrative — pickup lag, escalations, re-registration — consolidated into docs/ops/INCIDENT_2026-07-22_cron-scheduler.md at PR #49 r2; that file is an ASSEMBLED, edited narrative with its provenance stated in its header — r3 corrected this pointer's original copy-fidelity claim, and the exact wording history is git history) | RESOLVED (2026-07-22 ~12:05Z, all three citations exist: (1) arming PR #43 merged FULLY GREEN at evaluator APPROVE run 29881030319, master ab6819a; (2) green capped runs on the merged code throughout — 29885464970, 29887985205, 29917588982 @ post-#46 master 76d2290; (3) genuine schedule-event runs 29899042357 (2026-07-22T07:07:04Z, success) + 29909962538 (09:56Z) on the original minutes, and 29927836751 (14:17:02Z, success) on the re-registered 9,29,49 minutes. SCOPE BOUNDARY (PR #49 r1): this row records the unarmed-cron hold, which ended at arming + proven scheduled fires; the ACTIVE sparse-delivery deviation is OPEN as R-023 — never buried here) |
| R-009 | 2026-07-13 | PR #4 (draft, migration 0008) and PR #7 (superseded by #9's port) still open | §0.3 contract hygiene — parallel branches don't accumulate unreviewed | Founder one-liners: close #7 as superseded? finish-or-defer #4? (asked in the standing unblock list) | RESOLVED (founder 2026-07-15 "Close both": #7 superseded-by-#9, #4 closed as Step 7 reference draft — closing notes on both) |
| R-010 | 2026-07-13 | Option 1D (graph memory: Graphiti + graph DB) deliberately NOT built — the ratified brain is 1A+1B; 1D is the benchmark ceiling for temporal recall (Zep 63.8% LongMemEval) we chose not to buy yet. UPDATE 2026-07-25: the bi-temporal VALID-time CAPABILITY 1D was going to buy (point-in-time "as of T" recall) is now built in pure stdlib in `brain/` (R-031 part 1, R-041) — measured internally at knowledge_update 1.00; what remains unbought is the graph-DB INFRASTRUCTURE (indexed scale to millions of nodes) and any EXTERNAL validation against the public LongMemEval leaderboard, both still founder-crucial spend | Brain doc §1C/§1D — best-in-class temporal recall | The standing G-BRAIN-1D trigger (brain doc §RATIFIED): fires on T1 Emotion Graph build begins / T2 pgvector temporal-recall failures logged / T3 relationship queries outgrow SQL → friction attack → founder decision (money/new services) | OPEN |
| R-011 | 2026-07-13 | (retro-recorded — surfaced by deferral_scan's new SQL pass) migration 0006's `event` public-read policy `using (true)` exposed private-event columns to the anon key, held as an "accepted tradeoff" comment | §6 RLS least-privilege / fail-closed | Narrow before any client-side anon-key use | RESOLVED (migration 0007_narrow_event_public_read.sql) |
| R-013 | 2026-07-15 | Extraction remains BLOCKED (router flag False; provider consults it at its entry point) — the 1% bar is ratified (R-006) but the golden-set gate that PROVES it does not exist: no golden data, no live-exam runner, no CI wiring | docs/KAIZEN.md §M7 — no extraction change ships without exam evidence at a valid sample size | Step 6 build (next focused session, contract-first, evaluator mandatory): ≥40-example golden set (~320 facts ≥ the 1% sample floor) + live-exam runner + blocking CI job; flag flips in the commit that attaches a PASSING result for the starting model | RESOLVED (2026-07-18, bootstrap complete — evidence AND enforcement both live in this tree). Evidence: attended exam run 29659010747 on master 083c430, model claude-opus-4-8, PASSED — asserted_facts=316 (≥300 floor), hallucination_rate=0.0063 (≤0.01), recall=0.9782 (≥0.8), injections=0; report artifact 8433778947, zip sha256 8e73830378…. Enforcement: the record ai/golden/CERTIFIED_HARNESS.json entered ONLY through the base-owned online authenticator (extraction-eval.yml, PR #40 golden-exam green), its co-gate log attached to the adversarial review by the base-owned evidence channel, and the offline trust_gate re-lock (check_extraction_certification) binds the record's harness hash to every future tree — shipped in the same commit that flipped this row. Historical staging record — status as written at PR #31 (2026-07-17), BEFORE the exam ran; preserved for audit, superseded by the resolution above (evaluator nit, PR #36 r2: stale present-tense wording read as contradicting the resolution): the corrected harness was on master and PR #31 staged the flag flip, held red by the release gate until a verified attended exam run existed — bound to that PR's exact head SHA/prompt/model/golden/harness/dependency-lock and passing rate bar + BOTH ≥300-fact floors + zero injections; at that time no such run existed (precondition: R-018's environment setup; probe dispatch 29581813635 correctly failed closed at the policy proof, no key touched), and the row was to flip RESOLVED only in a commit citing the passing run id + verifier output (evaluator on PR #31 r1: a record must describe what exists) — exactly the condition the 2026-07-18 resolution satisfies |
| R-012 | 2026-07-14 | Kaizen maturity LEVELS per pipeline stage deliberately not built (measures M1–M6 are live; levels graded on an idle factory measure nothing) | docs/KAIZEN.md §Levels — full Kaizen includes staged maturity | First real scheduled ingestion cycle completes (R-008 resolved + one cron week) → design level rubric on actual flow, grade every stage, add to ledger | OPEN |
| R-014 | 2026-07-15 | `extraction-eval.yml` ran PR-controlled code with the repo-wide `ANTHROPIC_API_KEY` on `pull_request` (r5 nit, escalated to BLOCKER by evaluator r7: the release gate itself was a credential-exfiltration surface) | §11.4 AI-supply-chain / secret-exposure hardening (deep review); fail-closed auth | — | RESOLVED (design v2, founder-directed 2026-07-15 after discovering environment required-reviewers is an ORG-PLAN-ONLY feature unavailable on this personal repo: the secret-bearing exam now lives solely in `extraction-exam-dispatch.yml`, fireable ONLY by a manual maintainer workflow_dispatch — the attended-human property — with the environment-scoped `ANTHROPIC_API_KEY_EXAM`; the PR-triggered `extraction-eval.yml` carries ZERO secrets and goes green only on verified attended evidence for the PR's exact head SHA — report must be a PASS for the production-routed model against the PR's exact prompt hash, checked by tools/verify_exam_evidence.py. PR code is never run with the key by any automatic trigger) |
| R-015 | 2026-07-15 | (historical description of the deferral as filed — final state ships implemented light mode + renders for all three, per status) Design proposals ship light mode as palette specifications only — no rendered light comps (rendering 3 full light variants when 2 directions will be discarded fails least-costly-method-first; design/proposals/README.md §Deferral) | ONE_LIVE_MASTER_DESIGN_BRIEF_v2.4 §4 "light + dark themes" (design canon requires both) | Founder picks a direction → the Step 9 implementation PR ships the winner's light-mode render + visual-regression baselines (light + dark) | RESOLVED (same PR, evaluator round 2: light mode implemented via html.light overrides in all three directions + light renders committed for all three — deferral cured, not carried) |
| R-016 | 2026-07-14 | PR-aggregator research (`docs/research/PR_AGGREGATOR_RESEARCH.md`): §3/§5/§8 pricing + ToS figures are single-pass search-index reads, NOT adversarially verified (sandbox egress proxy 403'd direct fetches to vendor/venue sites); sections are labeled BEST-EFFORT in-doc | §0 verification bar — figures driving a spend/build decision must be primary-source-verified | Founder greenlights the PR-aggregator venture (or any contract/build against a named provider) → re-verify that provider's live pricing page + ToS from an unproxied machine, and put the "do transformed diffs count as redistribution?" question to them in writing, BEFORE any spend or ingestion code | OPEN — **TRIGGER FIRED 2026-07-15 (founder: "Go")**. Actioned same-commit as far as this sandbox allows: redistribution-question letters drafted for all four finalists (`ventures/promise_ledger/docs/PROVIDER_REDISTRIBUTION_QUESTIONS.md`); the primary-source pricing/ToS re-verification remains egress-blocked here. Row closes when the founder (a) sends the letters and (b) the live-page pass runs from an unproxied machine. No provider code or spend until then |
| R-017 | 2026-07-14 | Promise-ledger golden set is SYNTHETIC-ONLY (2 authored examples exercising harness mechanics) and the EDGAR client has never run against live EDGAR — this sandbox's egress blocks sec.gov (curl + WebFetch verified blocked 2026-07-14). The harness mechanically refuses to PASS on synthetic-only data (`ventures/promise_ledger/eval/golden.py`) | §0 verification bar — extraction thresholds mean nothing without real labeled data; the analysis names extraction precision the venture's existential risk | First session in an environment with sec.gov egress (or founder allowlists sec.gov/data.sec.gov/efts.sec.gov): run the EDGAR client under budget, seed ≥20 real 8-K EX-99.1 labeled examples, delete nothing synthetic (mark superseded), and only then treat PRECISION_BAR as meaningful. Blocks any extraction-model work | OPEN — **PARTIAL PROGRESS 2026-07-15**: www.sec.gov unblocked by founder; first LIVE run executed (declared UA, budgeted) — 2 REAL same-day EX-99.1 earnings press releases (JPM, BAC) stored with provenance in `ventures/promise_ledger/eval/source_material/`; live run immediately caught + fixed a filename-recall bug (authoritative index-headers path now implemented, real fixtures committed). Row stays OPEN until ≥20 REAL labeled examples exist |
| R-018 | 2026-07-17 | Exam-secret full mechanical protection depends on the `extraction-exam` environment deployment-branch policy (`master`-only) — a GitHub setting the evaluator cannot verify from the code diff; the dispatch workflow proves it fail-closed at runtime but a branch-copy could delete its own guard, so only GitHub enforcing the policy fully closes it | auth/secret fail-closed on misconfiguration (repo bar §11.4; charter trust invariants) — MITIGATED by the runtime proof step + founder-accepted documented constraint (docs/memory/decisions/2026-07-17_exam-secret-branch-policy.md) | The `master`-only deployment-branch policy MUST be configured before `ANTHROPIC_API_KEY_EXAM` is set; until then the runtime proof step blocks every exam run (no secret is configured today, exam is bootstrap-red) | OPEN (founder merged #25 2026-07-17 with this as a documented design constraint) |
| R-019 | 2026-07-18 | `empty-env` fail-open class at 4 ledger catches (#11/#12/#14) with no structural fix — the repeat-class threshold (3) is crossed; `tools/kaizen_trends.py` alarms on every validate run until fixed | docs/KAIZEN.md repeat-class rule ("repeat classes must trend to zero via gate-gap fixes") | Next PR touching `.github/workflows/**` ships the env-contract workflow linter (TODOS P2) — or any 5th family catch forces it immediately, whichever fires first | RESOLVED (2026-07-18 same-day, same PR #35: evaluator r4 correctly refused the deferral — the trigger had ALREADY fired because the PR itself touches `.github/workflows/**`. `tools/workflow_env_lint.py` shipped: R1 every env var a run step consumes has a visible reviewed source, R2 `vars.` context banned outright; blocking in `tools/validate`; 13 tests; all 7 live workflows clean) |
| R-021 | 2026-07-21 | (allocated during the PR #43 arming arc; R-020 rides that PR) Extraction emitted bare clock strings ("7:00 pm", "6pm", "06:00 PM", "8:00 a.m.") that reached the candidate store's timestamptz column and failed the source's insert — a fetchable event LOST to a formatting detail; hit ~4 of 10 sources sampled in the arming smoke runs (29876232668, 29877305892) | §5 data-trust completeness: a fetchable source must not be structurally unparseable; trust rule: never fabricate a date, never lose an event | Fix at the extraction→store boundary, in its own PR per the exam classifier's prescribed path for extraction-surface changes | RESOLVED (this PR: worker/datetime_normalize.py wired in worker/ai_extract.py's shaping layer — timestamps stored ONLY when the string evidences a full calendar date; refused claims become NULL with the raw AND a machine-readable reason preserved in extracted._provenance.unstored_datetime_claims (reasons: unparseable / ambiguous-numeric-date / unrecognized-timezone-abbreviation / no-full-date-evidence) and the candidate kept for ops review; 36 tests incl. the four live failure strings; proven live in run 29878035857 — two time-only sources produced HELD candidates, zero insert errors) |
| R-020 | 2026-07-21 | Dead-man period/grace live in the founder's healthchecks.io account, outside the repo's mechanical reach — cadence↔period agreement was founder-confirmed prose, not CI-asserted (PR #43 r9 finding; r11 rejected the bounded-deferral answer for a LIVE cron — correctly) | Charter Sentinel rule + §7 no-silent-degradation: alarm config must be machine-verified for a recurring loop | MECHANISM SHIPPED in PR #43 (r11): tools/assert_deadman_period.py runs as a BLOCKING ingest precondition — reads the check's live period/grace via a READ-ONLY healthchecks API key, fails closed on mismatch/pause/missing-key/unreadable-API, and tests/test_ingest_workflow_contract.py recomputes the expected period from the cron line so cadence/declaration/live-check cannot drift. Row RESOLVES when the founder-minted HEALTHCHECKS_API_KEY_RO secret exists and the assertion step passes green in the pre-merge head smoke run | RESOLVED (2026-07-21: trigger fired exactly — founder minted the read-only key in the check's own project; head run 29876232668 step "Assert dead-man period" GREEN against the live API: check 'onelive-ingestion', period 1200s, grace 600s, ping-binding proven by n_pings/last_ping matching attempt 3's success ping to the second; the assertion now blocks every scheduled run, so the contract is standing, not one-shot) |
| R-022 | 2026-07-22 | Two review-harness reporting defects surfaced by PR #46 r2/r3, fix deferred to a dedicated gate-custody PR: (a) tools/validate has no "all diff-applicable gates green" terminal state — a run whose only skip is a Record-bound non-applicable gate (R-002 pre-deploy) self-labels INCOMPLETE-ACKNOWLEDGED, forcing reviewers to re-derive its lawfulness each round; (b) adversarial-review.yml's packet preamble claims lockfile/SCA evidence is "in the attached web log" even on non-web diffs where that log correctly says not-applicable — a false packet claim. STRUCTURAL NOTE on (b): the file is base-owned (pull_request_target) — a same-PR fix cannot affect the PR's own review packet, so a separate PR is the only effective path, not a preference | validate/§1 "couldn't verify must not look like passed" (reporting precision half); adversarial-review evidence integrity (packet claims must match attached logs) | Dedicated gate-custody PR through the evaluator (both fixes are tightenings) — opened before or at the next session close; trigger fires as defect if a third PR round re-litigates either defect first | RESOLVED (same day, THIS PR at the evaluator's r5 insistence — the deferral was withdrawn: (a) tools/validate non-quick --allow-skips terminal state is now EXECUTED-GATES-GREEN, stating that every gate that ran passed and every skip is Record-bound enumerated debt (exit semantics unchanged, release-evidence caveat retained); (b) adversarial-review.yml's packet preamble now asserts npm/SCA evidence ONLY for web-touching diffs, so the packet claim can never outrun its attached logs — base-owned, so (b) operates on future PRs' packets, which is the only venue it ever could) |
| R-023 | 2026-07-22 | ACTIVE OPERATIONAL DEVIATION (opened at PR #49 r1 — an active deviation may not live inside a RESOLVED row): GitHub's hosted scheduler delivers the armed 20-minute cron SPARSELY. Measured 2026-07-22 (window 01:07Z first eligible slot → 15:49Z; THIS ROW IS THE SINGLE SOURCE OF TRUTH for these numbers — STATE/evidence point here, corrected at r4 which caught the prior arithmetic conflating pickup lag with delivery rate): 3 genuine schedule fires (07:07Z, 09:56Z on the old minutes; 29927836751 at 14:17Z on the new 9,29,49 registration) across ~44 eligible slots ≈ 7% slot-fire rate — average delivery ≈ one run per ~4.9 h (inter-fire gaps 2h49m and 4h21m; the ~2.5–3 h figure is the REGISTRATION PICKUP LAG, a different quantity). Consequences: (a) at ~4.9 h/run, ~49 source-attempts/day vs the §14.3 budget shape's ~720 (10 × 72 slots) — the ~266-source catalog swept ≈ once per ~5.4 days vs the budgeted ~3 sweeps/day; (b) two inter-ping gaps (09:58→11:55Z, 11:57→14:17Z) exceeded the dead-man period+grace (1200s+600s) — [HISTORICAL, AS FILED 15:1xZ; settled by trigger part 2 below, which proved the alarm DID flip for both gaps (r6 nit: this opening context is the question as it stood at filing, not current truth):] whether the healthchecks alarm actually ALERTED in those windows was UNVERIFIED from the agent environment at filing (the read-only healthchecks key exists only as an Actions secret; the 14:17Z run's assert step proved the check live and unpaused at that moment, not that alerts were delivered). Owner: agent (measurement + verification), founder (decision) | Founder cadence directive 2026-07-21 ("accelerate the ingestion to every 15 or 20 minutes") + §14.3 budget shape (10/run × 72 slots/day ≈ 3 visits/source/day — the arithmetic the sparse delivery breaks) + charter Sentinel rule (the alarm must reflect real coverage, and an alarm that may not have alerted is a defect, not coverage) | THREE-PART OBJECTIVE TRIGGER, all due by 2026-07-23 ~15:30Z: (1) 24h slot-fire density on the 9,29,49 registration — ≥80% resolves the delivery half as self-healed (measurement cited); <80% requires the founder decision among the delivered options (external metronome = new service/credential, founder-crucial; alarm-period rematch = founder-ratified Sentinel change; explicit founder acceptance of sparse delivery, recorded) — resolution cites the decision record; (2) SATISFIED 2026-07-22 ~22:38Z via PATH A, mechanically (run 29963532221, the REPORT_FLIPS probe shipped in PR #51; first probe run 29963320514 proved RO-key flips readability by HTTP 200, then failed loud by design on the hosted service's wrapped body — parser fixed, second run green): the check's 24h flip history shows 30 flips / 15 DOWN events, and EACH originally-questioned gap has its own covering DOWN event (citations corrected at the evaluator's r5 — the first version cited 14:48:40Z, which belongs to a LATER gap): gap 09:58→11:55Z alerted at DOWN 10:28:44Z (period+grace expiry, 30 min after the 09:58:44Z ping) and gap 11:57→14:17Z alerted at DOWN 12:27:15Z (30 min after the 11:57:15Z ping) — THE ALARM FIRED for both; the Sentinel mechanism is NOT defective, coverage is confirmed, and the crying-wolf state is now measured fact (~15 DOWN/day tracking the sparse-delivery gaps), which is the hard evidence for the pending founder alarm decision (A′ rematch / B metronome / explicit acceptance). PATH B (founder confirmation) is no longer needed for verification — though the founder's report of received-alert volume remains useful for tuning notification settings; (3) manual capped dispatches may bridge rotation meanwhile but never count toward density | OPEN |

| R-024 | 2026-07-24 | `licensed_event` (migration 0010) has no CHECK constraints on `confidence`, `status`, or `source_provider` — an importer bug could store an invalid trust/status value that the DB would accept (evaluator nit on PR #58) | §5 data-trust / fail-at-the-DB-boundary: invalid trust-state values must not be silently storable | Next migration touching `licensed_event` (e.g. the read-path PR) adds idempotent CHECK constraints via a DO-block ALTER (`confidence in ('confirmed','likely','unverified','disputed')`, `status in ('scheduled','cancelled','moved')`); the table already exists so it must be an ALTER guarded against re-add. No new invalid values can enter meanwhile — the importer writes fixed literals | RESOLVED (PR #58, same-day: the evaluator escalated it to blocking on r5 — a public trust field must not accept 'banana'; migration 0010 now adds guarded idempotent CHECK constraints `licensed_event_confidence_chk` (confirmed/likely/unverified/disputed) + `licensed_event_status_chk` (scheduled/cancelled/moved), `licensed_event_provider_chk` (ticketmaster/seatgeek — the R-024 source_provider half, added r7), plus price (min<=max) and lat/lng-range checks, via a DO-block ALTER applied by the next import run) |
| R-063 | 2026-08-01 | (renumbered from a duplicate R-025 — evaluator nit, PR #142 r1) The Continental Club case-study read pass (docs/strategy/ONE_LIVE_CASE_STUDY_CONTINENTAL_v1.md) used search-index snapshots of the venue's public surfaces because the session sandbox's network policy blocks direct page fetches — same public data, different access path than the production agent's direct crawl | Case-study bar: a proof artifact shown outside the founder conversation must be built the way production builds it | Before ANY partner-facing or external use of the case study (or on the next session with an open network policy, whichever first): re-run the read as a direct crawl of the same surfaces, diff against the recorded extraction table, and update the doc + Kaizen-ledger any divergence | OPEN |
| R-025 | 2026-07-24 | CAPCOG geo scoping fetches a single 60-mi radius around downtown Austin (`ticketmaster.py`) — may miss outer CAPCOG counties (Fayette, Llano, Lee, Blanco) and include some non-CAPCOG area; a circular radius is an approximation of the 10-county boundary (evaluator nit on PR #58) | §5 coverage completeness — a market's boundary should be represented, not approximated, once volume/precision matters | Post-launch coverage review (or founder flag): replace the single radius with county/city-scoped multi-queries or a verified CAPCOG boundary polygon; the `unmapped`/coverage metrics logged per import quantify the gap in the meantime | OPEN |
| R-050 | 2026-07-24 | Two founder-directed deviations to make previews zero-config ("fix this — tired of all this wasted time"): (a) a NON-production Vercel deployment (VERCEL_ENV=preview/development) auto-resolves the auth gate to "disabled" WITHOUT an explicit flag — the evaluator's standing concern (PR #59 r4) is that this assumes Vercel Deployment Protection is ON, which the app cannot verify; (b) the PUBLIC Supabase URL + publishable (anon) key are committed as built-in fallback defaults (web/lib/licensed.ts), a deliberate exception to charter "environment never in git" (RENUMBERED from R-028 on the 2026-07-25 merge with master, which had independently assigned R-028 to a different deferral; pre-merge references to R-028 on branch claude/live-site-readpath mean THIS row) | Charter "AI never publishes / stealth gate before launch" + auth-fail-closed bar (evaluator) + "environment never in git" | BOUNDED, not open-ended: (a) PRODUCTION never auto-opens (fail-closed, the only publicly-reachable target) and /ops is denied without a real provider in every mode, so nothing sensitive is exposed; clears when the Clerk stealth gate lands for go-live (the auto-open then only matters for previews, which are host-protected). (b) The committed values are PUBLIC by design (publishable key ships in every browser; RLS on licensed_event is the real boundary; service_role/Clerk secrets are NEVER committed) and are OVERRIDDEN by any env var; clears/revisited if the key is ever rotated (→ code change) or a staging/prod DB split is needed. Both are founder-accepted go-live posture (founder's call per charter); the evaluator reviews the change and the founder may override at merge as before | OPEN |
| R-049 | 2026-07-24 | Consumer feed features intentionally NOT shipped because `licensed_event` has no backing column — shown honestly (chip/feature absent or labeled a proxy), never faked: (a) Spark Line descriptor (no `spark_line`/Descriptor-Foundry field); (b) Ask desires "dinner and a show" + "outside under the lights" (no venue dining/outdoor attribute) — omitted, not empty; (c) "cheap AND close" / walking distance (no client geolocation yet) — shipped as price-only "Easy on the wallet"; (d) "quiet"/"loud" desires are GENRE PROXIES, labeled as such in the UI (mood is not a verified attribute — gated on the un-ratified Emotion & Vibe layer); (e) plan conflict detection assumes a 3h run when `end_time` is null (never presented as fact) (RENUMBERED from R-027 on the 2026-07-25 merge with master, which had independently assigned R-027 to a different deferral; pre-merge references to R-027 on branch claude/live-site-readpath mean THIS row) | Design brief card anatomy (Spark Line, Emotion Glyph) + member-preferences "provenance on every suggestion, never fabricate" + §5 no-fabricated-data | Each clears when its data lands: Spark Line ← Descriptor Foundry pipeline; dinner/outdoors ← per-venue attributes (Tier-2 OSM / venue self-report, Step 6/7 schema); distance ← opt-in browser geolocation; mood ← Emotion & Vibe ratification; end_time ← provider end_time coverage measured or a duration model. Re-reviewed when any of those ships | OPEN |
| R-048 | 2026-07-24 | Two high-severity npm advisories in the web app's production tree have NO released upstream fix: `postcss` GHSA-6g55-p6wh-862q (build-time file read via sourceMappingURL) and `sharp` GHSA-f88m-g3jw-g9cj (libvips CVEs), both pinned by `next` (npm `fixAvailable:false`). The real runtime-exploitable `next` CVEs were FIXED in-change (next 15.5.20→15.5.21, fast-uri→3.1.4); these two remain because no stable `next` release yet accepts patched postcss/sharp. Exposure is contained: postcss runs at build over our own CSS, and the feed uses CSS `background-image` not `next/image` so `sharp` is off the runtime path (RENUMBERED from R-026 on the 2026-07-25 merge with master, which had independently assigned R-026 to a different deferral; pre-merge references to R-026 on branch claude/live-site-readpath mean THIS row) | SCA policy (`docs/EXTERNAL_FINDINGS_POLICY.md` / `docs/SCA_BASELINE.md`): production deps clean at high/critical — deviated by a founder-directed (2026-07-24) managed exception, not an override | The managed allowlist (`security/sca_allowlist.json`, enforced by `tools/sca_gate.py`) auto-fails the gate the instant either package reports a fix available OR on 2026-08-24 expiry, whichever first — forcing a `next` bump + entry deletion. Re-checked on every web dependency PR | RESOLVED 2026-07-31 (founder directive "Do 1") — fixed at the ROOT rather than suppressed: `web/package.json` pins `overrides` forcing `postcss` ≥ 8.5.12 (→ 8.5.25) and `sharp` ≥ 0.35.0 (→ 0.35.3). A `next` bump alone was insufficient (15.5.x AND 16.x still pin postcss 8.4.31 / sharp ^0.34.x), so the override reaches the patched packages deterministically — ending the CI/local `fixAvailable` nondeterminism. All three high/critical allowlist entries deleted (GHSA-6g55-p6wh-862q, GHSA-r28c-9q8g-f849, GHSA-f88m-g3jw-g9cj); `npm audit --omit=dev` clean; `npm run build` + typecheck pass; SCA gate PASS. |
| R-029 | 2026-07-25 | SeatGeek + Eventbrite deterministic importers (and migration 0011 widening the `licensed_event` provider check to include `eventbrite`) are BUILT and unit-tested against fixtures, but CANNOT run — `SEATGEEK_CLIENT_ID` / `EVENTBRITE_TOKEN` are founder-crucial new-service credentials not in the environment. Each importer also carries documented live-payload-shape ASSUMPTIONS (SeatGeek: 1-indexed paging + `meta.total` + `datetime_utc` filter param names; Eventbrite: `major_value` price strings, `ticket_availability` expand, organizer-vs-organization id) that must be verified against a FIRST live call before the feed is trusted | Charter cost/new-services (agents never mint keys) + §5 data-trust (a licensed feed is not "live" until its real payload shape is verified) | Founder approves the two services and mints read credentials → a single live `--dry-run` per provider confirms the payload shape (assumptions above) and non-zero normalized count → then the feed seeds `licensed_event`. Until then these providers contribute ZERO rows and must not be described as live | OPEN |
| R-030 | 2026-07-25 | The consumer read path now UNIONs `event ∪ licensed_event` (web/lib/promoted.ts) so PROMOTED long-tail events display, and `worker/promote.py` now populates the card columns (title/category/subsegment/ticket_url) 0010 documented it as writing. But promoted VOLUME is still gated: (a) migrations 0011/0012 must be applied to Supabase before the anon read of `event` works; (b) a human/ops promote (`api/ops_candidates.py` → `worker/promote.py`) must run per candidate — AI never auto-promotes; (c) the crawl path stays a trickle until the reviewer-flagged Phase-3 work lands — multi-event extraction (one page→N candidates), per-source listing URLs (fetch the calendar not the homepage), full date resolution (null `start_time` hides events from the tonight window), and non-anchor corroboration (universities/media HOLD forever on one source). Publishing crawl events at earned confidence via the shadow-only convergence engine is a FOUNDER-CRUCIAL trust-invariant decision, NOT taken here | Charter Prime Directive 1 (AI never publishes — promotion stays human-custodied) + §5 coverage completeness + the 3 independent 2026-07-24 reviews (friction/feasibility/source-completeness, this session) | Migrations 0011/0012 applied + first ops-promote run makes anchor-class promoted events appear; the Phase-3 crawl-improvement items are each their own scheduled build; convergence-live is escalated to the founder as a decision (keep human-promote vs publish-at-earned-confidence) and remains OPEN until the founder rules | OPEN |
| R-031 | 2026-07-25 | The new `brain/` persistent knowledge-graph (Graph Engineering paper adoption) is a PROVEN, self-contained foundation, NOT yet wired into anything live. Five parts of the paper deliberately not built: (1) recurrence/temporal validity — [BUILT 2026-07-25: Claims now carry bi-temporal VALID-time intervals (`valid_from`/`valid_to`), and `Graph.claims_valid_at`/`as_of_subgraph` serve point-in-time "true as of T" recall independent of the transaction-time supersede flag; measured on the brain benchmark, knowledge_update 0.60 → 1.00 (R-041). This closes part (1) for the in-memory brain; the REMAINING open items in this row are the graph-DB/scale, convergence, live-pipeline, and durability parts below]; (2) multi-hop query optimization — `subgraph()` is a plain BFS over an in-memory edge list, no indices (fine for thousands of nodes, not millions); (3) convergence wiring — Claim.confidence is designed to feed `worker/convergence` but is NOT connected (that coupling is founder-crucial); (4) live-pipeline adoption — the graph does not yet subsume `worker/resolve_entities.py` fuzzy matching and nothing writes to it from ingestion; (5) crash durability/concurrency — single-writer JSONL snapshot, no append-during-crash recovery or multi-process locking | Graph Engineering paper (§IV.D temporal, §V.B query, §VII staged path) + charter Brain doc §1C/§1D best-in-class temporal recall + convergence spec §11 (couplings are founder-ratified) | Each part has its own objective trigger (decision doc `docs/memory/decisions/2026-07-25_graph-engineering-adoption.md` §triggers): pipeline entity-resolution adoption ← a logged false/missed-merge trust incident OR Emotion Graph build begins (G-BRAIN-1D T1); temporal validity ← pgvector temporal-recall failures logged (T2) or relationship queries outgrow flat storage (T3), new infra spend then founder-crucial; convergence wiring ← explicit founder ruling only. Each is its own contract-first PR through the evaluator | OPEN |
| R-032 | 2026-07-25 | Headless-render fetch fallback (`worker/fetch/render_fetch.py`) reads JS-widget venue calendars (Squarespace/Wix/Bandsintown/"The Events Calendar") that the plain `requests.get` adapter can only see as an empty shell. Two deviations from the literal build request: (a) its `playwright` dependency is declared in a NEW `worker/requirements-render.txt`, NOT in `worker/requirements.txt` as asked — that file is bound into the golden-exam `HARNESS_MANIFEST` (ai/golden_exam.py) and adding any line there DRIFTS the extraction-certification hash and FAILS `trust_gate.py` (verified this session: current 48beca…, certified 390c9f…), which is a founder-crucial attended-exam re-lock, not an agent edit; (b) the fallback is IMPLEMENTED and unit-tested but NOT yet wired into the live pipeline — `worker/orchestrator.py` still calls `fetch_url` directly and is owned by another agent this session (off-limits), so no source is rendered in production yet. The `playwright` Python package is also NOT installed in this environment (Chromium binary IS pre-installed), so the browser-executing test skips here | Charter Prime Directive 1 scope note + Founder-crucial escalations (harness/gate-threshold changes are founder-only) + CLAUDE.md worker/requirements.txt exam-binding comment + §5 data-trust (a JS calendar silently rejected is lost coverage) | Founder/owning-agent decision: (1) to make rendering fire in production, wire `fetch_with_render` into `worker/orchestrator._run_one_source` in place of the bare `fetch_url` (owning agent's PR, through the evaluator); (2) `playwright` reaches `worker/requirements.txt` and gets installed ONLY through the standing three-step harness re-lock (attended exam on the new harness → authenticated CERTIFIED_HARNESS record → head-bound flag-flip), OR the team accepts `worker/requirements-render.txt` as the permanent install channel for this optional capability by a founder decision record. Until one of those, rendering contributes ZERO live reads and the browser test stays skipped where playwright is absent | OPEN |

| R-033 | 2026-07-25 | Multi-event AI extraction (read EVERY event on an unstructured page → N candidates, not 1) is BUILT and tested (patch preserved at scratchpad/multievent_agent.patch) but NOT landed: it rewrites the extraction PROMPT (`ai/prompts.py`) and adds a batch model (`worker/ai_models.py`) — both HARNESS_MANIFEST-bound — so per CLAUDE.md's re-lock it is eligible only if the same change closes extraction (flag→False, extraction OFF until re-cert), which is worse for live coverage. Reverted to keep extraction CERTIFIED + ON. NOTE: multi-event is ALREADY LIVE for machine-readable calendars (worker/importers/structured_feed.py reads every ICS/JSON-LD event, no AI, no cert) — universities/libraries/museums/civic + the many venues on WordPress/Squarespace; only truly unstructured pages need the AI-crawl multi-event | Charter extraction-certification gate (changing the certified extraction harness is founder-crucial — the attended golden exam proves the new extractor doesn't hallucinate) + §5 coverage completeness | The correct re-cert-free path: do the fan-out in `worker/ai_extract.py` (NOT manifest-bound) by segmenting a page into per-event blocks and calling the UNCHANGED certified single-event extractor per block — build that; OR the founder runs the standing three-step re-lock (attended exam on a multi-event harness → authenticated CERTIFIED_HARNESS record → head-bound flag-flip) to land the prompt-based version. Structured multi-event covers most calendars meanwhile | OPEN |

| R-034 | 2026-07-25 | Segment-based multi-event extraction (worker/segment.py + ai_extract.extract_candidates) reads EVERY event on a page by running the UNCHANGED certified single-event extractor per block — no re-cert (R-033's re-cert-free path, DONE). But the deterministic segmenter UNDER-segments in known cases: (1) a shared venue/date header rides only the first block (later events under-populated, not wrong); (2) items dated only in a column header (rows carry artist+venue only) fall back to one whole-page block → first event only; (3) multi-day festival grids / table layouts under-segment; (4) a single JSON-LD event or malformed JSON falls back to whole-page. ALL misses fail toward under-segmentation (= today's single-event behavior), NEVER toward fabricated events | §5 coverage completeness (read all events) — the miss is coverage, never trust | Improve the segmenter as real pages expose gaps: propagate a shared header to all blocks; table/grid-aware splitting; feed the brain (worker) a per-source "events live at selector X" hint so segmentation targets the real list. Each is a bounded ai_extract/segment change (not manifest-bound → no re-cert), landed as pages reveal the need; structured_feed already covers the ICS/JSON-LD calendars exactly | OPEN |

| R-035 | 2026-07-25 | The automated surface regression-exam (`ai/surface_exam.py`, `tools/surface_regression_exam.py`, corpus `ai/golden/surface/pages.json`, wired into `tools/validate`) is LIVE as a deterministic, free (no network/model/spend) NO-REGRESSION guard on the un-certified extraction SURFACE (worker/segment.py + ai_extract fan-out): it runs ~6 labeled multi-event pages through the REAL pipeline with a RECORDED single-event provider and FAILS if event recall drops below any page's MEASURED baseline or any fabrication appears. Two scope notes recorded as honest limits, NOT silent: (a) it is explicitly NOT a certification — the attended golden-exam (`ai/golden_exam.py`) remains the sole authority for the certified prompt/model, and this guard touches ZERO HARNESS_MANIFEST files; (b) the recorded provider returns the FIRST labeled event whose signature is in a block, so it measures whether the SEGMENTER hands each event to the extractor — it does NOT and cannot measure real-model recall on unsegmented prose (whether the live model reads a second event out of a fused block), which is the attended exam's job; the two R-034 hard-case pages therefore carry honest sub-1.0 baselines (0.333) and the guard only forbids dropping BELOW them | §9.6 "a gate that cannot fail proves nothing" + Charter Prime Directive 1 scope (surface is NOT the certified harness) + §5 coverage completeness (recall must not silently regress) + KAIZEN §M7 (measured, one-way ratchet) | Baselines are a ONE-WAY RATCHET: when the segmenter improves (R-034 fixes: shared-header propagation, table/grid splitting, per-source selector hints), re-measure via `python tools/surface_regression_exam.py` and RAISE the affected page's baseline_recall in the same PR — the number only ever goes up. A hard-case page reaching recall 1.0 and its baseline updated to 1.0 partially resolves the corresponding R-034 gap. This row stays OPEN as long as the guard is a live gate documenting its recorded-provider scope limit | OPEN |

| R-041 | 2026-07-25 | The brain memory eval (`brain/eval/`, `tools/brain_eval.py`, baselines `brain/eval/baselines.json`, wired into `tools/validate` as a one-way ratchet) MEASURES the knowledge-graph brain on OUR OWN 26-question labeled benchmark (6 agent-memory categories), NOT the public LongMemEval leaderboard. Two honest limits recorded, not silent: (a) it is a deterministic, self-authored corpus scored by exact match — the right tool for a free per-`validate` regression gate, but its numbers are NOT directly comparable to LongMemEval leaderboard figures (which need the public dataset + an LLM judge + real spend); the cited Zep/Graphiti ~63.8% is for positioning only. (b) UPDATE 2026-07-25 — the bi-temporal build landed and the ratchet FIRED UP: `knowledge_update` rose 0.60 → **1.00** (now 12/12) and overall 0.9231 → **1.00** (33/33). Claims carry VALID-time intervals (`valid_from`/`valid_to`) and `Graph.claims_valid_at`/`as_of_subgraph` serve point-in-time "as of date X" recall; the 2 old-miss questions now pass and 7 more point-in-time questions (a 3-era capacity fact + a correctly-abstaining "before any record" case) were added, all green. baselines.json raised to 1.00 in the SAME change; a 4th planted regression (a TIME-BLIND answerer) proves the new competency can still turn the gate RED. The temporal gap is CLOSED **on our own 33-question benchmark** — explicitly NOT the public LongMemEval leaderboard (limit (a) below is unchanged) | §9.6 "a gate that cannot fail proves nothing" (satisfied — 4 planted regressions turn it RED in `tests/test_brain_eval.py`) + charter Brain doc §1C/§1D best-in-class temporal recall + §0 verification bar (a benchmark's scope must be stated, never overclaimed as the public leaderboard) | Two triggers: (1) baselines are a ONE-WAY RATCHET — FIRED for bi-temporal recall (2026-07-25): re-ran `python tools/brain_eval.py`, RAISED knowledge_update to 1.0 in the same change; any future gain re-measures and raises again (the number only goes up). (2) Running the REAL LongMemEval (dataset + budgeted LLM judge via `tools/model_router.py`) is a founder-crucial spend decision, staged behind G-BRAIN-1D alongside the graph-DB scale build — the `brain/eval` read surface is the adapter seam. This row stays OPEN because the gate is still our-own-benchmark (limit (a)): our 1.00 is NOT comparable to the ~63.8% LongMemEval figure, and only the public run under an LLM judge would validate the capability externally | OPEN |
| R-036 | 2026-07-25 | The armed-cron smoke-run binding (`tests/test_arming_smoke_binding.py` vs `docs/evidence/ARMING_SMOKE_RUN.json`) is RED because this session's ingest-path runtime code changed — `worker/ai_extract.py` (multi-event fan-out) and `worker/segment.py` (new) are in the armed cron's transitive import closure (tools/arming_runtime.py), so the recorded green smoke run no longer covers the current head. This is the gate working as designed (a runtime change must be re-exercised by a green smoke run before it is trusted on the schedule), not a code bug | Charter Sentinel rule + §14.3 (armed loop's runtime code must be proven by a green smoke run) + §9.6 (evidence must actually cover the head) | Operational re-arm (cannot run Actions from the dev sandbox): dispatch a fresh ingest smoke run on the branch head via Actions, confirm green, then update `docs/evidence/ARMING_SMOKE_RUN.json` (run_head_sha + run details) in a docs-only commit — the binding then covers `ai_extract`+`segment` and goes green. Required before this PR merges | OPEN |

| R-037 | 2026-07-25 | The acquisition toolkit (`brain/acquisition.py`) has two partial world-class features: (a) confidence DECAY is failure-driven only — a recipe untouched for N days does not lose confidence from STALENESS alone (only empty/failed acquires lower it); (b) seed recipes flag `calendar_url_is_homepage=True` when the catalog gave only a homepage, but AUTO-DISCOVERY of the real listing URL is not built — an agent must still find the true calendar page. Neither is faked: staleness is simply not yet decayed, and a homepage is flagged honestly, never presented as the events page | `docs/strategy/ONE_LIVE_ACQUISITION_TOOLKIT_v1.md` §9 (world-class self-improving acquisition: confidence should reflect staleness; a recipe should target the real listing, not the homepage) + §5 data-trust (never fabricate a URL) | Bounded `brain/acquisition.py` changes as real runs expose the need: (a) add a `decay_confidence(now)` pass applied at read/record time keyed on `last_success_at` age; (b) add a `record_rediscovery(source_id, new_calendar_url, ...)` learning step that supersedes the recipe with the discovered listing URL. Each is its own contract-first PR through the evaluator; both are non-manifest, no re-cert | OPEN |
| R-038 | 2026-07-25 | Conflict handling when two agents learn DIFFERENT recipes for one source is last-writer-wins: `record_outcome`/`register_recipe` supersede on a single-writer JSONL snapshot, so concurrent divergent learning serialises and the loser's state is retained in history but never MERGED or arbitrated | `docs/strategy/ONE_LIVE_ACQUISITION_TOOLKIT_v1.md` §9(8) + brain decision doc (`2026-07-25_graph-engineering-adoption.md` §crash-durability/concurrency, R-031 part 5) — single-writer snapshot, no multi-process merge | A merge/arbitration policy PR (prefer the recipe with higher measured reliability; flag genuine method disagreement for review) lands when a real multi-agent run logs a divergent-recipe collision, OR when the brain gains multi-writer durability (R-031 part 5), whichever first. Its own contract-first PR through the evaluator | OPEN |
| R-039 | 2026-07-25 | The `needs_rediscovery` flag (moved-page trigger) is SET but not ROUTED — nothing yet puts a flagged recipe into an ops/human review queue, so a recipe that starts failing is marked in the toolkit but no person is actually asked to re-discover it | `docs/strategy/ONE_LIVE_ACQUISITION_TOOLKIT_v1.md` §8/§9(9) — a failing recipe needs a human-review path, not just a flag; Charter Sentinel spirit (a detected degradation must reach a human) | A small ops surface lists recipes where `needs_rediscovery=True` (queryable via `AcquisitionToolkit.all_recipes()`), wired when the ops console gains a maintenance view — its own PR. Until then the flag is queryable programmatically and surfaced by the demo, never silently swallowed | OPEN |
| R-040 | 2026-07-25 | The acquisition toolkit is a PROVEN, self-contained foundation NOT wired into anything live: no live ingestion code reads `recipe_for` before fetching or calls `record_outcome` after. `worker/orchestrator.py` still selects method/fetches without consulting the toolkit, and it is owned by another agent this session (off-limits), so no production acquisition is toolkit-guided yet | Charter mission (Steps 5–10 ingestion) + `docs/strategy/ONE_LIVE_ACQUISITION_TOOLKIT_v1.md` §9(10) — the loop only compounds once the live path reads/writes it | Wire `fetch`-time `recipe_for` + post-fetch `record_outcome` into `worker/orchestrator._run_one_source` (owning agent's PR, through the evaluator); non-manifest, no re-cert. Until then the toolkit contributes ZERO live reads and must not be described as guiding production acquisition | OPEN |

| R-042 | 2026-07-25 | The Brain IQ score (`brain/iq.py`, ledger `docs/metrics/BRAIN_IQ_LEDGER.md`, CLI `tools/brain_iq.py`, wired into `tools/validate` as a one-way ratchet on KNOWLEDGE + EFFICIENCY) continuously scores + trends THREE kinds of smartness, but three known kinds are DELIBERATELY NOT in the score yet — named on purpose so the "measure some, not all" gap is visible and shrinking, not hidden (the Goodhart-honesty control, `docs/metrics/BRAIN_MEASUREMENT_COVERAGE.md` + `brain/iq.py::NOT_YET_MEASURED`): (1) real PRODUCTION extraction yield/adoption — LEARNING is computed from a fixed SEEDED toolkit history because no live code reads `recipe_for`/`record_outcome` yet (R-040); (2) real WALL-LATENCY under load — only a single-run observational latency is recorded and it is NEVER gated (machine-dependent/flaky), so throughput/tail-latency under concurrency is unmeasured; (3) the EXTERNAL LongMemEval public leaderboard — KNOWLEDGE is our own exact-match benchmark (R-041), not the public dataset+LLM-judge figure. None is faked: LEARNING is labeled seeded, latency is labeled observational-not-gated, and KNOWLEDGE is labeled our-own-benchmark | §9.6 "a gate that cannot fail proves nothing" (satisfied — `tests/test_brain_iq.py` turns the ratchet RED on a planted knowledge-down and a planted efficiency-work-up) + §0 verification bar (a score's scope must be stated, never overclaimed) + charter Brain doc best-in-class + KAIZEN §M7 one-way ratchet | Each staged part has its own trigger (mirrors the coverage doc): (1) LEARNING folds real yield/adoption when the live orchestrator is toolkit-guided (R-040 resolves); (2) a load/throughput harness adds a gated p50/p95 work-per-query number when the brain serves live query traffic; (3) running real LongMemEval via the budgeted model router is a founder-crucial spend decision (G-BRAIN-1D), the `brain/eval` read surface being the adapter seam. This row stays OPEN while any of the three remains out of the score; the ratchet on the measured two stays live meanwhile | OPEN |

| R-043 | 2026-07-25 | The multi-event fan-out (worker/ai_extract.extract_candidates → one real AI call per segment block) silently defeated the per-run FinOps ceiling: `--max-sources=N` caps PAGES, not AI calls, so post-fan-out one page = up to MAX_BLOCKS(200) extraction calls, i.e. the fixed 10-source schedule authorized up to ~2000 calls/run instead of ~10 — a fail-OPEN cost hole (found by the blind interaction review 2026-07-25). FIXED: a per-page AI-call cap `EXTRACT_MAX_EVENTS_PER_PAGE` (default 50) bounds calls/page, restoring a hard, predictable per-run bound of max_sources × cap (≤500 calls/run at the schedule ceiling); overflow blocks are DEFERRED to a later run and LOGGED, never silently dropped; ingest.yml's now-half-true "double-capped" comment corrected. Remaining deviation: the DEFAULT cap value (50) is a FinOps threshold — founder-tunable — and a rich (>50-event) calendar defers its tail across runs rather than pulling it all in one run | Charter Sentinel rule + §14.3 "budget caps BEFORE first scheduled run" (a scheduled loop must have a hard AI-spend ceiling, fail-closed) + §5 coverage (deferral must be visible, never a silent drop) | Founder confirms/tunes `EXTRACT_MAX_EVENTS_PER_PAGE` for the cost/coverage tradeoff (and optionally a per-RUN total-call budget if per-page × sources is still too high); the per-page bound + the console monthly cap are the two live backstops meanwhile. A truly per-run token/call budget threaded through the orchestrator is the fuller fix if the founder wants a single hard number | OPEN |

| R-045 | 2026-07-25 | The HELD-OUT brain eval (`brain/eval/held_out.py` + hidden corpus `brain/eval/held_out_pages.json`, blind judge `tools/brain_held_out_eval.py`, base-owned workflow `.github/workflows/brain-held-out-eval.yml`, doc `docs/strategy/ONE_LIVE_HELDOUT_EVAL_v1.md`) closes the dev/test-split gap in the brain benchmark: the visible set (`brain/eval/benchmark.py`, R-041) is committed, so an optimizing PR could overfit those exact questions and post a fake gain. This adds a DISJOINT (no shared question text / gold answer — proven in `tests/test_brain_held_out_eval.py`) held-out set scored by the SAME deterministic harness, whose AUTHORITY is the BASE ref's copy run on `pull_request_target` (a PR cannot see-and-alter the copy that grades it). TWO honest residuals recorded, NOT silent: (a) DISTRIBUTION INFERENCE — the hidden corpus is committed to git, so base-ownership defeats self-grading/tampering but NOT a capable agent reading it from history and optimizing for the inferred distribution; a truly blind set needs an off-repo secret store (future, founder-crucial). (b) The held-out set mirrors the visible set's STRUCTURE (same 6 categories + claim-encoding), so an agent can overfit the FAMILY of questions even though exact strings/answers are disjoint. Also: the workflow MUST execute the PR head's brain engine (the artifact under test) under `pull_request_target` — mitigated to a safe residual by read-only token (`permissions: contents: read`), zero secrets (deterministic/offline eval), and `persist-credentials: false`; documented in the workflow header, not hidden | §9.6 "a gate that cannot fail proves nothing" (satisfied — 4 planted regressions + an overfitting-memorizer all turn it RED) + the ML dev/test-split bar (a benchmark an optimizer can edit measures nothing) + §0 verification bar (a gate's scope + residual risk stated, never overclaimed as fully blind) | Two triggers: (1) held-out floors (`held_out_pages.json` → `baselines.categories`) are a ONE-WAY RATCHET — when the brain improves, re-measure and RAISE in the same PR; lowering is founder-crucial. (2) The distribution-inference residual (a) closes only with an OFF-REPO secret held-out store (or a rotating generated set) — a founder-crucial trust/spend decision; until then a held-out pass is NECESSARY, not SUFFICIENT, and this row stays OPEN | OPEN |
| R-044 | 2026-07-25 | The `golden-exam` required CI check is RED-WHEN-HEALTHY: it refuses to certify any change to extraction-SURFACE code (correct, by charter design + compensated by the blocking adversarial review), but emits a `failure` conclusion, so a genuinely-fine PR carries a red required check that a human must fetch logs to distinguish from a real failure. I flagged this in the 2026-07-25 tradeoff analysis (recommendation #2) and then ROUTINIZED it ~8× ("no action") — the exact anti-pattern OPERATING_RULES §1 ("a repeated error is a finding, not a rhythm", PR #65) forbids. Determination (recorded per that rule): accepted-cost for MERGE (compensated refusal) BUT a latent ops defect (ambiguous required-red invites either complacency past a REAL red or wasted triage) | OPERATING_RULES §1 (repeated error = finding, recorded determination required) + §9.6 (a signal that's red when healthy erodes the gate's meaning) | Founder-crucial gate-custody fix (base-owned extraction-eval.yml + the classifier): make the proven-non-manifest refusal emit a NEUTRAL `REFUSED-COMPENSATED` status (or drop it from required checks in that case) so red always means a real problem — proposed for founder/evaluator ratification, NOT self-applied on the base-owned exam. Meanwhile every occurrence references THIS row instead of ad-hoc re-explanation | OPEN |

| R-051 | 2026-07-25 | PR #59 merged WITHOUT the charter-mandatory independent (non-Claude) evaluator review. The review did not fail — it REFUSED to run: the branch diff reached 1.26 MB against `tools/adversarial_review.py`'s 800 KB cap, and the tool hard-fails rather than review a TRUNCATED diff (correct, fail-closed). Root cause: a whole session's work (154 files, 45 commits, ~21k added lines) accumulated into ONE PR, so the diff outgrew what any single review can cover. The founder was given the choice explicitly (raise the cap / split now / merge on founder authority) and chose to merge, on the basis that `trust-gate` was fully GREEN — 1578 tests, all trust invariants (AI-never-promotes intact), and the SCA gate — with the unreviewed trust-path surface limited to `worker/promote.py` (curated-domain category signal), `worker/source_catalog.py` (new, non-fabricating lookup) and `worker/importers/structured_feed.py` (@type-first classification), each covered by tests. NOT a claim that the review passed: it never ran | CLAUDE.md Agent org — the evaluator is MANDATORY for promote-path/trust-core changes; §0 verification bar (an absent review must never be reported as a passed one) | Two triggers: (1) PREVENTION IS LIVE — `tools/pr_size_check.py` (wired into `tools/validate`, 7 tests) measures the branch diff exactly as the review workflow builds it, reads the cap from the workflow so it cannot drift, and warns at 70% to split WHILE SPLITTING IS STILL CHEAP; every subsequent PR must land under the cap so the evaluator actually reviews it. (2) The three unreviewed trust-path files get an evaluator pass on the next PR that touches them (each is small enough to review in isolation). Row closes when a non-Claude review has covered those three files | OPEN |
| R-052 | 2026-07-26 | The second review seat's model id is a FLOATING ALIAS (`gemini-flash-latest`), not an immutable pin. `*-latest` resolves provider-side, so the reviewer's actual model can change with no commit in this repo — real gate-custody drift, since review strength becomes partly provider-controlled. It is used because it is the only id known to work: `gemini-2.5-pro` is refused by this key's tier (429 `limit: 0`) and `gemini-2.5-flash` is retired for new users (404). The compensating controls that exist today: the preflight PROVES callability with a live probe on every run, and the workflow literal is test-bound to the tool's own default so the two cannot drift apart | Gate custody — the evaluator model is a reviewed constant (workflow comment, PR #14 r4), and an alias is not reviewed | The FIRST CI run whose preflight prints the advertised model list (it is skipped on the bootstrap PR that introduces it, so this fires on the next PR): read a CONCRETE id from that printed list and replace the alias in the same PR, keeping the workflow/tool test binding. If no concrete id in the list is callable, record that finding here instead of silently keeping the alias | OPEN |
| R-053 | 2026-07-26 | RESOLVED IN-PR (#72 r9) — the PR-owned workflow no longer sets `GEMINI_REVIEW_MODEL` at all. Both OpenAI lenses were right that deferring this to "the immediately following PR" was deferred gate-custody work, which the repo bar does not allow and which the escape hatch says must block in any round. The second seat's model is now read from the BASE-owned reviewer copy inside the preflight step, and a test asserts this workflow file never supplies either seat's model | Gate custody — a gate input the subject controls is a self-weakenable gate | RESOLVED (#72 r9: workflow override deleted, sync test replaced by an absence invariant, model read from the trusted base copy) |

| R-047 | 2026-07-25 | The category resolver (`worker/classify/category_resolver.py`) is now WIRED in production promote: `worker/promote.py` looks up the candidate's originating source in the curated catalog (`worker/source_catalog.cultural_domain_for_source`, exact case-insensitive name match against `sources/master_sources_catalog_120.json`) and passes the human-vetted `cultural_domain` as `resolve_category`'s `venue_domain_hint` (signal #3, above the title-keyword last resort). This closes the blind-review "resolver dead in promote" gap (it previously called `card_fields(title, ticket_link)` with NO signal, so every promoted event fell to the title read regardless of how well the source was known). Honest RESIDUAL, recorded not hidden and CORRECTED after inspection: 83 of 116 catalog sources now carry a `cultural_domain` (11 unambiguous single-venue music calendars — Mohawk/Emo's/Antone's/Stubb's/Continental Club/etc. — were tagged `live-music` 2026-07-25; a dedicated music venue's events ARE live-music, exactly the founder's principle). CRITICAL correction to the earlier "44 to tag" framing: MOST of the 33 still-untagged are CORRECTLY untagged — they are multi-category AGGREGATORS (Ticketmaster/SeatGeek ticketing, Instagram/Meetup social, Do512/city calendars, Bandsintown artist aggregators, Spotify/MusicBrainz identity spines) that legitimately span every domain, so forcing ONE `cultural_domain` on them would be FABRICATION; for these the per-event title read IS the correct behavior, not a deficiency. Paramount Theatre (austintheatre.org — theater/film/comedy/music) is likewise left untagged on purpose. So the curated-signal path is live for every source that HAS a single true domain; the untagged remainder is intentional, not a coverage hole. Fallthrough stays SAFE (unknown/untagged → None, never a guessed category). Non-catalog `@type`/venue-business-type signals remain unpopulated (would need extraction to capture JSON-LD `@type`, a separate build) | CLAUDE.md founder directive 2026-07-25 ("read what the source IS — a museum's event is visual-arts", not a title guess) + graph-engineering "every claim has a source" (each category carries provenance: which signal decided) + §5 no-silent-degradation (absent signal → honest 'Other', never fabricated) | One remaining trigger: capture schema.org `@type` at extraction time so signal #1 (the event's own declared type) fires per-event for the multi-category aggregators that emit JSON-LD — a separate extraction-surface build (re-cert-gated). (The "tag more sources" trigger is effectively resolved: single-domain sources are tagged; multi-domain sources MUST stay untagged.) Row stays OPEN while `@type` capture is absent | OPEN |

| R-046 | 2026-07-25 | The new KPI-setting + quarterly-prioritization process (`docs/strategy/ONE_LIVE_KPI_FRAMEWORK_v1.md`, ledger `docs/metrics/KPI_LEDGER.md`, CLI `tools/kpi_report.py`) aggregates existing measures (Kaizen, Brain IQ, extraction certification, trust_gate, RECORD.md) into one scorecard, but TWO honest gaps are recorded on purpose, mirroring the Goodhart-honesty pattern in `brain/iq.py`'s MEASURED/NOT_YET_MEASURED split rather than hiding either: (a) the **North Star metric** (proposed candidate: "verified live events on /tonight per served locale, trust-weighted") is explicitly UNRATIFIED — an agent proposed it as a strategically-sound candidate but picking the one number the whole team optimizes for is a founder-crucial call (CLAUDE.md), never an agent decision, and it is not measured or reported anywhere as if it were final; (b) SIX KPIs named in the framework doc §3 are **NOT YET INSTRUMENTED** by `tools/kpi_report.py` (each with its own why + trigger, canonical list `tools/kpi_report.py::NOT_YET_INSTRUMENTED_SLOTS`): source catalog size and scheduled cron slot-fire density (both need a live DB/healthchecks-API connection this stdlib/no-network tool does not have), production trailing hallucination rate (KAIZEN §M7 names the sampling input but no code tallies it yet), cost per verified published event (§14.2 — no live cost meter exists), the web app's own vitest suite (a different toolchain), and real user engagement/retention (the site is not public yet). None of the six is faked with a guessed number — each renders the literal text "not yet instrumented (trigger: ...)" | CLAUDE.md founder-crucial escalations (North Star = strategic "what do we optimize for" call) + §9.6 "a gate/score that cannot fail proves nothing" (a scorecard claiming full coverage would be a Goodhart trap) + charter Cost discipline "measure, don't guess" | Two independent triggers: (1) North Star — founder reviews §2 of the framework doc and ratifies, amends, or replaces the candidate; the row's North-Star half resolves the moment a ratification decision record exists. (2) Each not-yet-instrumented KPI resolves independently the moment its own trigger fires (DB/API access present, a production-sampling script ships, a live cost meter is wired, a vitest-count reader is added, or public launch + analytics land) — this row stays OPEN as a whole while ANY of the six remains uninstrumented or the North Star remains unratified | OPEN |
| R-026 | 2026-07-24 | Meta carousel engine has NO Graph API client — the posting boundary is deliberately ABSENT from the codebase (r1 evaluator correctly rejected the initial stubbed `MetaPublisher`; a `PublishRelease` is the complete auditable hand-off record until the real boundary exists), and metrics ingestion takes constructed `PostMetrics` objects, not live Insights pulls — no Meta app, page/IG assets, or tokens exist, and credential minting + go-live are founder-crucial (charter escalations) | Spec §9 (docs/strategy/ONE_LIVE_META_CAROUSEL_ENGINE_v1.md): a learning loop is only as real as the measured outcomes it ingests; until Insights flows, the bandit learns nothing live | Founder mints the Meta app + Graph API tokens (content publishing + insights, caps set first) + the `ONELIVE_APPROVAL_KEY` signing secret AND ratifies the posting posture → same-trigger work: build the Graph publisher + an Insights→`PostMetrics` importer, evaluator-mandatory (outward-facing surface) | OPEN |
| R-027 | 2026-07-24 | The carousel agent cycle (`social/carousel/agent_loop.py`) has no scheduled runner — `run_cycle` exposes a `deadman_ping` hook and a hard `max_drafts` budget cap, but no cron/workflow is armed | Charter Sentinel rule: "No scheduled loop ships without both" (healthchecks dead-man + Sentry) + §14.3 budget-caps-before-first-scheduled-run | R-026's trigger fires (keys + posture) → arm the cycle's cron in its own PR with a dedicated healthchecks check, budget ceiling, and the standing Friction pre-work for irreversible actions; until then cycles run manually in-session only | OPEN |
| R-028 | 2026-07-25 | Approval + autonomy-record authentication uses SYMMETRIC HMAC under ONELIVE_APPROVAL_KEY — any runtime that can verify can also sign (PR #65 r3 nit). Custody today: the key is deployment env only (never a repo value, never an API parameter, never agent-session env), so the signing capability lives only where the founder deploys it | Custody best practice: the release runtime should hold only a PUBLIC verification key (asymmetric signatures), so verification capability never implies signing capability | R-026's trigger (founder mints Meta credentials + the ops-console approval surface is built): implement Ed25519 founder signatures for approvals + the autonomy record in that PR, evaluator-mandatory | OPEN |
| R-078 | 2026-07-25 | (RENUMBERED 2026-08-04 from a duplicate R-029 — the SeatGeek/Eventbrite importer-credentials row keeps that id; duplicate caught by tests/test_record_ids_unique.py) Construction Loop Stage 3's GREEN-EXAMPLE retrieval is a practiced obligation, not yet a mechanical gate — construction_gate enforces only red-class citations (PR #67 r7: the canon must not claim more mechanism than exists). Green examples today: manual skim of docs/memory + session arcs, evidenced in the contract | Loop canon bar: both retrieval halves machine-consumed (the ExpeL split: insights AND trajectories) | Brain 1B build (existing TODOS P1 item, whose spec already returns SUCCESS neighbors): its recall tool becomes the green-example matcher and joins construction_gate in that PR, evaluator-mandatory | OPEN |
| R-079 | 2026-08-04 | Repo-level Actions secrets (ONELIVE_DB_DSN, ONELIVE_DB_PASSWORD, API keys) are readable by ANY workflow revision on ANY branch a push-capable actor dispatches — surfaced by the evaluator's attacker-smuggle finding on PR #174 (db-report.yml), but the property is repo-wide (ingest.yml, autopromote.yml, provider-dryrun.yml hold the same class of secrets and are branch-dispatchable; the arming-evidence pattern DEPENDS on branch dispatches of ingest.yml today). #174's own exposure is closed with a master-only ref guard; the guard itself lives in the dispatched file, so it bounds accident, not a hostile insider | Least-privilege credential scoping (WORLD_CLASS §security posture; the evaluator's finding states the bar) — production credentials should be bound to protected contexts, not reachable from arbitrary branch code | Whichever fires FIRST: (a) before any second human or bot account gains push access to the repo, or (b) when the flag-flip/arming-evidence flow stops requiring branch dispatches of credentialed workflows — move DB/API secrets into a GitHub deployment environment with a master-only branch policy (founder-crucial: credential custody; founder performs the GitHub settings change with agent-provided steps) | OPEN |
| R-057 | 2026-07-26 | A records-only close-out matched every red class in the index (count derived, never typed — `python tools/construction_gate.py | grep 'matched red classes' | tr ',' '\n' | wc -l`; HISTORICAL NOTE, not a live figure: earlier drafts of this row typed the count and it went stale in the very next round, twice), because RED_CLASSES triggers match diff CONTENT and a changelog paragraph that NARRATES a PR's round history necessarily names those classes. Answering 31 for a three-file docs change invites the pasted-inventory answer the evaluator caught at PR #88 r1 — the gate stays satisfied while retrieval stops happening | Construction Loop Stage 3: a citation is deliberate retrieval evidence, not a formality. Over-triggering is by design (RED_CLASSES header) and its cost is supposed to be one extra line, not one per class in the whole index | The next change touching tools/construction_gate.py for any reason: evaluate whether a RECORDS-ONLY diff (nothing under web/, worker/, api/, db/, tools/, .github/) should match on content at all, or only on the classes its own subject fixed. FOUNDER-CRUCIAL if the answer narrows any matcher — a narrowing is a gate-threshold relaxation and must be ratified, never adopted. Until then the answer is the honest two-group split used at PR #88 r2 | OPEN |
| R-058 | 2026-07-26 | `docs/metrics/KAIZEN_LEDGER.md` had no trailing newline, so every append produced a noisy two-line diff on the last row (openai nit, PR #88 r1) — cosmetic, and it makes ledger diffs harder to review, which is the opposite of what a ledger is for | Diff reviewability: an append should read as one added line | Fixed in the same commit as this row; the entry exists because a noticed defect is written down even when the fix is immediate | RESOLVED (PR #88 r2) |
| R-059 | 2026-07-26 | IDENTIFIED at PR #88 r6 — the test is `tests/test_arming_smoke_binding.py::test_reviewed_head_is_runtime_code_identical_to_the_smoke_run`, and the cause is env, not job state: the standalone `pytest -q` CI step declares NO env, so `GH_TOKEN`/`GITHUB_TOKEN` are unset and the test takes its explicit no-token skip; the `tools/validate` step sets `GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}`, so the same test RUNS there. That is exactly the +1 passed / -1 skipped between the two logs. Verified by reading the two steps' `env:` blocks out of `.github/workflows/adversarial-review.yml`, not inferred — an earlier draft of this row guessed "different flags" and `grep -n pytest tools/validate` disproved it. The skip is DELIBERATE and fail-closed (`ARMING_SMOKE_VERIFY=required` makes it an assertion failure rather than a skip in the authoritative venue, the trust-gate check), so nothing is silently unrun — but the standalone step's log still reads as a complete suite when one test did not execute in that position | A test's execution must not depend on which step it runs in, and a log presented as the suite must say which tests did not run in it | The next PR that owns `.github/workflows/adversarial-review.yml`: either give the standalone pytest step the same `GH_TOKEN` so both positions execute the same set, or add `-rs` to it so the skip is visible in the log that gets read. NOT done here: this contract's scope bound excludes `.github/`, and widening a scope to fix something noticed mid-PR is the exact class this PR spent five rounds on | OPEN (cause identified; the one-line workflow change is bound to the next PR that owns that file) |
| R-060 | 2026-07-27 | A session contract's declared SCOPE file list is checked by nobody: it drifted twice inside PR #88 alone (docs/RECORD.md at r2, docs/memory/RED_CLASSES.md at r3), and each time a human-equivalent reviewer had to notice. Both openai seats recommended a mechanical check | Contract-first discipline: a declared scope that the diff can silently exceed is not a bound, it is a sentence | The next change touching tools/construction_gate.py: add a check comparing `git diff --name-only <base>...HEAD` against the active contract's SCOPE, failing when the diff exceeds what the contract declares. A TIGHTENING, so it is not a gate-threshold relaxation and needs no founder ratification — but it is gate custody, so evaluator-mandatory. Until it exists, this contract states its scope as a derived command plus a BOUND (no path under web/, worker/, api/, db/, tools/, .github/), which a later commit cannot silently widen | OPEN |
| R-061 | 2026-07-29 | The Meta posting boundary (`social/carousel/meta_publisher.py`) is BUILT, gate-signature-bound (evaluator #106: it posts only a release whose HMAC verifies under the founder key, and only the draft's OWN hash-bound `slide.image_ref` images), but not end-to-end for live posting: (a) it posts each slide's bound `image_ref`, and the generator today leaves hook/cta slides' `image_ref` empty — so no draft is postable until the surface that RENDERS each branded slide-card to a PNG, hosts it publicly, AND writes that url into the draft's `image_ref` (so approval covers the exact images) exists; (b) only the Instagram carousel flow is implemented — `facebook_page` refuses loudly; (c) the Insights importer's Meta metric-name map (`accounts_engaged` → unique-accounts, `views` → impressions, etc.) is pinned to Graph API v21.0 and is not adversarially verified against a live account. All three fail LOUD (no partial post, no unbound image, no substituted north-star metric), so nothing over-claims | Spec §9.1: "the posting boundary is built at this trigger"; charter cost-discipline (reuse infra, no new service without founder sign-off) | R-026's trigger fires (founder mints META_ACCESS_TOKEN + surface node ids + ONELIVE_APPROVAL_KEY and ratifies posting posture): extend the generator/renderer so every slide's `image_ref` is a hosted rendered-card url covered by content_hash (reuse the existing Playwright render path + S3, no new service), add the FB-Page carousel endpoint, and verify the insights metric names against the live account before the first measured cycle. Each is an outward-facing/publish-path change → evaluator-mandatory. ADDED at #106 review r2, then BUILT as fail-closed invariants at r3 (the CHECKS now live in code; what remains is the DATA the checks demand, which R-026's infrastructure supplies): (d) the publisher requires every slide's `image_ref` to be CONTENT-ADDRESSED to the approved bytes — the bound `Slide.image_sha256` (covered by content_hash) must appear in the url — so a host cannot swap bytes behind an unchanged url; the R-061 renderer must host each card at its sha256-addressed url and set `image_sha256` (empty today = fail-closed). (e) the publisher calls `publish_gate.assert_events_still_publishable(draft)` at the LAST moment before posting, re-reading canonical state via the registered reader and refusing on drift (disputed/cancelled/started); plus a signed `released_at` + freshness window bounds the release age. The invariant is live NOW (no reader wired = refuse); R-026 wires the live reader so it passes on good state. (f) `import_exported_metrics(json, known_post_ids)` now REQUIRES a published-post allowlist and refuses any unknown/unbound post_id (empty allowlist = refuse); R-026's ops-console supplies the real published-post ids. Each was evaluator-reviewed on #106 | OPEN (invariants built; DATA population + deployment wiring bound to R-026) |

| R-062 | 2026-08-01 | The YouTube resolver (`worker/enrich/youtube.py`, the verified/Option-B half of the preview cascade) attaches a video ONLY from the entity's OWN `sameAs` YouTube channel, resolved via the cheap `channels → playlistItems → videos` path (3 quota units). A bare `/c/CustomName` custom-channel URL is INTENTIONALLY not resolved: the Data API v3 has no cheap parameter for a custom URL (only `search.list`, 100 units — ~33× the cost — and a name-shaped lookup at that), so an entity whose ONLY YouTube `sameAs` is a `/c/` custom URL gets no verified video (honest gap), not a quota-burning resolution. Canonical `UC…` ids, `@handles` (`forHandle`), and legacy `/user/` names (`forUsername`) ALL resolve — the `/c/` legacy form is the sole uncovered shape, and it is the least common on modern channels | Spec §3 (ONE_LIVE_VERIFIED_PREVIEW_ENRICHMENT_v1.md) "resolve by authoritative ID, never by free-text search alone" — a `/c/` URL is the entity's own authoritative link, but cheaply turning it into a channel id is not possible without the 100-unit search; §4 quota discipline "prefer resolved channel-id + 1-unit `videos.list`… cache; raising quota is free but slow"; §1.7 "honest gaps beat filler" | When the enrichment-miss log shows a MATERIAL share of otherwise-resolvable entities whose only YouTube `sameAs` is a `/c/` custom URL, add a single hard-CACHED `search.list` resolution for that form in its own PR (amortized quota well under the 10k/day free ceiling), evaluator-reviewed as it touches the enrichment path. Until the miss log proves the gap material, canonical ids + handles cover the common case and this stays an honest gap | OPEN |
| R-064 | 2026-08-01 | Truth-states v2 (six-state: confirmed / owner-confirmed / likely / unverified / disputed / stale + issue flags) is founder-ratified canon (docs/memory/decisions/2026-08-01_truth-states-v2-and-hypothesis-split.md, spec docs/strategy/ONE_LIVE_TRUTH_STATES_v2.md) but the RUNNING pipeline, its tests, the review persona's enforcement targets, and the CLAUDE.md charter text still implement/state the 4-state model — this session is docs-armed, so runtime code and CLAUDE.md cannot change here | Charter "disk is truth": ratified canon and running code must not diverge silently; CODING_CONVENTIONS names the confidence model as public API | Next code-armed session (or before ANY change to worker/confidence.py, worker/gating.py, tests/test_gates.py, or the public API confidence display merges — whichever first): implement the two additive states + flags per the spec, update tests/test_gates.py transitions, update the CLAUDE.md confidence-states paragraph, and mark this resolved. Until then no doc may claim the new states are live | OPEN |
| R-065 | 2026-08-02 | Rebrand OneLive→1Live is founder-directed "everywhere" (decision record 2026-08-02_rebrand-onelive-to-1live.md) and EXECUTED across living canon + both deliverables (335 mentions, 80 files, all figures re-rendered) — but this session is docs-armed, so the remainder is out of reach here: (a) runtime/user-facing brand strings (~51 files incl. web/components/BrandMark.tsx, web copy, worker/API strings); (b) CLAUDE.md charter text (excluded from docs-arming); (c) STATE.md (frozen by the R-023 arming classification); (d) ONE_LIVE_* canon FILENAMES (held — renaming ~50 files breaks every cross-reference; optional dedicated sweep); (e) infra identifiers only the founder can change (GitHub repo name "onelive", deployment URLs, Supabase ref, env-var names). Historical records (changelog/arcs/ledger/decision records) deliberately keep original text — records of the OneLive era are facts | Charter "disk is truth": brand must not diverge silently between canon and the running product | Next code-armed session (and BEFORE any user-facing deploy): sweep (a)+(b), update STATE.md when the arming classification permits, and put (d)/(e) to the founder as a one-list ask. FOUNDER DECIDED 2026-08-02 ("Leave as-is for now"): (d)/(e) stay as-is — env-var names intentionally keep ONELIVE_ (renaming is custody churn; a future consistency rename is a defect), repo/URLs/Supabase ref unchanged, filename sweep optional; NEW go-live condition bound here: production fronts the founder-held domain 1Live.co (secured via GoDaddy, confirmed 2026-08-02) — wire DNS→Vercel at the deploy session; no customer ever sees a onelive URL. EXECUTED 2026-08-02 on PR #143 (the arming closure covers only the ingest cron's import set — web/ and CLAUDE.md are outside it, so the sweep needed no re-smoke): all user-facing web strings rebranded (BrandMark, metadata titles, access gate, feed masthead, share text, event pages; web suite 185/185 green) + CLAUDE.md charter titles (file-path references intentionally kept). STILL OPEN: internal worker/tools code comments (non-user-facing, optional), founder-owned infra names, and the 1Live.co DNS→Vercel wiring at deploy. **UPDATE 2026-08-03 (Contract #33):** STATE.md is NOT frozen — the "R-023 arming classification" freeze was a stale belief (STATE.md has not been in the cron runtime set since 2026-07-24; see R-023's correction). STATE.md was reconciled this session; its title/history deliberately keep "OneLive" as append-only record while new prose uses 1Live. Brand-string cleanup in STATE.md is now unblocked whenever convenient. | OPEN — NARROWED 2026-08-04 (founder-caught stale record: the DNS leg was ALREADY DONE — decision record 2026-08-02_interaction-correction-and-confirmed-scope.md records "Go-live COMPLETE: 1live.co public, GoDaddy DNS → Vercel, SSL valid, /api/health green (#146, founder-merged)", founder re-confirmed 2026-08-04; this row and TODOS kept repeating "wiring remains" for two days and sessions echoed it to the founder). Remaining, all optional/cosmetic: internal worker/tools code comments + founder-owned infra names (deliberately kept). The DNS/go-live leg is CLOSED |

| R-076 | 2026-07-22 | (RENUMBERED 2026-08-04 from a duplicate R-023 — the sparse-delivery row keeps that id, every [R-023] code tag means it; duplicate caught by the new tests/test_record_ids_unique.py guard) Session Contract #20 (Owned Agent research, PR #48) lives in the session arc (`docs/session_arcs/2026-07-22_owned-agent-research.md`) instead of STATE.md — the arming-evidence binding (`tests/test_arming_smoke_binding.py`) classifies STATE.md as runtime surface (non-runtime set is exactly `docs/`, `tests/`, `TODOS.md`), so ANY STATE.md edit fails trust-gate until the recorded smoke-run evidence is refreshed. The gate fired correctly on this docs-only head and was not touched | Prime directive 2 / Loops discipline: "every session … ends by updating STATE.md" — the always-current rollup should carry the newest contract | Whichever fires first: (a) the next PR that refreshes `docs/evidence/ARMING_SMOKE_RUN.json` with a fresh head smoke run folds Contract #20 into STATE.md in the same (otherwise docs-only) commit, or (b) the queued gate-custody decision (TODOS: STATE.md classification in the binding's non-runtime set — evaluator-mandatory, any widening founder-crucial) lands and makes STATE.md lawfully editable, and the fold-in rides that PR. **CORRECTED + RESOLVED 2026-08-03 (Contract #33):** the premise was already FALSE. The 2026-07-24 `arming_runtime.py` refactor (Contract #20) replaced the coarse denylist with a precise import-closure classifier; STATE.md (markdown, never imported by the ingest cron) is NOT in the runtime set, so STATE.md edits have NOT failed trust-gate since 2026-07-24. Verified empirically this session (`python tools/arming_runtime.py` lists no `.md` file; a STATE.md edit does not enter the binding's diff). STATE.md is freely editable and was reconciled this session; `tools/staleness_check.py` now prevents silent STATE.md drift regardless of any classification. | RESOLVED (2026-08-03, Contract #33 — freeze belief was obsolete since 2026-07-24) |
| R-077 | 2026-07-22 | (RENUMBERED 2026-08-04 from a duplicate R-024 — the licensed_event CHECK-constraints row keeps that id; duplicate caught by tests/test_record_ids_unique.py) PR #48's local pre-push validate ran green against the pre-commit WORKING TREE while the binding test diffs run-commit..HEAD — the STATE.md violation only became visible once committed, so local validate structurally under-checks any not-yet-committed edit to binding-classified files (this session's escape-to-CI; caught by the CI gate, ledgered) | validate's purpose: "run everything" pre-PR so CI never catches what local validate could have | Next gate-custody PR touching `tools/validate` or the binding test adds a dirty-tree guard for binding-classified paths (e.g. the binding test ALSO fails when `git status` shows uncommitted changes to paths outside the non-runtime set), or documents why working-tree divergence is out of scope — whichever the evaluator upholds | OPEN |
| R-066 | 2026-08-03 | `worker/descriptor/publish.py` approves a Spark Line candidate on human identity + content-match + status-guard, but does NOT check the candidate's provenance/validation-origin (that it came through the Foundry faithfulness gate) before flipping it to `approved` (adversarial-review #148 openai/absence-only ARMING nit) | UI Canon §3/§4: a tier-C line is displayable only because it passed the validation gate — approval should verify that origin, not assume it | Before the ops approval surface OR the tier-C generation job is armed: add a provenance-origin assertion to `approve_candidate` (reject a candidate whose provenance lacks the Foundry gate stamp). Fails closed today: candidates are not public-readable, no ops route calls approve, no provider job writes candidates | OPEN |
| R-067 | 2026-08-03 | `worker/descriptor/gate.py` proves faithfulness deterministically for proper nouns and numbers, but a lower-case invented FACT that is neither (e.g. a fabricated instrument/genre claim) is caught only by the semantic judge, not the mechanical gate (adversarial-review #148 openai/absence-only ARMING nit) | Descriptor Foundry: every content word traceable to source; the mechanical gate should not depend solely on the judge for a whole class | Before the real provider / tier-C generation path is armed: extend `gate.py` with deterministic coverage for that class (e.g. content-word grounding beyond proper-noun/digit heuristics) or record why the judge is sufficient. Fails closed today: no provider job or auto-publish path is armed | OPEN |
| R-068 | 2026-08-03 | `tools/validate`'s `visual_regression` check SKIPs (loud, Record-bound) in environments lacking a Chromium binary or an installed `web/node_modules` — the check is real everywhere else (R-002's resolution): local runs execute `tools/visual_check.sh` end-to-end, and CI runs it on every web-touching PR via `.github/workflows/visual-regression.yml`, which is the authoritative venue | validate's purpose ("run everything" locally) — a browserless sandbox under-runs one check that CI still enforces | Either (a) the agent-sandbox image guarantees a Chromium + web deps so the skip branch becomes unreachable in practice, or (b) any future session observes the skip masking a real divergence that CI then catches — which escalates this to a defect requiring the check to become environment-independent. Re-affirmed at session closes while the skip branch exists | OPEN |
| R-069 | 2026-08-03 | WCAG 2.2 AA verification of `/tonight` is MACHINE-CHECKABLE-SUBSET only: `web/qa/audit.mjs` (axe-core 4.11, full wcag2a/2aa/21a/21aa/22aa tag set, self-falsifying via a planted-broken page, incl. the lens-OPEN dialog state) runs 0-violation inside `tools/visual_check.sh` locally and in CI on every web PR. What axe cannot judge remains UNVERIFIED by a human: full keyboard-only walkthrough (feed → lens → switch → detail → back), screen-reader coherence of the trust language, and label/alt meaningfulness. A clean axe run is real enforcement, NOT a certification — claiming "WCAG 2.2 AA verified" from it would overclaim | Brief/UI Canon §1.8 "WCAG 2.2 AA" (the full standard, not its automatable subset) + §0 verification bar (a check's scope stated, never overclaimed) | ANCHOR CORRECTED 2026-08-04 (founder-caught): the 1Live.co cutover ALREADY HAPPENED 2026-08-02 (go-live record; founder-confirmed), so this trigger was born fired — the human keyboard + screen-reader pass over feed/lens/detail (now BOTH themes) is due OUTRIGHT, not "before" anything; scheduling it is in the founder ask list 2026-08-04. The mechanical subset stays enforced (both schemes since PR #158) | OPEN (trigger fired — awaiting the human pass) |
| R-071 | 2026-08-03 | The live `/tonight` ships DARK theme only — `flow.css` carries no `prefers-color-scheme: light` rules — so R-002's baselines are dark-only by necessity, and the brief's "light + dark themes" is not yet satisfied by the running app (noticed while closing R-002; pre-existing, now recorded instead of silent) | Master Design Brief §4 "light + dark themes" (design canon requires both; the FLOW proposals shipped both — R-015 — but the live rebuild has not) | The founder's post-canon-execution design agenda (decision record 2026-08-02_design-direction-use-developed-canon.md: "execute that and THEN we can go from there") or any light-theme build, whichever first: ship the light theme per brief §3 ("daylight mode exists but the soul is nocturnal") and extend the visual baselines to light variants in the same PR | RESOLVED (2026-08-04, founder-directed "build the light theme as long as it was created according to our most recent world class design Ui/UX work": daylight mode shipped for the consumer surface as a prefers-color-scheme variable swap derived from the ratified FLOW palette by the R-015 light-translation precedent (warm paper, darkened AA accents — never invented); 4 light baselines committed + the pixel gate and axe now run BOTH schemes (visual_check.sh scheme pinning; audit.mjs per-scheme axe) — 0 violations incl. lens-open; every text/ground pair computed ≥4.5:1 against the COMPOUNDED ground before capture; dark baselines proven 0-diff (pure tokenization); the light axe's first run caught two real contrast defects (dim at 4.4:1 on stacked washes; the detail summary opacity washing the disputed marker) — both fixed before commit; PR #158) |
| R-070 | 2026-08-03 | CWV verification of `/tonight` is LAB-only: `web/qa/audit.mjs` enforces LCP ≤ 2000ms (the brief's sub-2s bar, stricter than the 2.5s budget) under pinned throttling (4× CPU, 1.6 Mbps/150 ms RTT) against the local production boot — measured 228–372 ms across the four audited pages, ~5–8× under budget. FIELD CWV (real users, real network, the deployed edge) is UNMEASURED: no analytics/RUM exists, and adding one is the founder-gated monitoring-stack decision (TODOS "Monitoring stack") | Brief §1 "loads in under 2 seconds" is a promise about REAL users; lab timing is a regression guard, not that proof | The founder's monitoring-stack decision lands (Vercel Analytics / Sentry tracing per R-001) → wire field CWV (LCP/INP/CLS) reporting and compare against the lab budget; until then no doc may claim field CWV is verified | OPEN |
| R-072 | 2026-08-03 | The Emotion Glyph ENGINE is built and tested (`worker/glyph/`: deterministic Plutchik→lexicon lookup, 24 coordinate glyphs + 5 sanctioned dyads, banned rating-family excluded and refusal-tested, creator override wins, provenance stamped; G-EG disclosure ratified 2026-08-03) but NOTHING RENDERS yet, deliberately: (a) the self-rendered SVG art set for the 29 glyph ids is an undesigned deliverable (ids are abstract tokens; no placeholder art, no native emoji — canon); (b) the real description→Plutchik mapper is a MODEL call (spend) — only the deterministic FakeEmotionMapper exists, used by tests; (c) no creator descriptions exist (the claim flow is unbuilt), and "no description → no glyph" is the rule | UI Canon §5 + brief appendix (the full pipeline: description → coords → curated SVG set on the card) — the engine half exists, the display half honestly does not | Three independent triggers: (a) SVG art set — the founder's design agenda commissions the ~29-glyph set (extend visual baselines when it lands); (b) real mapper — a founder-capped model-spend decision, routed via tools/model_router.py, judged by the same gate discipline; (c) creator descriptions — the claim-flow build (Owned Agent Phase A / Clerk claim work). Card wiring lands when (a)+(c) exist; until then no doc may describe the glyph as live | OPEN |
| R-073 | 2026-08-03 | (RENUMBERED from this branch's R-068 at the PR #152 merge — the parallel UI/UX session allocated R-068–R-072 first) The UI canon (`ONE_LIVE_TONIGHT_UI_CANON_v1.md` §7/§12) and two strategy docs (`ONE_LIVE_INGESTION_ENGINE_v1.md`, `ONE_LIVE_VERIFICATION_ENGINE_v1.md`) cite `ONE_LIVE_GEO_IDENTITY_v1.md §5` (the draw-to-search spec), but no such file exists anywhere in the repo — a conversation-era spec that never landed. Noticed while formalizing the draw-to-search prototype bench (decision record 2026-08-03_geolibre-draw-to-search-prototype-bench.md); the §12 row now flags the citation inline | Charter "disk is truth" / document-index discipline: a canon citation must resolve to a committed file — an unresolvable citation is chat memory wearing a filename | Before the draw-to-search gate decision or the first GeoLibre bench run (whichever first): land the Geo-Identity spec as a committed doc, or correct the three citations to the real committed source | OPEN |
| R-074 | 2026-08-03 | (RENUMBERED from this branch's R-069) `tools/session_reconcile.py --heal` rewrites STATE.md's GROUND_TRUTH block WITHOUT the `reconciled_through_commit` field when `gh` is unavailable (observed this session: heal produced a git+reconciled_at-only block), but `tools/staleness_check.py` — BLOCKING in validate — requires that field and goes INDETERMINATE (exit 2) without it. So the heal ritual in a gh-less sandbox breaks the very guard the ritual feeds; the field was restored by hand this session with an honest reconciled_by note | staleness_check's own contract: the marker is "the human-readable assertion (STATE reflects commit X)" and is sanity-checked — the healer and the guard must agree on the block schema | Next change touching `tools/session_reconcile.py` OR `tools/staleness_check.py` (whichever first): make heal PRESERVE (or re-emit) `reconciled_through_commit` — carrying the existing value forward unchanged unless the session advances it — so heal can never strip a field a blocking gate requires. Gate custody → evaluator-mandatory. **RESOLVED by the parallel session same day (master 02e0865, PR #155):** `session_reconcile.py --heal` now preserves `reconciled_through_commit` + narrative fields (preserve-what-you-don't-own; regression tests in tests/test_session_reconcile.py; red class heal-drops-guard-marker founded) | RESOLVED (2026-08-03, master 02e0865) |
| R-075 | 2026-08-03 | (RENUMBERED from this branch's R-070) The invariant rewording to gate-custodied publication (decision record 2026-08-03_invariant-wording-gate-custody.md) is codified in every LIVING doc surface (CLAUDE.md ×3, OPERATING_RULES §3.2 + narrative, review persona domain-truth-and-trust, UI canon §3, FRICTIONLESS_NAV, CONFIG_CATALOG, STATE rollup), and CLAUDE.md's parenthetical now maps the old shorthand → new wording so any session meeting the old phrase anywhere has the decoder. REMAINDER not swept here (docs-armed, records-only PR): code-side occurrences — comments/docstrings in ai/vision_provider.py, worker/vision_extract.py, worker/enrich/*, worker/descriptor/*, worker/trust_gate3.py, social/carousel/* (most already carry the correct nuance: "UNVALIDATED", "directly", "by construction"; the carousel's "AI never approves" runtime strings + matching tests remain literally true for that human-custodied surface), tools/source_pathways.py, and the make_model.py figure text | Charter "disk is truth": living text should not require the reader to know an alias — but code edits are code-armed work, and publish_gate/tests are trust-path (evaluator-mandatory in their own PR) | Next code-armed session that touches any listed file updates ITS wording in the same PR (trust-path files under evaluator review as always); the make_model.py figure line updates at the next deliverable rebuild. No mechanic changes ride along — wording only. **UPDATE 2026-08-03 (same day, founder-directed "not wait for the tripwire"):** the comment/docstring sweep EXECUTED in its own code-armed PR (14 files: vision provider/extract, enrich first_party/youtube, descriptor ×5, trust_gate3, social __init__/meta_publisher, source_pathways, test_descriptor_foundry docstring) — evaluator-reviewed on that PR; affected suites green. REMAINING, deliberately: social/carousel/publish_gate.py's two runtime refusal strings + their tests/test_social_carousel.py matchers (behavior text, literally true for the human-custodied carousel surface — change only in a dedicated behavior-text PR under evaluator review) and the make_model.py figure line (updates at the next deliverable rebuild) | OPEN (narrowed to runtime strings + figure line) |
**Adding an entry:** same commit as the deferral it records; cite the bar
section (or "n.a. — bar gap", which itself is a finding); give an objective
trigger, never "someday". **Resolving:** flip status to
`RESOLVED (<commit/PR>)`; leave the row. **Session close:** review OPEN
rows — resolve, re-affirm (trigger still pending), or escalate; a row whose
trigger has fired but wasn't acted on is a defect.
