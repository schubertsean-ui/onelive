# KAIZEN LEDGER — append-only measures (docs/KAIZEN.md)

Greppable summary: one row per merged PR (M1 rounds-to-green, M2 catches by
gate/class, M5 est. cost), plus event rows for M3 escapes (immediately),
M4 gate-gap fixes, M6 po harvests. Rows are never edited after append —
corrections get a new row referencing the old. Trends quoted in the weekly
founder digest in plain language.

## PR rows

| Date | PR | M1 rounds | M2 catches (gate: class × n) | M4 gate-gaps closed | M5 est. cost | Notes |
|---|---|---|---|---|---|---|
| 2026-07-13 | #11 | 3 | evaluator: reviewer-supply-chain ×2, silent-truncation ×1, coverage-gap ×2, SCA-gap ×1; CI: empty-env ×1 | trusted-base-ref bootstrap; no-path-filter; blocking SCA gate | ~3 evaluator calls + CI | gate armed itself; bootstrap merged red under human review (documented exception) |
| 2026-07-13 | #12 | 2 | evaluator: fail-open budget ×2, shell-injection-surface ×1, empty-env ×1, audit-trail ×1 | fail-closed caps at every layer; env-only workflow inputs | ~2 evaluator calls + CI | |
| 2026-07-13 | #13 | 1 | — | — | ~1 evaluator call | communication rules (docs-only) |
| 2026-07-14 | #14 | 5 | evaluator r1: trust-invariant-not-enforced ×2, enforcement-not-wired ×2, silent-deferral ×3; r2: fail-open-provisional ×2, invariant-not-at-entry-point ×1, overstated-record ×1; r3: swallowed-error(CI) ×2, fail-open-empty-env ×2; r4: unclosable-config-channel ×1 | non-Claude-evaluator invariant (router + reviewer entry point); OPEN-row-only Record enforcement + SQL/block-comment scanning; fail-loud router steps; CI override channel removed | ~5 evaluator calls + CI | repeat class across r3–r4: empty-env fail-open (same class as #11/#12) — see class note below |
| 2026-07-14 | — (session) | — | deferral_scan(SQL pass): pre-existing silent deferral in 0006 RLS comment ×1 (already resolved by 0007; retro-recorded R-011) | SQL + block comments added to scanner | ~0 | M4 example: a widened gate immediately caught a real latent item |
| 2026-07-14 | #18 (in review) | 2 (r1 red → fixes pushed) | evaluator r1: overstated-provenance ×2 (research doc claimed primary-source-derived pricing + "cites inline" when figures were search-index reads), unauditable-verification-artifact ×1 (journal in transcript, not committed), stale-cross-reference ×1 (contract #4 vs #5 after parallel-session renumber); nits: performative-metric, overstrong-"verified", provisional-license-in-ranked-bucket ×3 | verification journal now a committed artifact; per-subsection source-page lists | ~1 evaluator call | new class: **overstated-provenance in research docs** — watch for repeat; gate caught founder-facing spend/licensing claims presented stronger than their evidence |
| 2026-07-14 | #18 (in review, r2) | 3 (r2 red → fixes pushed) | evaluator r2: fail-open-reuse-rights ×1 (EDGAR company exhibits mislabeled US-gov work), overstated-provenance ×2 (section headers still said "primary-source fetch"; RTPR license simultaneously known-hostile and "unread"), fail-open-licensing-bucket ×1 (provisional France license in the cleared bucket), completion-overclaim ×1 (contract marked COMPLETE with pricing sub-scope deferred); nits: newline, "fetched"→"consulted", in-cell "reported" prefixes, per-vote records x4 | per-vote verifier records committed (75 votes); STATE completion claim narrowed to "delivered, sub-scope OPEN under R-013" | ~1 evaluator call | overstated-provenance repeat within same PR (r1→r2) — the r1 fix patched the intro but not headers/cells; lesson: fix a provenance class doc-wide, not at the flagged line only |
| 2026-07-14 | #18 (in review, r3) | 4 (r3 red → fixes pushed) | evaluator r3: fail-open-reuse-rights-repeat ×1 ("EDGAR (public domain)" survived the r2 sweep in §3.2), promotion-of-provisional-to-plan ×1 (§10 "underwritten by verified findings" over provisional items), overstated-provenance-repeat ×1 (closing note called secondary sources primary), completion-overclaim-repeat ×1 (STATE "done-criteria met"), invalid-audit-artifact ×1 (# comments broke JSONL) | tests/test_research_artifacts.py added — suite now parses every committed docs/research/*.json(l) (M4: the gate the evaluator asked for); STATE contract #5 reworded to PARTIALLY DELIVERED with explicit founder paths-to-closure | ~1 evaluator call | overstated-provenance now 3 rounds running — grep-audit the whole doc for strong-verification language BEFORE pushing, not after the evaluator finds the next instance |
| 2026-07-14 | #18 (in review, r4) | — | test_audit (trust-gate CI): cannot-fail-test ×2 — the r3 gate-gap fix itself shipped assert-less parametrized tests; the repo's own false-confidence auditor caught the new gate being toothless | asserts added (top-level type, error accumulation, non-empty audit trail) | ~0 (CI only) | meta-catch: a gate added to satisfy an evaluator nit was itself gated — the harness's layered defenses working as designed |
| 2026-07-14 | #18 (in review, r5) | 5 (r5 red → deliverables landed) | evaluator r5: silent-open-contract ×1 (Contract #6 recorded with no deliverables in diff — resolved by committing them this round); nits: jsonl-lines-must-be-objects ×1, hard-coded-artifact-list ×1, "0 unverified" scoping x1 | JSONL test asserts object lines; artifact-reference check made dynamic over top-level research docs | ~1 evaluator call | the blocker was sequencing (contract landed a push before its deliverables), not overstatement — first round with no provenance-class finding |
| 2026-07-14 | #18 (in review, r6-r7) | 7 | r6: gate hard-fail on 405KB diff > --max-diff-bytes (fail-closed working as designed); my exclusion fix was then REJECTED by r7 as evidence-hiding ×2 (cited appendices + vote records removed from reviewed diff), false-confidence-gate ×2 (md appendices claimed "parse-validated" but untested; reference check skipped them); nits: "decision-grade"→directional, absolute market claims→"no occupant found in this pass" ×3 | resolution reversed to the script's other sanctioned path: --max-diff-bytes raised to 800000 VISIBLY with in-workflow rationale, full diff reviewed, zero research exclusions; new test validates cited md appendices exist/non-empty/letter-mapped | ~2 evaluator calls | class lesson: "shown, never hidden" applies to OUR OWN evidence — never fix a size gate by narrowing what the reviewer sees |
| 2026-07-14 | #18 (in review, r8) | 8 | evaluator r8: hallucinated-evidence ×1 — appendix C cited an MCP release-candidate blog post publication-dated TWO WEEKS AFTER compilation (time-incoherent; caught only BECAUSE r7 forced the full appendix diff under review) | citation struck with visible editor correction (audit trail preserved); new blocking test: no ISO date after the appendix's Compiled date; blast-radius grep confirmed no analysis claim relied on it | ~1 evaluator call | new class: **hallucinated/time-incoherent citation in agent research** — the r7 reversal (review everything) paid for itself one round later; date-sanity now mechanical |
| 2026-07-14 | #18 (in review, r9) | 9 | evaluator r9: evidence-redaction ×2 (my r8 'strike' REPLACED the bad citation's title/date/URL with placeholders — hiding disputed evidence instead of showing it with a verdict; appendix header + analysis §15 still claimed 'verbatim' after the edit = false provenance), false-confidence-gate ×1 (date test bypassable by exactly that blanking pattern); nits: typed jsonl records, basename collisions, po raw output x3 | refuted row restored VERBATIM with additive REFUTED verdict; headers state the additive-corrections policy; date test now requires REFUTED lines to retain the original URL (verdict-without-evidence = failure); votes.jsonl records typed meta/vote; po raw run committed | ~1 evaluator call | class lesson deepened: disputed-shown-never-hidden means corrections are ADDITIVE — a verdict replaces nothing, it attaches to preserved evidence. This is the OneLive trust invariant applied to our own research |
| 2026-07-14 | #18 (in review, r10) | 10 | evaluator r10: stale-provenance-claim ×2 (STATE + changelog still said "verbatim appendices" after C gained its additive verdict — the r9 fix updated the artifact and analysis but not the ground-truth ledgers), **wrong-arithmetic ×1 (IR pool: "S&P-500-class × $10–25k = $100M+" — 500×$25k is $12.5M; a plainly false founder-facing TAM line)**; nits: URL-publication-date gate, cited-.txt existence, po-artifact scope x3 | STATE/changelog corrected with visible correction note; pool recomputed honestly (4–5k US-listed × $10–25k ≈ $50–125M + observed Meltwater/Cision revenue as the reality check); date gate extended to URL-slug dates; .txt artifacts covered | ~1 evaluator call | two lessons: (1) when an artifact's provenance changes, grep EVERY ledger that describes it; (2) arithmetic in founder-facing sizing gets recomputed by hand before commit — a directional caveat does not excuse a wrong multiplication |
| 2026-07-14 | #18 (in review, r11) | 11 | evaluator r11: stale-provenance-claim-repeat ×1 (the analysis doc's own header paragraph still said "committed verbatim" — the r10 grep-sweep covered STATE/changelog but skipped the doc that started the sweep); nits: changelog round-count stale x1, date-gate name broader than implementation x1 | header fixed via full-repo verbatim grep this time; changelog defers to ledger as authoritative count; test renamed test_no_publication_style_date_after_compilation with explicit scope docstring | ~1 evaluator call | the r10 lesson executed incompletely — "grep EVERY ledger" includes the deliverable itself; sweep checklists must enumerate ALL files touched by the changed claim |
| 2026-07-14 | #18 (in review, r12) | 12 | evaluator r12: **wrong-arithmetic-repeat ×1 — the r10 "fix" itself misstated the lower bound ($50–125M; correct is $40–125M since 4,000 × $10k = $40M), and the error propagated into the r10 ledger row and the changelog** (this row corrects the r10 row per the append-only convention — the r10 row is NOT edited) | analysis/changelog corrected to $40–125M; NEW MECHANICAL GATE: test_sizing_arithmetic_is_internally_consistent recomputes every "count × $price ≈ $range" claim in founder-facing research docs | ~1 evaluator call | wrong-arithmetic is now a REPEAT class (r10, r12 — the fix itself failed). Per the class-watch escalation pattern: the response is now mechanical (a recomputing test), not another hand-check promise |
| 2026-07-14 | #18 (in review, r13) | 13 | evaluator r13: unsupported-superlative ×1 ("fastest-growing slice" — evidence shows fast growth, not a ranking of every slice), **po-coverage-overclaim ×1 ("full canon" asserted while only condensed notes were committed — the exact partial-coverage-in-full-coverage-clothes class PR #15 established)**; nits: prose-date convention, absence-claim hedge, float tolerance x3 | complete per-provocation working notes committed (PO_BATTERY_WORKING_NOTES_20260714.md — every operator P1–P8.6, ≥2 named movement techniques per provocation); superlative reworded to what evidence supports; ISO-publication-date convention documented in the gate | ~1 evaluator call | the po-coverage rule generalizes: any claim of full coverage must point at a committed artifact exhibiting that coverage — narrative summaries are views, never the record |
| 2026-07-14 | #18 (in review, r14) | 14 | evaluator r14: bypassable-trust-gate ×1 (the r9 date gate's "see row" escape accepted commentary lines without validating the referenced evidence still existed — a redaction could pass CI), stale-remediation-claim ×1 (the r9 ledger row recorded "requires REFUTED lines to retain the original URL" — stronger than the code enforced; this row corrects the r9 row per append-only convention) | gate reimplemented with cross-reference resolution: commentary future-dates must be a subset of dates on preserved-evidence lines (REFUTED + original URL) in the same file; negative test executed — gate verified RED when the evidence URL is redacted | ~1 evaluator call | remediation rows must describe the code as it IS, and trust gates get a demonstrated failure case before their ledger row claims them |
| 2026-07-14 | #18 (in review, r15) | 15 | evaluator r15: bypassable-trust-gate ×2 (any-URL substitution passed the evidence gate; prose future publication dates uncovered), false-confidence-gate ×1 (arithmetic gate missed point-estimate form — the historical failure's own shape); nits: no negative fixtures, caveat proximity, appendix header wording x3 | gates REDESIGNED: pinned REFUTED_CITATIONS.json registry (original url+date must remain verbatim — substitution and redaction both fail), prose day-date coverage with an explicit reviewable event-word allowlist, arithmetic point+range+B-unit forms, and 9 committed negative fixtures proving each gate fails on its target failure shape (incl. the two historical arithmetic errors verbatim). Stated limitation, not hidden: non-numeric counts ("S&P-500-class") remain evaluator/editor territory | ~1 evaluator call | trust gates are now fixture-tested against their own failure modes — the "demonstrated failure case" rule from r14 made mechanical |
| 2026-07-14 | #18 (in review, r17 — Contract #7 groundwork) | 17 | evaluator r17: false-capability ×2 (EDGAR function named press-release extraction but only listed 8-K primary docs — exhibits live in the per-filing index; its test fixture encoded the same wrong contract), fail-open-interchange-schema ×1 (JSON Schema lacked the validator's identifier/conditional/time invariants), false-confidence-lockstep-test ×1 (compared field names only), vacuous-match hole x2 (kind-only predictions could match; committed labels demonstrated it); nits: 429 fail-closed, $id namespace, arithmetic-gap trigger, caveat-proximity ×4 | two-stage EDGAR design (list filings → index.json EX-99 candidates with confidence capped at "likely", limitation stated); JSON Schema now mirrors validator invariants with the inexpressible one declared in x-invariants + tested; discriminative-match-keys rule enforced at load AND match time with negative tests; class-watch row added for the arithmetic gap | ~1 evaluator call | groundwork code gets the same bar as research prose: a function's NAME is a claim, and a test that encodes the wrong contract is worse than no test |
| 2026-07-14 | #18 (in review, r18) | 18 | evaluator r18: silent-recall-loss ×1 (EX-99 regex missed EDGAR's filing-agent generated names like d123456dex991.htm — real press releases silently dropped while friendly-name fixtures passed); nits: over-broad "from " in the date-gate event allowlist, empty-string match keys still "discriminative", implicit urllib.error import, LifecycleEvent schema unexported x4 | regex covers the digit+dex99 shape with a named regression test; allowlist tightened (existing corpus re-verified green); empty strings refused at load and at match; urllib.error imported explicitly; LifecycleEvent interchange schema exported with the verdicts-require-evidence conditional + test | ~1 evaluator call | fixture realism matters: test filenames must come from what EDGAR actually generates, not what a human would name a file |
| 2026-07-15 | #18 (in review, r19) | 19 | evaluator r19: undeclared-cross-field-invariant ×1 (target_low<=target_high enforced by the validator but absent from the schema's x-invariants after the diff claimed inexpressible invariants are declared), non-evidence-as-evidence ×1 (LifecycleEvent schema accepted [{}] for verdict evidence — an accusation could validate with no source/timestamps; the test checked only minItems); nits: whitespace metric, duplicate example_ids, x@y email x3 | evidence items are now full Provenance records in both the base property and the verdict conditional (fixture-tested against the empty-object shape); both cross-field invariants declared in x-invariants with tests; validator/schema parity restored on metric; duplicate ids refused; contact-email shape tightened | ~1 evaluator call | the r15 rule generalizes to interchange schemas: an exported schema is a trust gate for CONSUMERS — its weakest accepted document, not its happy path, is what it promises |
| 2026-07-15 | #18 (in review, r20) | 20 | evaluator r20: silent-recall-loss-repeat ×1 (PDF EX-99 exhibits dropped — same class as r18's filename miss), fail-open-empty-string ×2 (DATED_EVENT with due_date_text="" validated; parsed due_date could keep an empty phrase — both break the promise-maturity/provenance-of-parse invariants), unsourced-ledger-mutation ×1 (WITHDRAWN/MODIFIED/REITERATED/EXPIRED_UNRESOLVED validated with zero evidence); nits: pdf fixture, per-process budget scope guard, unit required x3 | .pdf accepted with regression fixture; empty/whitespace due_date_text rejected both ways; EVERY lifecycle event now requires ≥1 provenance-typed evidence item (validator + schema + tests, incl. the expired_unresolved cite-the-original rule); unit mandatory for numeric guidance (validator + schema); single-process budget guard stated loudly | ~1 evaluator call | two classes reinforced: recall-loss hides in file-format assumptions, and "sourced assertion" applies to every ledger event, not only the accusations |
| 2026-07-15 | #18 (in review, r21) | 21 | evaluator r21: fail-open-interchange-schema ×4 (present-but-null lei/cik satisfied the anyOf; minLength:1 accepted whitespace where the validator strips — claim_id/name/statement/source_kind/metric/unit; empty due_date_text passed the dated_event conditional — the r20 class reopened at the schema layer), trust-gate-bypass ×1 (bare "by " in EVENT_CONTEXT exempted citation prose like "post by Vendor, Jul 28, 2026") | schema parity closed with a shared non-blank pattern (\S) and null-proof identifier branches; EVENT_CONTEXT tightened to "by <Month>" deadline phrasing with both negative fixtures; NEW: minimal internal JSON Schema checker in tests — 10 data-driven parity fixtures now prove the exported schema rejects what the Python validator rejects, against the schema itself rather than its structure | ~1 evaluator call | parity is a property of DOCUMENTS, not structure: assert what the schema accepts/rejects, not what keys it contains — the mini-checker makes every future parity claim mechanically testable |
| 2026-07-15 | #18 (CI, post-Contract-#9 push) | — | adversarial-review CI: fail-closed diff cap fired x1 — committing the two real EX-99.1 documents pushed the reviewed diff to 899,793 bytes > the 800,000 cap; the gate HARD-FAILED rather than truncating (working as designed, same shape as r6) | structural fix, not another cap raise (the corpus targets ≥20 filings — no fixed cap survives that): machine-fetched THIRD-PARTY documents in eval/source_material/ are now excluded from the review diff under the lockfile precedent, with MANIFEST.json (our provenance claim) kept in-diff and NEW manifest-integrity tests hashing every excluded file against its manifest sha256 in CI — unvouched or drifted files fail the build; exclusion rationale + its distinction from the r6-r7 own-evidence ruling stated in-workflow for the evaluator to attack | ~0 (CI only) | boundary drawn: OUR evidence (appendices, vote records, manifests) is always in-diff; external inputs we merely fetched are validated by hash, not re-read by the reviewer — the evaluator rules on this policy next round |
| 2026-07-15 | #18 (Contract #9 — first LIVE EDGAR run) | — | live-run gate: silent-recall-loss THIRD occurrence x1 — real same-day filenames `a2q26erfexhibit991narrative.htm` (letters before "exhibit991") and `bac06302026ex991.htm` (digits before "ex991") were missed by the EX-99 fallback regex that had survived 21 synthetic-fixture review rounds (r18 caught dex991, r20 caught PDFs; reality caught two more shapes on day one) | root-cause fix, not another regex patch: authoritative exhibit typing now parses the filing's `-index-headers.html` SGML TYPE/FILENAME table (EDGAR's own typing — filename-shape-independent); fallback regex broadened and demoted to fallback-only; REAL fixtures committed (`tests/fixtures/edgar/`, unaltered live excerpts) with regression tests proving the old pattern's misses | 1 budgeted live run (~6 requests, declared UA, ≤5 req/s) | class lesson sealed: filename-shape recall was a REPEAT class (r18/r20/live) because every fix guessed at shapes — the fix that ends the class reads the authority's own metadata instead of guessing. Live runs against reality are a gate tier synthetic fixtures cannot substitute for; note the `<TYPE>/<FILENAME>` pairs are HTML-escaped inside `<PRE>` (first parse returned [] until unescaped) |
| 2026-07-15 | #18 (in review, r22) | 22 | evaluator r22 (first review of the full build-phase diff; the fetched-corpus diff-exclusion policy was ACCEPTED): **point-in-time-violation ×2** (store keyed lifecycle as-of reads on `observed_at` — late-discovered outcomes time-traveled into backtests; structural: LifecycleEvent had no known-at timestamp at all), stub-record-type ×1 (`source_retrieved` advertised with no public writer/contract/test), **false-confidence-fiscal-guess ×2** (parser emitted calendar dates for issuer-fiscal periods AND the test suite locked the guess in as expected behavior — a wrong trust contract enforced by CI), stale-cross-reference ×8 (the parallel session's contract renumbering #5–#9→#14–#18 never propagated: changelog headings, TODOS, README "GROUNDWORK ONLY"/"not yet run live", edgar.py status banner, sprint steps 4/9 still BLOCKED after their gates cleared); nits: exhibit fn name too strong, grep-based append-only test, caveat proximity x3 | LifecycleEvent gained required `recorded_at` (validator: tz-aware, ≥ evidence retrieved_at; schema + x-invariants + parity fixture); store as-of keys on recorded_at with a late-discovery regression test; `record_source_retrieval()` writer with door validation + happy/negative tests; fiscal phrases now return due_date=None (never guessed; locking test replaced by negative tests); `find_ex99_exhibits_authoritative` rename; repo-wide renumber + status sweep | ~1 evaluator call | two lessons: (1) the product's own core invariant (as-of-known-when) must be applied to the product's code with the same rigor as to our research prose — "two timestamps" means two FIELDS, not a column name; (2) merging a parallel session's reconciliation imports its renumbering as YOUR stale-reference debt — sweep every cross-reference the moment the merge lands. NOTE (append-only correction): the two 2026-07-15 rows above say "Contract #9"; per the merge reconciliation that contract is #18 |
| 2026-07-14 | — (session) | — | external-review(Weco RSI post): charter-prose gap ×1 — gate-tooling changes not named in the evaluator-MANDATORY list; no founder interrupt on gate relaxations | gate custody added to charter; threshold relaxations now founder-crucial (CI enforcement predates the prose — adversarial-review.yml was already path-filterless per #11) | ~0 | class: gate-custody-prose-gap; caught by founder-supplied external article review, founder approved amendment same day |
| 2026-07-15 | — (first real run) | — | pipeline fail-loud: stale extraction model id (claude-3-5-sonnet-latest, 404) ×1 — class: stale-model-config; log review: run reported SUCCESS + dead-man pinged healthy despite 3/3 sources erroring ×1 — class: fail-open (silent-degradation) | model id → routed tier + P3 liveness check queued; TotalRunFailure raises so dead-man pings /fail on zero-work runs | ~$0 (calls 404'd) | infrastructure itself green first try: DB (266 sources), caps, splice, replay, pings |
| 2026-07-15 | #21 | 3 | evaluator r1: invariant-not-at-entry-point ×1 (provider bypassed router gate — repeat class from #14), fail-open explicit-param ×1, ungated-model-change ×1 (my own KAIZEN §M7 rule enforced against me) | provider wired through router (tools now a package, single-sourced id); blank explicit model fails closed; model change WITHDRAWN until the Step 6 golden gate exists (R-013) | — | the reviewer enforcing the ratchet doc within hours of it merging |
| 2026-07-14 | #16 | 1 | — | — | ~1 evaluator call | harness-review adoptables (docs-only) |
| 2026-07-14 | #17 | 2 | evaluator r1: dispute-collapse-in-candidate-idea ×1, link-possession-elevation ×1 (+3 nits: DMARC alignment, fast-lane contradiction check, hibernation reachability) | disputes never collapsed; magic links only initiate verified claim flows | ~2 evaluator calls | sensor canon |
| 2026-07-14 | #19 | 3 | evaluator: GITHUB_ENV secret-scope regression ×1, add-mask escaping hole ×1, scope-without-masking ×1, untruthful-record ×1 | credential scoped to one step AND escaped-masked; assembly in tested script; no-secret-in-errors invariant | ~3 evaluator calls | DSN founder-ergonomics |
| 2026-07-15 | #22 | 1 | — | — | ~1 evaluator call | measured-unit definition (docs-only) |
| 2026-07-15 | R-006 ratified | — | — | the 1% bar + one-way ratchet codified (KAIZEN §M7); extraction remains BLOCKED under R-013 until the golden-set gate ships and the starting model passes (Step 6) | — | founder: "I'm ok to BEGIN at 1%" — the NUMBER is ratified; the gate that proves it is next |
| 2026-07-16 | #18 (in review, r23) | 23 | evaluator r23: **gate-threshold-without-founder-record ×1** (the r6–r7 --max-diff-bytes raise 300000→800000 predates the 2026-07-14 gate-custody charter amendment but carries no recorded founder approval — escalated to the founder, **founder-approved 2026-07-17** (docs/memory/decisions/2026-07-17_diff-cap-approval.md)), knowability-ordering ×2 (a lifecycle event could be recorded with a knowledge horizon EARLIER than its claim's — events_as_of could show a verdict before the claim it judges; supersedes accepted any existing seq — cross-claim/cross-type/backdated corrections all possible), stale-design-contract ×1 (LEDGER_STORAGE_DESIGN.md still described the pre-r22 timestamp model that caused the time-travel bug), overstated-timing ×1 (STATE said JPM/BAC filed "TODAY"; they filed 2026-07-14, retrieved 2026-07-15), stale-cross-reference ×3 (research-doc headers + M6 ledger row still cited orig contract numbers after the renumber sweep), stale-decision-banner ×1 (sector memo still said "no decision taken here"); nits: manifest newline, extension-agnostic manifest tests, fallback-regex breadth, grep-based SQL guard, hash-vs-authority provenance job x5 | store now refuses a lifecycle event knowable before its claim AND constrains corrections to same-type/same-claim/never-earlier-horizon (negative tests for each); design doc rewritten to the observed_at/recorded_at model; STATE timing corrected visibly; research headers now cite renumbered contracts; sector memo carries the decision record; manifest tests extension-agnostic | ~1 evaluator call | the renumber-sweep lesson (r22) applied incompletely AGAIN — headers of the deliverables themselves were missed; a sweep checklist must grep the ORIGINAL number, not just known ledger files. Gate-custody finding escalated, not self-resolved: cap changes are founder-crucial |
| 2026-07-14 | #15 | 2 | evaluator r1: fail-open coverage knob ×1, partial-coverage-as-full ×1 (battery trimmable while claiming "run EVERY prompt") | combos knob removed; tests fail if any P8.x missing | ~2 evaluator calls | row added 2026-07-15 — see record-missing note in class watch |
| 2026-07-15 | #23 | 1 | — | — | ~1 evaluator call | session-close arc (docs-only); row added 2026-07-15 |
| 2026-07-15 | #24 | 4 | evaluator: ratification-path governance hole ×1 (founder acceptance is the ONLY ratification path for founder-facing decisions), untruthful-record ×1 (ledger/changelog described intended state as existing), + structural nits | ratification-path rule written into the display-stack canon; records describe what EXISTS | ~4 evaluator calls | certainty display stack; row added 2026-07-15 |
| 2026-07-15 | #25 (in flight) | 5+ | evaluator r1–r4: exam-channel confinement (grep→runtime) ×2, None-swallowed-as-empty ×1, mismatch-display-only ×1, self-contradicting keys ×3, + nits; r5: wrapper-hole confinement ×2 (runtime + static layers), evidence-channel visibility ×2, sample-floor semantics ×1, secret-on-PR posture ×1 (→ R-014); exam cycles 1–4: prompt–key contradictions ×7 (title conventions), key incoherence ×3 (g004 vs g037/g066; g044), prompt–exam contamination ×1 (golden key strings cited as prompt examples) | two-layer golden_exam confinement; validity = property of the set; key-change log with justifications (ai/golden/README.md); prompt examples must be invented, never golden strings | 5 evaluator calls + 4 real exams (~$2 total) | final row at merge |
| 2026-07-17 | #25 (merge-coordination land) | 1 | evaluator: gate-truthiness-not-exact-boolean ×1 (`resolve_model` still used `not FLAG` after the r26 provider fix hardened only the provider read — REPEAT CLASS of r26, one production layer missed) | router extraction gate → exact `EXTRACTION_THRESHOLD_RATIFIED is not True`; repo-wide derived-completeness grep confirms BOTH production reads (provider + router) are now exact-boolean; router-level regression test asserts truthy non-bool ("False"/1/"yes") keeps extraction closed | ~1 evaluator call | in-flight repeat-class rule applied: fixed the CLASS by deriving the complete set of flag-reads (2), not just the flagged instance |
| 2026-07-16 | #27 | 4 | evaluator r1: gate-custody-ambiguity ×1 (sequential mode could look like it discharges Black custody), unverifiable-validation-claim ×1; r2: record-precision ×1 (pytest count didn't match CI log — same class as #20 r18's history-must-match-suite); r3: finding-laundering-path ×1 (generator-family Blue merge allowed on founder-crucial runs), exam-custody ×2 (lens-owned golden-set rows in TODOS + white.md) | fail-closed Blue-merge family constraint; exam custody never lens-owned; raw-outputs-preserved rule for manual hat runs | ~4 evaluator calls + CI | dedicated-hat registry (docs/hats/) — notable: the evaluator enforced custody language in the document that DEFINES custody; the M6 row for the design's po battery merged in the same PR; founder merged ("You merge it"), squash 2c1ef94 |
| 2026-07-18 | — (session, PR #35 in flight) | — | founder(Red): skip-report-missing-record-citation ×1 (visual_regression SKIP reported without citing R-002); evaluator r1 (f654c0f): gate-evidence-missing ×1, unverifiable-ci-claim ×1 (REPEAT CLASS — #20 r18 / #27 r1–r2 family, third PR); evaluator r2 (c384bf2): deferred-class-fix ×1 (queueing the mechanization after the repeat-class threshold was itself the violation — "a prose rule is not a failing gate") | SHIPPED same PR after r2: tools/skip_record_binding.py (fail-closed, 9 tests) + validate skip→Record binding (unrecorded skip = RED even under --allow-skips) + machine-stamped evidence block (stdout + .validate-evidence.txt) + adversarial-review.yml attaches validate.log to the evaluator — evidence generated, never hand-copied; SESSION_START prose now points at the mechanism | ~3 evaluator calls | class watch: unverifiable-claim family's structural fix now exists — a fourth catch of this class is an escape of the new gate, not another instance. Final M1 row at merge |
| 2026-07-18 | — (marker backfill) | — | no new catches — this correction row adds machine-readable M4 class markers for fixes that had ALREADY shipped, per the new docs/KAIZEN.md trend-instrumentation convention (rows are never edited; corrections append) | silent-deferral → deferral_scan SQL/block-comment widening (shipped, #14 M4); keys + contradictions + incoherence (the #25 exam-cycle prompt–key classes, prose names truncated by the token convention) → key-change log + validity-as-property-of-the-set + two-layer confinement (shipped, #25 M4) | — | `empty-env` deliberately NOT marked: its structural fix (env-contract workflow linter) is unshipped — the kaizen_trends alarm stays red-loud until it lands (R-019, TODOS P2) |
| 2026-07-18 | — (session, PR #35 in flight, cont.) | — | evaluator r3 (b9d3c99): fail-open-quick-exemption ×1 (note-substring exemption bypassable), fail-open-loose-binding ×1 (raw substring could bind unrelated Record rows), stale-evidence ×1 (arc carried a dirty-worktree block for a superseded head — repeat-adjacent to unverifiable-claim) | QSKIP structured status (exemption by state, never note text) + backticked-marker binding (incidental mentions never bind) + binding loop extracted to sourced tested file (5 loop tests) + arc evidence replaced by CI-artifact pointer; kaizen_trends meter SHIPPED (mechanizes the repeat-class + escapes-zero + trend-direction rules — trends computed, never asserted, per founder direction 2026-07-18) | ~1 evaluator call | the meter's first real run immediately caught R-019 (empty-env) and the marker-convention gap — instrumentation paying for itself on day one |
| 2026-07-18 | — (session, PR #35 in flight, r4) | — | evaluator r4 (7442f9a): self-firing-trigger ×1 (R-019's "next workflows-touching PR" trigger fired IN the PR that wrote it — the r2 deferral failure mode, subtler form, caught third time running), short-token-alarm-gap ×1 (class regex length floor let `sql`-style tokens escape counting), substring-fix-credit ×1 (M4 credit by raw substring — the r3 loose-binding pattern, meter edition) | empty-env CLASS FIX SHIPPED: tools/workflow_env_lint.py (R1 visible-source for every consumed env var, R2 vars. context ban, blocking in validate, 13 tests, 7/7 live workflows clean) — R-019 RESOLVED same day; meter hardened: no length floor on class tokens, exact-token M4 credit, single-segment tokens never absorb compounds | ~1 evaluator call | deferral-refusal is now 3-for-3 (r2, r4×2): the lesson is structural — never write a trigger your own diff fires; the alarm this row clears was created and resolved inside one PR |
| 2026-07-18 | — (session, PR #35 in flight, r4-fix push) | — | self(validate): pushed-on-red ×1 (commit cb87a84 pushed while validate was RED — the agent's shell chained `git commit && git push` off a `sed` pipeline that swallowed validate's exit code; the red itself was deferral_scan correctly flagging untagged deferral language in a test comment, plus its mirror pytest failure) | comment reworded (not a deferral — resilience description); process rule for the agent: NEVER chain commit/push off a pipeline containing validate — run validate bare, check its exit, then commit (candidate mechanization: a pre-push hook running validate --allow-skips, noted in AGENT_FEEDBACK) | ~0 | caught by the very next validate read, fixed in the immediately following push; CI red on cb87a84 is this defect's public trace |
| 2026-07-18 | — (session, PR #35 in flight, r5) | — | evaluator r5 (bd80bb9): ambient-token-fail-open ×1 (GITHUB_TOKEN wrongly allowlisted — Actions gives shell no $GITHUB_TOKEN by default), stripped-expression-blindness ×1 (${{ secrets./env./github.token }} directly in run: renders empty silently, invisible to the linter), order-blind-local-credit ×1 (assignment anywhere credited uses before it), comment-blind-export-credit ×1 (commented-out GITHUB_ENV export credited later consumers), waivable-hard-finding ×1 (kaizen_trends as advisory let --allow-skips mask an M3 escape or repeat-class alarm in the CI evidence run) | linter rewritten as comment-aware position-aware line-order scanner (defs credit only at-or-before use; comment lines credit nothing); R3 added: secrets./env./github.token expressions banned inside run: (pass via env:); GITHUB_TOKEN dropped from ambient; kaizen_trends promoted to run_check — findings RED, never waivable; 8 bypass regression tests (21 linter tests total) | ~1 evaluator call | all five are the same meta-class: loose-matching fail-open (r3 skip-binding, r4 M4-credit, r5 ×4) — the evaluator is systematically red-teaming every matcher I write; conditional-branch export credit remains as documented false-negative limit with runtime [ -n ] checks as the guard |
| 2026-07-18 | — (session, PR #35 in flight, r6) | — | evaluator r6 (f161db3): declared-but-empty-secret ×1 (env declaration accepted as sufficient source — a missing secret renders empty and flows silently), false-confidence-test ×1 (the test suite codified that fail-open as the clean case), prefix-assignment-credit ×1 (TOKEN=abc cmd "$TOKEN" credited though the shell expands the PRIOR value), permanent-fix-waiver ×2 (an M4 marker suppressed the family's alarm FOREVER — post-fix recurrences would never fire) | R4 SECRET-GUARD shipped: secret-backed step-env vars consumed in shell text require a visible non-empty guard ([ -n ]/${X:?}) at-or-before first use — 4 real unguarded $GH_TOKEN sites found and guarded in the live exam workflows; same-line def credit now requires a command separator (shell-semantic, not position-only); meter markers are EPOCH-scoped: any family catch after its last M4 marker row alarms immediately as "fix escaped" (recurrence threshold 1, not 3) | ~1 evaluator call | the linter's stricter rule catching 4 live workflow sites in the same push that added the rule = the gate paying for itself before it ever reached master |
| 2026-07-18 | — (session, PR #35 in flight, r7) | — | evaluator r7 (bdbd2a9): inverse-guard-accepted ×1 ([ -z ] credited as a guard — it SUCCEEDS when empty), line-wide-guard-credit ×1 (use-then-guard on one line passed), scope-limited-secret-detection ×1 (workflow/job-level secret env escaped R4), short-name-blindness ×1 ($DB-style 2-char names invisible to R1) | guard regex accepts only -n forms + ${X:?} (never -z); within-line guard positions enforced (guard covers only later uses); secret-backed detection at ALL env scopes; name floor lowered to 2 chars across every regex; live -z-idiom guards in ingest.yml + exam-dispatch converted to the equivalent -n form (same semantics, now machine-verifiable); 7 r7 bypass regression tests (32 linter tests) | ~1 evaluator call | golden-exam check red-by-design on this PR (touches exam workflow files — the verifier refuses to certify its own harness, documented bootstrap pattern; merge path = adversarial review under the recorded exception) |
| 2026-07-18 | — (session, PR #35 in flight, r8) | — | evaluator r8 (3c32a99): non-terminating-probe-as-guard ×1 (`[ -n ] || true` and bare if-then-fi probes satisfied R4 while an empty secret still flowed into later commands), missing-termination-regressions ×1 (suite proved guard SYNTAX, not the abort semantics) | guards must TERMINATE: ${X:?} always; a -n test counts only with exit/return on the SAME line; probe mentions are not consuming uses (so aggregate-precondition idioms stay legal); 5 termination regressions (37 linter tests, 466 suite); RESULT stdout token → INCOMPLETE-ACKNOWLEDGED; trend report now states pre-convention counts are floors, not history | ~1 evaluator call | seventh consecutive round of the loose-matching meta-class being tightened — each round narrows the gap between what the rule SAYS and what the shell DOES |
| 2026-07-18 | — (session, PR #35 in flight, r9) | — | evaluator r9 (ba52685): wrong-branch-abort ×1 (`[ -n ] && exit` / if-then-exit-fi credited — they terminate when the secret is PRESENT), quoted-text-as-assignment ×1 (`echo "ok; MY_TOKEN=abc"` could credit a definition through a quoted separator), inline-comment-export-credit ×1 (an export pattern inside a trailing `# ...` comment credited later consumers) | small shell lexer added (_line_views): quote/comment-aware with positions preserved — inline unquoted comments blanked for ALL scanning, single-quoted spans blanked (nothing expands there — awk '$F' is no longer a use), double-quoted spans blanked for DEFINITION scanning only; -n guards credit solely on the FAILURE branch (`|| exit` / `|| { ...; exit; }`); single-letter uppercase names scanned everywhere; 7 r9 regressions (44 linter tests, 473 suite) | ~1 evaluator call | the r5 nit ('use a small shell lexer — false positives acceptable, false negatives not') is now literally implemented; escape sequences inside quotes remain the documented false-positive-direction limit |
| 2026-07-18 | — (session, PR #35 in flight, r10) | — | evaluator r10 (055ac8a): inert-text-guard ×1 (a guard-shaped string as an echo ARGUMENT credited protection), dead-branch-definition ×1 (`false && X=1` / if-false blocks credited sources), dead-branch-export ×1 (same for $GITHUB_ENV exports) | execution semantics shipped: ALL structure (defs, guard skeletons, export commands, if/case tracking) matches on the unquoted view at COMMAND POSITION only — arguments, quoted text, and &&/`||` continuations credit nothing; if/elif/else/fi and case/esac regions credit no defs/exports/guards (uses still checked); negation and shell keywords recognized as command-position preceders; 7 r10 regressions (51 linter tests, 480 suite) | ~1 evaluator call | remaining documented false-negative: loop bodies credit textually (for X in <empty> leaves X unset) — runtime [ -n ] stays that path's guard |
| 2026-07-18 | — (session, PR #35 in flight, r11) | — | evaluator r11 (0f2503c): word-exit-as-terminator ×1 (`|| echo exit 1` credited — 'exit' as an argument terminates nothing), defs-not-command-position ×1 (`echo read MY_TOKEN` defined a var; function-body assignments credited via the { anchor), dot-only-expression-detection ×1 (secrets['X'] bracket syntax evaded both the run: ban and secret-backed detection) | aborts must be COMMANDS in the `|``|` branch (exit/return at brace-start or after ;); for/read define only at command position; function bodies tracked as non-executing regions (one-liner functions handled); bracket expression syntax recognized in ban + secret detection; finding text now names the terminating forms (r11 nit); 8 r11 regressions (59 linter tests, 488 suite) | ~1 evaluator call | commit_sweep size nit acknowledged: this PR grew through 10 evaluator-driven hardening rounds on one artifact — future matcher hardening splits into follow-up PRs (process note, nothing due deferred) |
| 2026-07-18 | — (session, PR #35 in flight, r12) | — | evaluator r12 (20b276c): vars-bracket-evasion ×1 (${{ vars['X'] }} slipped the R2 ban — dot-only regex, the r11 bracket lesson applied incompletely, REPEAT-adjacent to r9's incomplete r7 fix), dead-code-after-exit ×1 (an export after unconditional `exit 0` credited later steps) | vars ban recognizes dot + bracket forms; unconditional command-position exit/return marks the rest of the script dead — credits nothing (conditional aborts unaffected); 4 r12 regressions (66 linter tests, 492 suite) | ~1 evaluator call | class note: 'fix applied to one regex but not its siblings' has now bitten twice (r9 short-names, r12 vars-bracket) — when hardening one pattern, grep for every sibling pattern in the same file before pushing |
| 2026-07-18 | — (session, PR #35 in flight, r13) | — | evaluator r13 (3fe4360): secrecy-scoped-guard-duty ×1 (R4 guarded only secrets-backed env — `TOKEN: ${{ env.MISSING }}` and every other empty-able expression context consumed unguarded), untested-core-property ×1 (no negative fixture for exactly that bypass), advisory-findings-as-PASS ×1 (nit promoted: commit_sweep printed 15 findings, exited 0, evidence said PASS) | R4 broadened to ANY expression-backed env value that can render empty (only a safelist of always-present github.* platform fields is exempt) — 8 real unguarded sites found across the three pipeline workflows, all guarded ${X:?} fail-loud; advisory exit contract: 0=clean / 3=findings (commit_sweep + test_audit) so validate records honest ADVISORY rows without parsing stdout; 4 r13 regressions (70 linter tests, 496 suite) | ~1 evaluator call | guard duty is about EMPTY-ABILITY, not secrecy — the rule's name changed from SECRET-GUARD to EXPRESSION-GUARD to match |
| 2026-07-18 | — (session, PR #35 in flight, r14) | — | evaluator r14 (459a084): run-expression-ban-scoped-to-secrets ×1 (the r13 lesson applied to env values but not to direct run: expressions — inputs/steps/needs/event contexts still interpolated raw), blessed-bypass-test ×1 (a fixture explicitly asserted the banned pattern clean), nondeterministic-contract-test ×1 (the 0-or-3 assertion could not fail on regression) | R3 generalized: ANY non-safelisted expression directly in run: is banned (also the script-injection surface per GitHub's own hardening guide) — 8 live sites in adversarial-review.yml itself hoisted to guarded env (incl. the range resolution rebuilt as expression-layer ternaries + ${X:?}); blessed test inverted; commit_sweep contract pinned deterministically (injected-finding → 3, empty-checks → 0); tool-broken advisory exits recorded as 'tool failed (rc=N)', never 'findings' | ~1 evaluator call | the examiner's own workflow was the last unhardened surface — the gate now polices the gate that runs it |
| 2026-07-18 | — (session, PR #35 in flight, r15) | — | evaluator r15 (f1301f9): non-executing-param-guard ×1 (`false && : "${X:?}"` and escaped `\${X:?}` credited as guards — the r10 execution-semantics lesson applied to -n tests and defs but not to param expansions, third member of the apply-the-lesson-to-all-siblings class after r9/r12) | ${X:?} credit now requires execution: escaped expansions are literal text; a &&/`||` continuation before the guard or a conditional region never credits; escaped \$X is not a consuming use either; 6 r15 fixtures (505 suite) | ~1 evaluator call | sibling-completeness class at THIRD occurrence (r9 short-names, r12 vars-bracket, r15 param-guards) — per the repeat-class rule the structural counter-move is now due at the next linter touch: a checklist test enumerating every credit path × every execution-semantics rule |
| 2026-07-18 | — (session, PR #35 in flight, r16) | — | evaluator r16 (8984e1d): prefix-assignment-persistence ×1 (`X=abc cmd` credited LATER lines though the shell never sees it), self-corrupting-ledger-rows ×1 (this PR's own rows quoted shell pipes raw — the naive split shifted M2/M4 columns, so the repeat-class alarm read the wrong cells: the meter's input hygiene was its own blind spot) | assignment persistence requires the STANDALONE form, parsed by a quote+paren automaton on the raw line (command substitutions with nested quotes read as one value); trends parser masks code-span pipes and FAILS LOUD on any row that is not exactly 7 cells; the three offending live rows made machine-safe; 6 fixtures incl. the live failure shapes (511 suite) | ~1 evaluator call | the meter catching its own input corruption via the evaluator = the writing convention is now enforced by the parser, not by care |
| 2026-07-18 | — (session, PR #35 in flight, r17) | — | evaluator r17 (3bb08aa): git-failure-as-no-match ×1 (a bad diff range read as "no web files touched" and silently skipped the npm/SCA gate), swallowed-diff-extraction ×1 (a failed `git diff` could hand the evaluator a notes-only pr.diff — the review gate could approve without seeing the subject) | web-check trigger captures the file list via command substitution under `set -euo pipefail` (git failure kills the step); subject diff extracted standalone-first so failure precedes any pr.diff assembly; dispatch range syntax validated at resolution (nit promoted) | ~1 evaluator call | both holes were PRE-EXISTING in the examiner's workflow since PR #11 — sixteen rounds of hardening the new gates ended with the evaluator auditing its own delivery pipeline |
| 2026-07-18 | — (merge reconciliation, PR #18 rows) | — | no new catches — merging master imported PR #18's ledger rows, and the trend meter (hard gate, this PR) immediately alarmed on three of that workstream's families whose fixes shipped in prose without machine markers, plus a token artifact (`class repeat ×N` read as class `repeat` — normalized to `class-repeat ×N`, counts preserved in-family) | marker backfill per the KAIZEN convention, citing the rows whose M4s contain the fixes: overstated-provenance → PR #18 r2/r3 (doc-wide provenance rewording, per-vote verifier records, STATE completion narrowed); stale-cross-reference → PR #18 r22/r23 (repo-wide renumber + status sweep, then deliverable-header sweep; no recorded catches after r23); provisional-license-in-ranked-bucket → PR #18 r2 (fail-open-licensing-bucket fix moved the provisional France license out of the cleared bucket) | — | epoch semantics apply: these markers cover the RECORDED catches only — any post-merge recurrence of these families alarms immediately as fix-escaped; cross-workstream debt surfacing at merge is the meter doing its job |
| 2026-07-18 | — (merge reconciliation, PR #18 rows, cont.) | — | no new catches — completing the marker backfill after per-family adjudication of the imported rows (facts: last-catch row vs recorded fix, per `tools/kaizen_trends.py` family listing) | false-confidence-gate → #18 r15 M4 (gates REDESIGNED: pinned registry + arithmetic point/range/B-unit + 9 committed negative fixtures, at the family's last catch); bypassable-trust-gate → same r15 redesign (substitution and redaction both fail); stale-provenance-claim → #18 r11 M4 (full-repo verbatim grep sweep; no recurrence after); fail-open-interchange-schema → #18 r21 M4 (shared non-blank pattern, null-proof branches, 10 data-driven parity fixtures against the schema itself); caveat-proximity → fixed in-round at #18 r17 (nit-severity placement fixes; zero recurrence through r24 — if it EVER recurs, epoch semantics alarm immediately and the due structural response is a proximity linter over docs/research) | — | markers assert the RECORDED catches were addressed, nothing more — post-marker recurrence of any of these five alarms as fix-escaped |
| 2026-07-18 | — (session, PR #35 in flight, r18 post-merge) | — | evaluator r18 (3c34fac): case-blind-scanner ×1 (lowercase env names — valid in GitHub and shell — invisible to every rule), brace-fragile-expression-matchers ×1 (`format('{0}', ...)` wrappers evaded the vars ban and the run: expression ban — the `[^}]*` class breaks on any single `}`; FOURTH occurrence of the sibling-completeness class: I fixed two of three matchers before testing), return-as-terminator ×1 (run: scripts are not sourced functions — `|| return 1` aborts nothing) | all name regexes case-insensitive (shell specials RANDOM/SECONDS/LINENO/HOSTNAME/OSTYPE/REPLY added to ambient); every `${{ }}` matcher uses lazy/tempered matching; terminators are `exit` only; the sibling-completeness COUNTER-MOVE finally shipped: a source-level test bans the fragile `[^}]` class in every expression matcher at once — a new sibling regex cannot silently reintroduce it; 7 regressions (619 suite) | ~1 evaluator call | the class that repeated four times now has its structural fix — the meter's own rule applied to the process that maintains the meter's sibling tools |
| 2026-07-18 | — (session, PR #35 in flight, r19) | — | evaluator r19 (267f842): charter-decision-divergence ×2 (the founder's verbatim merge directive lived only in a memory decision note + night_shift — the charter's "AI never publishes" invariant text was never amended, so the controlling docs conflicted as written; the evaluator, unable to see chat, correctly demanded the ratification live where the invariant lives) | the founder-ratified scope note now sits IN the charter's prime directive 1, quoting the 2026-07-18 directive verbatim and drawing the boundary explicitly: "AI never publishes" = the product data path and outward product surfaces, untouched; agent merges happen ONLY at evaluator-APPROVE + all-checks-green, founder notified; docstring nits aligned to r18's rules | ~1 evaluator call | lesson: a founder ratification recorded anywhere except the invariant's own text leaves the invariant contradicted on disk — ratifications amend the controlling doc, with the memory note as the why |
| 2026-07-18 | #35 (MERGED 083c430) | 20 | full chain in the 18 in-flight rows above: 1 founder(Red) catch, ~40 evaluator catches across 19 REQUEST-CHANGES rounds spanning gate-shipping, shell execution semantics, meter integrity, examiner-pipeline hardening, and charter governance; plus the self-caught pushed-on-red and the four-occurrence sibling-completeness arc (gated at r18 — summary reference, no new catches) | every round's fixes shipped in-PR with regression tests (619 suite at merge); notable M4s: skip→Record binding, kaizen_trends hard gate with epoch markers, workflow_env_lint shell scanner (R-019), examiner workflow fail-loud, source-level sibling-class gate, charter merge-scope ratification | ~20 evaluator calls + CI | merged by the agent under the founder-ratified scope note at evaluator-APPROVE + trust-gate green, golden-exam in its designed red (the enumerated exception, ratified same day); M1=20 is the series' high-water mark BY DESIGN — the PR became the harness-hardening vehicle; the meter's M1 direction now carries this outlier honestly |
| 2026-07-18 | — (correction to the #35 merge row above) | — | evaluator r1 on PR #36 (deb152f): laundered-gate-red ×1 (the merge row and changelog recorded golden-exam's designed red as part of a successful merged state WITHOUT naming its consequence — the merge left extraction formally UNCERTIFIED against the changed harness, since `extraction-exam-dispatch.yml` is manifest-bound and the flag's evidence hash no longer matches), prose-classified-bypass ×1 (the ratified exception had no mechanical classifier and no compensating control — fail-open gate custody as written) | compensating control SHIPPED: trust_gate `check_extraction_certification` — flag True requires the committed certification record (`ai/golden/CERTIFIED_HARNESS.json`) to match the CURRENT harness hash; missing/drifted/malformed all FAIL with the founder action named (6 tests); charter exception rewritten with the verifier's own refusal output as the classifier and the re-lock as the executable compensation | ~1 evaluator call | the #35 merge row's framing is SUPERSEDED: golden-exam's red was a DEBT MARKER, not release evidence — the debt is now mechanically red (trust_gate) until the attended exam re-certifies master's harness |
| 2026-07-18 | — (PR #36, bootstrap-red arming) | — | no new catches | the certification re-lock ships ARMED-RED by design: master's harness drifted at #35 with no record to match, so trust_gate correctly fails on this very PR until the founder's attended exam produces the record — the documented PR #11 pattern (a gate that arms itself merges red once, human-reviewed) | — | NOT the pushed-on-red class: validate was run, its red is the shipped gate reporting the true certification state, and the exit path is a single founder dispatch |
| 2026-07-18 | — (attended exam run 29659010747) | — | no new catches — the founder-directed attended exam ran on master's harness and PASSED every corrected-gate bar: 316 asserted facts (≥300), hallucination 0.0063 (≤0.01), recall 0.9782 (≥0.8), zero injections, zero unanswered | certification record `ai/golden/CERTIFIED_HARNESS.json` committed (harness 390c9fba…, run id + artifact digest bound); trust_gate green everywhere; R-013 RESOLVED with cited evidence — the first fully-verified extraction certification in the repo's history | one attended exam (~$2-class spend, founder-authorized) | M7 baseline hereby MEASURED at 0.63% field-level — the one-way ratchet now has its first real data point; event-level rates (2.6% ≥1-hallucinated-field, 9.1% any-error) recorded for product visibility per KAIZEN §M7 |
| 2026-07-18 | — (PR #36 r2, c435fd0) | — | evaluator r2: forgeable-compensating-control ×1 (the cert re-lock read only harness hash and run id — a self-authored record with the new hash and any run id turned trust_gate green while golden-exam sat exception-red, reopening the bypass the PR claimed to close), test-blesses-forged-fixture ×1 (`test_matching_record_passes` asserted OK on a two-field forgery), test-swallows-gate-red ×1 (`test_real_repo_passes` tolerated any extraction-certification failure — claimed self-tightening, never tightened); nits: stale-superseded-prose ×1 (resolved R-013 kept present-tense pre-exam wording), unhandled-type-error ×1 (non-object record JSON crashed instead of failing closed); plus one self-caught class before CI: heredoc-lint-blindspot ×1 (our own workflow_env_lint rejected the draft workflow steps — python-written GITHUB_ENV exports and python-in-heredoc conditionals are invisible or poisonous to its shell scanner; seven symptom findings, one design defect) | offline layer: the record must be a complete well-typed PASSED certification whose metrics pass the CURRENT thresholds before the hash re-lock applies (fourteen branch tests, the forged fixture now a rejection); ONLINE layer shipped: extraction-eval.yml authenticates any CHANGED record against the Actions API — real successful default-branch attended dispatch bound to the record's subject SHA, artifact zip digest, single-member report cross-checked metric-by-metric, then the same verify_exam_evidence re-derivation as the head-bound path — forging a record is now a red check, and record-only PRs green MECHANICALLY instead of needing the enumerated exception (proof replaces judgment: strictly stronger); the charter-keyed classifier and the authenticator moved from inline YAML into tools/classify_extraction_surface.py and tools/authenticate_certification_record.py — unit-testable for the first time (thirty-three tests incl. a classifier-surface to trigger-paths sync test), workflow shell fully lint-verifiable; `test_real_repo_passes` asserts exit 0 flat | ~1 evaluator call | the self-caught class is treasure per KAIZEN: the linter shipped in #35 caught the #36 draft before CI could |
| 2026-07-18 | — (PR #36 r3, f818e19) | — | evaluator r3: bootstrap-order-inversion ×1 (the record and its only authenticator rode the SAME PR — under pull_request_target only a merged copy authenticates, so the record would have entered unverified: the self-authored-record hole again, one layer up), exception-scope-overreach ×1 (the charter promised the red moves to trust_gate for the whole classified surface, but the surface is broader than the certification hash — a verifier or trust-path change could use the exception while the drift check stayed green: fail-open as written), nonfinite-metric-bypass ×1 (json accepts NaN and an abs-difference tolerance test on NaN is always False — a NaN rate slid through the authenticator agreement check) | staged bootstrap adopted, gate then record then re-lock in SEPARATE PRs — this PR reduced to stage 1 (authenticator + classifier + charter mechanics; record and offline re-lock withdrawn to stages 2 and 3); the classifier now partitions its refusal by HARNESS_MANIFEST read AS DATA from the runner single source — manifest-bound (re-lock, the red moves) vs NOT manifest-bound (base-owned execution + per-run data bindings + the blocking review on that PR) — with a mechanical-identity test so scope drift fails CI; charter exception rewritten per class and mirrored into the merge decision note (nit three); the authenticator finite-validates every metric on BOTH sides of the agreement check (NaN, Infinity, bool, string all reject, with tests); correction to the exam row above (append-only): its wording — record committed, trust_gate green everywhere, R-013 RESOLVED — described the pre-r3 single-PR branch state; the record enters at stage 2, the re-lock at stage 3, and R-013 stays resolved on its own trigger (passing run + verifier output cited) | ~1 evaluator call | the r2 design survived review in mechanism but not in ORDER — sequencing is part of a gate soundness, now stated in the charter |
| 2026-07-18 | — (PR #36 r4, 3a4f97c) | — | evaluator r4: prose-classified-bypass ×2 (SECOND appearance of the r1 class, both instances in one round: the charter described the manifest-bound compensating re-lock as live while stage 1 ships no re-lock — an active prose bypass for the staging window — and the merge decision note mirrored the same unimplemented condition as if executable), premature-resolution ×1 (R-013 marked RESOLVED on the evidence half alone while its enforcement half was explicitly deferred to future staged PRs — a resolved row must describe enforcement that exists); nit: negative-count-acceptance ×1 (the authenticator count validator accepted negative integers) | prose-classified-bypass → structural CLASS fix shipped this round at the meter's threshold-3 alarm: tools/governance_claims_lint.py (blocking in validate, six tests) — scans CLAUDE.md + docs/memory/decisions/ for cited repo artifacts and FAILS when an absent artifact is presented as live without staging/negation language in the same sentence; on first run it also caught seven pre-existing stale charter references (validate.py naming, absent FOUNDER_DIGEST and KEYS docs, superseded design-brief filename, never-committed source-canon paths, renamed changelog-append doc), all corrected; the exception is now scoped to what EXISTS: eligibility keys on the classifier's printed partition — a refusal is covered ONLY when proven to contain no manifest-bound file, manifest-bound refusals have NO exception until the stage-3 re-lock ships (ordinary red, hard stop), and the stage-3 PR widens the exception in the SAME commit as the re-lock so text is never ahead of mechanism; fail-open subclass caught in the same pass and closed: an unreadable manifest previously labeled everything as the exception-ELIGIBLE unbound class — it now marks the refusal exception-INELIGIBLE explicitly; decision note re-mirrored to the narrowed condition; R-013 reverted to OPEN with evidence and enforcement halves stated separately; counts must be non-negative integers (tests for all of the above) | ~1 evaluator call | prose-classified-bypass is now at two appearances — a third fires the class-watch structural response: a lint that greps charter and decision notes for compensation claims naming tools or checks absent from the tree |
| 2026-07-18 | #36 (MERGED d3d08e5, stage 1 of 3) | 5 | full chain in the four in-flight r1–r4 rows above — summary reference, no new catches here: 14 recorded evaluator catches (machine-countable from the four rows above) spanning gate-laundering, prose-ahead-of-mechanism (three of one family, its structural class fix shipped at the meter's own threshold alarm in the r4 row), forged-record acceptance in gate and tests, swallowed test red, bootstrap ordering, exception scope, and non-finite metrics — no new catches at r5 (APPROVE with four non-blocking hygiene notes carried to stages 2–3: routing_ok TypeError catch, marker tightening, curl -f, TMPD trap) | stage-1 gate merged: online record authenticator, unit-tested manifest-partition classifier, charter exception scoped to what exists, governance_claims_lint blocking in validate | 5 evaluator calls + CI | merged by the agent under the founder-ratified scope note at evaluator r5 APPROVE + trust-gate green; golden-exam in its designed refusal red, VERIFIED exception-eligible under the narrowed rule (refused files extraction-eval.yml, test_golden_exam.py, trust_gate.py — none in HARNESS_MANIFEST); stages 2 (authenticated record) and 3 (offline re-lock + exception widening in the same commit) follow immediately |
| 2026-07-18 | — (PR #37 stage-2 r1, 0d1e283) | — | evaluator r1 on the record-only PR: reviewer-evidence-gap ×1 (the golden-exam authenticator ACCEPTED this exact head — run log: classifier record-class, run 29659010747 verified, artifact digest matched, metrics cross-checked, verify_exam_evidence re-derived — but the review bundle carried only pytest, web, and validate logs, so the evaluator could only see the record's self-assertion and correctly refused it; the other two blocking items are the same gap seen twice: the subject_sha-vs-validate-head complaint is answered by the authenticator's binding chain, and the record-without-authenticator-output complaint IS the gap); standing item, response recorded: the validate INCOMPLETE-ACKNOWLEDGED objection is R-002 (visual_regression skip, bound with an objective deploy-time trigger, unchanged across every merged PR since it was recorded — not alterable from this PR without relaxing a gate, which is founder-crucial); nit fixed: merge-row counts made exact (14 catches, 5 calls) | adversarial-review.yml now fetches the golden-exam check's job log for the PR head (waits for conclusion, attaches verbatim, absent = stated explicitly) and passes it as a fourth evidence log — the evaluator sees the co-gate's own output, never a record's self-assertion; read-only checks/actions permissions added | ~1 evaluator call | the co-gates now share evidence: what one gate proves, the other can see |
| 2026-07-18 | — (PR #37 stage-2 r2, a2f6962) | — | evaluator r2 on the evidence-sharing step itself: mixed-head-evidence ×1 (validate.log reports GitHub's synthetic merge commit while golden-exam.log keys to the PR head — two SHAs with no visible relation reads as stale or mixed-commit evidence), http-error-as-absent-evidence ×1 (the api wrapper lacked curl -f, so a 401/403/404/rate-limit body could parse as JSON and demote to absent instead of failing loudly), swallowed-evidence-fallback ×1 (a failed log download degraded to explanatory prose while the review proceeded — the exact gap the step exists to close) | the step now ASSERTS the binding mechanically — the review checkout's second parent must equal the PR head or the step fails closed, and the relation is printed into the evidence header; api() uses curl -f; the download fallback is deleted (verbatim evidence or a red check, never prose in its place) | ~1 evaluator call | evidence about evidence gets the same fail-closed bar as evidence |
| 2026-07-18 | — (PR #37 stage-2 r3, 9afa628) | — | evaluator r3, still sharpening the evidence channel: unauthenticated-evidence-selection ×1 (the co-gate check was selected by name and first-in-list — a duplicate or stale check named golden-exam could put the WRONG log in front of the evaluator; identity was asserted nowhere), prose-delegated-cogate-failure ×1 (a missing or non-success co-gate became explanatory prose in the bundle while the review proceeded — a swallowed failure delegated to the AI reviewer instead of a mechanical gate) | selection is now identity-BOUND: only a completed github-actions job whose parent run is extraction-eval.yml on pull_request_target with run and job head_sha equal to the PR head is accepted, and more than one validated candidate fails closed (ambiguity rejection, also the r3 nit); conclusion policy is mechanical three-way — success attaches, a failure whose log is exactly the classifier designed harness-refusal attaches (hard-failing there would deadlock the charter exception), ANY other failure or a missing check on a PR touching the extraction-surface trigger paths fails the step; off-surface applicability is computed from the trigger paths themselves, never assumed | ~1 evaluator call | remaining nits routed to stage 3: curl -f in extraction-eval.yml, action SHA-pin and Node-20 cleanup already queued in TODOS |
| 2026-07-18 | — (PR #37 stage-2 r4, 03fe00a) | — | evaluator r4: subject-owned-policy-read ×1 (co-gate applicability was computed from the MERGED tree's copy of extraction-eval.yml — the PR under review could edit that paths list and classify itself off-surface: fail-open bypass of the very gate being attached), substring-exception-match ×1 (the designed-refusal exception accepted any failed log CONTAINING the refusal sentence — a job that also failed for a second reason, like a rejected record, would ride the exception into the AI bundle instead of blocking) | trigger paths now read from the BASE copy via the merge commit's first parent (git show HEAD^1:...), and since the base paths list names extraction-eval.yml itself, a policy-editing PR is on-surface under the OLD policy regardless; the refusal exception is now exact — EVERY error line the failed job emitted must be the designed refusal (logic unit-verified against pure, mixed, reject-only, and error-free logs) or the review step blocks; own-lint catch in the same pass: the exactness flag was first assigned inside a conditional, invisible to workflow_env_lint, and hoisted at its finding | ~1 evaluator call | the three standing hygiene nits (curl -f in extraction-eval.yml, SHA pins, TMPD trap) are stage-3 work STRUCTURALLY: touching extraction-eval.yml in this record PR would flip it to harness-refusal red and destroy the authenticated-record green path |
| 2026-07-18 | — (PR #37 stage-2 r5, ce1e7da) | — | evaluator r5: annotation-only-failure-scan ×1 (the refusal check read only error ANNOTATIONS — a co-gate job failing with the refusal annotation plus an unannotated traceback or rejection in plain output still passed as the allowed exception), conclusion-state-overbreadth ×1 (the exception applied to every non-success conclusion — cancelled or timed_out could ride through if the annotation set looked right; incomplete evidence must fail closed), untested-gate-logic ×1 (the parser was a shell one-liner: security-critical gate logic with no tests that could fail) | the judgment moved into tools/cogate_evidence.py with twelve tests: grounded on the job's STEP conclusions from the Actions API — exactly one failed step and it must be the classify step, every other step success or skipped — plus exact refusal annotations, and only for conclusion exactly failure; raw-text scanning deliberately rejected (the runner echoes step source, so healthy logs contain words like REJECTED as code text — step conclusions are ground truth, and the tests pin that false-positive case too); the path-grammar nit landed in the same helper (explicit fnmatch grammar, ordered exclusions fail closed) | ~1 evaluator call | the un-annotated second-failure case and the script-echo false-positive case are both pinned as named tests — the two ways this parser could lie are now the two ways it visibly breaks |
| 2026-07-18 | — (PR #37 stage-2 r6, 4d15dc2) | — | evaluator r6: subject-owned-judge ×2 (the tested helper — and its tests — lived in the PR merge tree: a future PR could rewrite the very code that judges its own co-gate evidence, for both the applicability and the refusal calls), stale-cross-base-evidence ×1 (identity binding checked workflow, event, and head SHAs but not base freshness — checks attach to commits, so a same-head success minted against an OLDER base would be accepted for the current merge), grammar-passthrough ×1 (the path helper claimed a narrow grammar but handed arbitrary patterns to fnmatch — GitHub and fnmatch diverge on wildcard semantics, so an unanticipated pattern would silently mis-evaluate instead of failing closed) | the EXECUTING helper copy is now base's (git show merge^1), with a loud one-time subject-bootstrap while the helper first lands in the reviewed diff itself; candidate validity moved into the trusted helper and gained freshness (run must start AFTER the current base head commit exists) and PR-number binding, both tested; the grammar accepts exactly the two forms the policy uses — literal paths and dir/** — and raises on anything else even when another pattern already matched; nits fixed in the same pass: settled polling (no false red from a stale completed check racing a pending valid one) and per_page=100 pagination | ~1 evaluator call | seventeen helper tests now pin every judge; the judge itself is no longer the judged |
| 2026-07-18 | — (PR #37 stage-2 r7, 7e4ceae) | — | evaluator r7: timestamp-heuristic-as-binding ×1 (the r6 freshness check compared run start to the base commit timestamp — a run can start after a commit exists yet still execute against another base; a heuristic was standing where a proof belonged), optional-identity-binding ×1 (PR membership was skipped whenever the API returned an empty pull_requests list — the header claimed bound while the code accepted unbound), test-blesses-fail-open ×1 (a named test asserted the empty-list acceptance as an API quirk — the suite protected the unsafe branch; SECOND appearance of the blessing-test shape after stage-1 r2's test-blesses-forged-fixture) | base binding is now PROVEN, not inferred: the base-owned verifier declares BASE_SHA and HEAD_SHA in step env and the runner prints them verbatim into the job log — every echo must equal the current merge-parent base and the PR head, at least one of each, or the review fails closed (log-bindings judgment, tested for wrong-base, wrong-head, single-mismatch-among-many, echo-free, and malformed cases); the timestamp heuristic is DELETED, not layered; PR membership is mandatory and its blessing test inverted; nineteen helper tests | ~1 evaluator call | second blessing-test catch — if the shape appears once more the structural response is a reviewer checklist item: every fail-open tolerance in a test must cite a Record row or be treated as a defect |
| 2026-07-18 | — (PR #37 stage-2 r8, 01832e7) | — | evaluator r8: bootstrap-judges-own-evidence ×1 (the subject-bootstrap helper fallback was judging THIS PR's own record evidence — the PR carried both the judge and the judged; the r6 bootstrap allowance was sound only for plumbing-only diffs and this PR was not one), page-size-as-completeness ×1 (per_page=100 was treated as exhaustive — a second identity-valid run beyond page one would be invisible, so ambiguity detection could silently report a false singleton) | the PR is SPLIT at the evaluator's prescription: #37 becomes stage 2a, evidence plumbing only — the certification record is withdrawn to a follow-up stage-2b PR where the executing judge is base-owned; PLUS the mechanical belt so this composition can never recur: while the helper is not on base, any PR touching the extraction surface fails closed (bootstrap PRs are plumbing-only, enforced not narrated); pagination now fetches all pages and asserts collected == total_count, failing closed on any shortfall (merge logic verified against mismatch and match fixtures) | ~1 evaluator call | bootstrap sequence is now: gate (#36) → plumbing (2a) → record (2b) → re-lock (stage 3) — each PR judged only by already-merged machinery |
| 2026-07-18 | — (PR #37 stage-2 r9, 438b3a8) | — | evaluator r9, terminal verdict on the bootstrap concept: privileged-subject-execution ×1 (subject-bootstrap ran PR-owned code inside the privileged review job — GH_TOKEN in env, same mutable workspace as every evidence file, BEFORE the evaluator ran), self-enforced-containment ×1 (the plumbing-only belt was computed by the very subject-owned helper it was meant to contain — a malicious helper could classify itself off-surface), vacuous-validation-on-empty-input ×1 (pattern validation lived inside the per-file loop, so an empty changed-file list skipped it — malformed policy returned False instead of failing loud), unanchored-log-parsing ×1 (base/head binding scanned the whole log for substrings — described as proof, implemented as spoofable text matching) | the subject-bootstrap concept is DELETED, not patched: adversarial-review.yml reverts to master byte-for-byte and this PR lands the helper INERT (nothing executes it in CI); the wiring follows in its own PR that hard-requires the base copy with no fallback path at all; helper fixes land reviewed-now-executed-later: pattern validation independent of the changed-file list, and log-binding parsing anchored to runner Run-group env regions in exact format — step output, the only subject-influencable region of the base-owned job's log, is ignored, with the spoof-in-output and spoof-instead-of-real cases pinned as tests (twenty-one total) | ~1 evaluator call | bootstrap sequence, final form: gate → inert helper → wiring (base-copy only) → record → re-lock — five PRs, zero moments where unmerged code judges anything |
| 2026-07-18 | #37 (MERGED c1bb564, stage 2a of 5) | 10 | full chain in the nine in-flight rows above — summary reference, no new catches here: the co-gate evidence channel was forced through the same trust discipline as the gates themselves across nine rounds (evidence visibility, head binding, fail-closed transport, identity-bound selection, mechanical conclusion policy, base-owned policy reads, exact refusal matching, tested judges, proven base bindings, mandatory identity, bootstrap containment and finally bootstrap DELETION) | helper landed INERT with twenty-one tests; adversarial-review.yml untouched (master byte-for-byte); the four APPROVE nits (startswith refusal matching, PyYAML on-key parsing, prose volume, R-002) carried to the wiring and re-lock stages | 10 evaluator calls + CI | merged FULLY GREEN — APPROVE with zero blocking issues, trust-gate green, golden-exam correctly not triggered: the first merge of the arc that needed no exception at all |
| 2026-07-18 | — (PR #38 stage-3 r1, c06b10f) | — | evaluator r1 on the wiring: stdlib-shadowing-import ×1 (plain python3 from the subject checkout leaves cwd and script-dir on sys.path — a future PR committing json.py or pathlib.py at repo root hijacks even the base-sourced helper, with GH_TOKEN in the environment), symlink-clobber-evidence ×1 (trusted helper and evidence intermediates were written into the subject checkout under predictable names — a committed symlink named golden-exam.log or trusted-cogate.py redirects the write, corrupting the evaluator bundle or the judge itself) | every python in the evidence step now runs with -I (isolated, implies -P: no cwd, no script dir, no user site — the same discipline as the pinned evaluator script) and EVERY intermediate lives in a fresh mktemp -d outside the checkout, handed to the evaluator step via an env-exported path with a fail-closed guard; nothing the evidence channel reads or writes has a subject-controllable name or path | ~1 evaluator call | the channel now matches the threat model of the job it runs in: privileged token, hostile workspace |
| 2026-07-18 | #38 (MERGED 534a806, stage 3 of 5) | 2 | stage-3 r1 row above (stdlib-shadowing-import, symlink-clobber-evidence) — no new catches at r2 (APPROVE; nits: governance prose density deserves a state-machine table, action SHA-pins still queued) | the co-gate evidence channel is LIVE: base-copy-only helper, python -I, trusted tempdir, proven bindings, mechanical conclusion policy | 2 evaluator calls + CI | merged with every required check for the diff green (the standing R-002 visual_regression skip acknowledged in validate — not full release evidence); second consecutive no-exception merge; the channel judged its own wiring PR's not-applicable path live |
| 2026-07-18 | — (PR #39 stage-4 r1, 469c5b4) | — | evaluator r1 on the record PR: deferred-binding-in-artifact ×1 (the record's own note field promised the re-lock lands in the FINAL bootstrap stage — deferral prose inside a committed trust artifact, where only live bindings belong), hardening-claim-contradicted-by-evidence ×1 (the attached co-gate log showed extraction-eval.yml running plain python3 and unflagged curl while the evidence header asserted isolation — the review record cannot rest on a claim its own attachment disproves); nit acted on: ledger-overclaim ×1 (merge rows said fully green while validate is INCOMPLETE-ACKNOWLEDGED under R-002 — the #38 row is reworded precisely here; the #37 row is already merged history, so THIS row is its append-only correction: read its fully-green as every-required-check-for-the-diff-green with the standing R-002 skip acknowledged) | PR #39 is repurposed to the verifier-hygiene stage (the hardening cannot ride the record PR: touching the verifier is refusal-red and would strand the record unauthenticated): extraction-eval.yml gets curl -fsS everywhere, python3 -I on every script and heredoc (all verifier tools self-insert the repo root, verified), and tempdir cleanup traps; the review evidence header claim is scoped to the review side explicitly; the record follows with a live-bindings-only note | ~1 evaluator call | bootstrap, final form: gate, helper, wiring, verifier hygiene, record, re-lock — six PRs |
| 2026-07-18 | — (PR #39 stage-4 r2, 5be9136) | — | evaluator r2: isolation-breaks-import ×1 (a REAL shipped defect caught before merge: the blanket python -I removed cwd from sys.path and broke the one -c invocation importing the repo-local ai package — the hardening PR would have crashed the verifier it hardens on the next surface PR; root cause is a testing gap, the workflow shell has no executable tests, so the -I sweep was verified only against the tools it calls, not the inline snippets), aspirational-evidence-claim ×1 (the evidence header pointed at the attached log for the verifier's hardening while that log is necessarily produced by the UNMERGED-hardening master copy — provenance claims must be mechanically true at attach time), unencoded-exception-in-evidence ×1 (the evaluator was asked to accept a failing co-gate without the reviewed exception being stated in the evidence it judges) | the repo-local import gets an EXPLICIT trusted-dir sys.path insert under -I (verified live against the real invocation, hash matches); the header claim is scoped to the review side with judge-by-log wording; the charter exception is now ENCODED into the evidence bundle for every screened designed-refusal — the eligibility rule plus the instruction to verify it against the classifier's printed partition in the attached refusal | ~1 evaluator call | first live round-trip of the whole channel: the merged classifier printed its partition, the merged evidence channel screened the refusal mechanically, and the evaluator judged it — the loop the arc was built for, working end to end |
| 2026-07-18 | #39 (MERGED f079405, stage 4 of 6) | 3 | stage-4 r1 and r2 rows above (deferred-binding-in-artifact, hardening-claim-contradicted-by-evidence, isolation-breaks-import, aspirational-evidence-claim, unencoded-exception-in-evidence, ledger-overclaim) — no new catches at r3 (APPROVE, no blocking issues; nits: the changelog's earlier fully-green phrasing is corrected by THIS row's convention — every merge claim in this arc reads as every-required-check-for-the-diff-green with the standing R-002 skip acknowledged; workflow-shell testability idea noted for the harness backlog; SHA pins standing under the TODOS P3 verified-upstream constraint) | verifier hygiene LIVE: fail-closed transport, isolated python, trap-cleaned tempdirs; exception encoded in evidence | 3 evaluator calls + CI | merged under the encoded exception with the refusal screened mechanically — the first merge whose exception eligibility was judged from evidence the channel itself attached |
| 2026-07-18 | #40 (MERGED 2c200cf, stage 5 of 6) | 1 | no catches — APPROVE with no blocking issues on the first round (nits: a changelog phrasing typo, prose density, R-002 standing) | the certification record is ON MASTER, entered exclusively through the merged hardened gate: golden-exam authenticated it (real run, artifact digest, report cross-check, verify_exam_evidence re-derivation) and the review judged the co-gate log the channel attached | 1 evaluator call + CI | every required check for the diff green (R-002 skip acknowledged); the first certification record in the repo's history to enter fully authenticated |
| 2026-07-18 | — (stage 6, the re-lock: WHAT the commit ships; review outcomes recorded only after rounds conclude) | — | rows below record the rounds as they conclude | charter exception WIDENED to manifest-bound refusals in this same commit (the red moves — mechanism and text simultaneous, as the r4 rule demanded); trust_gate check_extraction_certification restored from the reviewed design with the fourteen-branch test contract; classifier bound-class message, encoded evidence note, decision-note mirror, test docstrings, and R-013 all move together | — | the design intent of the closing commit; audit outcomes live in the per-round rows |
| 2026-07-18 | — (stage-6 r1, 1742fd6) | — | evaluator r1 on the re-lock: nonbool-flag-as-closed ×1 (the guard treated every non-literal-True flag — the string true, 1, None — as a safely closed state; misconfig must fail loud, the same rule the classifier already applies to routing_data's flag), unvalidated-example-count ×1 (metrics.examples was never checked — a record with passing asserted_facts but zero or missing examples passed the completeness gate; behind it a real offline gap: the golden set is outside the harness manifest, so set drift was invisible to the offline layer); nit acted on: premature audit prose in the in-flight ledger row, reworded to design-intent-only | the flag guard returns clean ONLY for literal False and fails loud on any non-bool (five malformed values tested); examples must exist, be a true count, meet the 40-example floor (R-013's documented requirement, pinned by test_golden_exam), AND equal the CURRENT golden set's size (NOTE, corrected at r2: this count binding alone did NOT catch same-size edits — the true content binding shipped at r2; the count check remains as record-field validation); success-message indentation aligned | ~1 evaluator call | the examples check turned a completeness bug into a new offline drift binding |
| 2026-07-18 | — (stage-6 r2, 548dbfa) | — | evaluator r2: count-as-content-binding ×1 (the r1 golden-set binding compared nonblank line counts — a same-size edit or replacement stayed green while the certification no longer described the set: false confidence, and the r1 ledger prose overclaimed it), test-encodes-weak-binding ×1 (the r1 tests pinned the count behavior with no same-count-drift failure case — the suite could not fail for the real drift class) | the binding is now CONTENT: the current set must hash byte-identical to the set at the record's AUTHENTICATED subject commit (git history at subject_sha — the sha the run's head binds, whose set the exam's golden_sha verified); same-size edits re-red, unreadable history fails closed with the shallow-clone remedy named, trust-gate CI checkout gains full history; same-count tampering and git-unavailable cases pinned as tests; the r1 row's overclaim corrected in place (unmerged) with the correction noted | ~1 evaluator call | the authenticated subject_sha turned out to carry the content binding for free — proof was already in the record, the gate just had to use it |
| 2026-07-18 | — (stage-6 r3, 56050e9) | — | evaluator r3, the arc's deepest catch: circular-trust-hasher ×1 (the re-lock imported compute_harness_sha from ai/golden_exam.py — itself manifest-bound, so the exact PR class the widened exception covers could alter the hasher or shrink the manifest to preserve the certified hash, waiving the golden-exam red with no control firing; the classifier message, the charter widening, and the sole-proof line were all surfaces of this one root), fixture-trusts-subject-hasher ×1 (the valid-record fixture derived its hash from the same runner function, so the suite could never fail for the bypass) | trust_gate owns an independent manifest copy and hasher — no manifest-bound code participates in verifying itself; threshold imports stay safe because the independent hash pins ai/exam_thresholds bytes before a lowered value could judge anything; lockstep sync test (single-sided drift fails pytest in the same PR; coordinated tamper touches trust-path files, compensated by the mandatory review — stated in the charter clause added this round); adversarial tests: a LYING runner hasher cannot change the verdict either direction, and the independent hasher reds on any single byte of any manifest file; fixture rebased onto the independent hasher; docstring audit-trail nit fixed | ~1 evaluator call | charter-density nit recorded for follow-up: prime directive 1 needs a structural reformat (bulleted mechanics or linked table) — TODOS row added |
| 2026-07-18 | — (stage-6 r4, 257beee) | — | evaluator r4, one root across five surfaces: record-rides-refusal-bypass ×1 (a PR changing manifest-bound harness code AND the certification record would be refused BEFORE the authenticator runs, yet the widened exception read it as re-lock-compensated — while trust_gate would validate the forged-but-complete record against the PR's own harness: the re-lock only ever compensates the unchanged, previously-authenticated record), missing-adversarial-test ×1 (the suite had the bare-forgery rejection but no case for the dangerous shape: complete hash-matching forgery riding a harness refusal) | refusals accompanied by any record change now carry the classifier's canonical EXCEPTION-INELIGIBLE marker (unreadable-manifest refusals unified onto the same marker), and the review's evidence step FAILS CLOSED on the marker mechanically — eligibility is no longer memory; charter, encoded evidence note, and trust_gate's scope docstring all state the unchanged-record-only compensation; the combined-case and marker-detection tests pinned | ~1 evaluator call | the exception's last judgment call is now a grep-able token printed by base-owned code and enforced by a failing step |
| 2026-07-18 | — (stage-6 r5, d586aab — infra red, caught by our own fail-closed usage error, evaluator never reached) | — | workflow-helper-version-skew ×1 (kin of the staged-bootstrap family, distinct mechanism: the PR's WORKFLOW copy activates immediately while the helper it calls runs from BASE by design — the r4 ineligibility check referenced a subcommand base's helper predates, and the helper's fail-closed usage error killed the step; the discipline worked, the composition was mine) | the marker check is a fixed-string grep in the workflow — no helper-version dependency, no logic beyond a token whose semantics the helper's tests pin (the subcommand still ships for future use); rule extracted for the harness: a PR-copy workflow may only call base-copy helper interfaces that ALREADY exist on base | self-caught by CI, no evaluator call spent | the fail-closed default turned a would-be silent skew into a loud red |
| 2026-07-18 | — (stage-6 r6, f1e8ca8) | — | evaluator r6, the governance-logic capstone: dead-letter-or-double-red-exception ×1 (with the re-lock live, a manifest-bound PR is trust_gate-red ON ITSELF — so the widened exception either could never fire, or silently implied merging through a non-exempt red; the classifier and evidence note presented eligible without encoding that consequence); nit: hasher-test docstring overstated single-mutation coverage | eligibility for manifest-bound refusals is now CLOSURE-CONDITIONED, verified mechanically: the classifier parses the subject's routing data and marks the refusal ELIGIBLE only when the same PR sets EXTRACTION_THRESHOLD_RATIFIED to literal False — the closure moves, not the red: extraction is off, fail-closed, from merge until the standing three-step re-opens it (attended exam, authenticated record, head-bound flag flip), trust_gate green throughout, and a still-ratified manifest-bound refusal carries EXCEPTION-INELIGIBLE (no double-red path exists, stated in charter, evidence note, and decision note); closure-eligible and still-ratified cases pinned as classifier tests; docstring rescoped | ~1 evaluator call | the exception ends fully mechanical: every eligibility branch is a printed token derived from parsed data, and every merge path is green-at-merge |
| 2026-07-18 | #41 (MERGED 289eaf3, stage 6 of 6 — BOOTSTRAP COMPLETE) | 7 | full chain in the six stage-6 rows above — summary reference, no new catches here: circular-trust elimination, content-over-count binding, fail-loud flag, combined-case ineligibility, version-skew rule, and the closure-conditioned eligibility capstone | the certification system is WHOLE: authenticated record entry (base-owned online), independent offline re-lock (own manifest+hasher, content-bound golden set), per-class evidence-encoded exception with every eligibility branch a printed token, and no double-red merge path anywhere | 7 evaluator calls + CI (one round self-caught by our own fail-closed design, no evaluator spent) | merged with every required check for the diff green except golden-exam's designed refusal (not manifest-bound, screened+attached by the evidence channel, eligible per the printed partition; R-002 skip acknowledged in validate); R-013 RESOLVED with evidence and enforcement both cited; the arc that opened with a prose-bypass finding closes with a fully mechanical exception |
| 2026-07-22 | #42 (MERGED 1244783 on 2026-07-18 — row appended LATE) | 1 | self-caught at this session close: missing-pr-ledger-row ×1 — the docs-only session-close PR #42 (the very PR that appended #41's merge row) never received its OWN row, and no gate noticed for four days ("one row per merged PR" is prose, not a check) | none yet — candidate mechanization noted in the class watch: a validate check that every merged PR number since #11 has a ledger row (previously proposed in the 2026-07-18 session row, still unbuilt — that gap is what let this slip) | 1 evaluator call (APPROVE run 29664329112, first round, no findings) | append-only convention holds: this row is dated the day it was written, and names the miss instead of backfilling silently |
| 2026-07-22 | #44 (MERGED 53f6f9f — R-021 datetime truth boundary, split from #43) | 4 | evaluator r1 (BLOCKER): silent-preservation-skip ×1 (preserve_discarded_claims silently SKIPPED preservation when _provenance existed as a non-dict — breaking the central "NULL with raw preserved" contract exactly when provenance was already suspect); r1 nits: dropped-timezone-abbreviation ×1 ("7pm ET" parsed naive, a timestamptz column would silently shift it), locale-ambiguous-numeric-date ×1 ("03/04/2026" — dateutil quietly applies the US assumption); r2 (BLOCKER): truncated-preserved-value ×1 (the r1 fix truncated the malformed original it preserved — truncation is itself data loss); r2 nits: misleading-refusal-reason ×1 (tz-refused claims are dated but tz-unusable, not "undated"), overwrite-on-merge ×1 (existing unstored claims clobbered instead of merged); r3: no new findings, wording pass | all fixes shipped in-PR with 36 tests incl. the four exact live-failure strings and every refusal-reason branch; the malformed-provenance path now REPLACES (original kept IN FULL under _provenance_malformed_original) and merges per-field | 4 evaluator calls + CI | merged at APPROVE run 29880533719 under the ratified per-class exception (classifier printed NOT-manifest-bound for worker/ai_extract.py, zero ai/golden/ files in the diff, trust-gate green); the fix had already proven itself live pre-merge (run 29878035857: two time-only sources → HELD candidates, zero insert errors) |
| 2026-07-22 | #43 (MERGED ab6819a — Step 5 ARMED: 20-minute ingestion cron) | 22 | full chain in FRICTION_LOG entry #3 (r1–r21 attacks + terminal APPROVE), the arc's real catches in brief: sequencing (evidence-first: green smoke committed → APPROVE on that head → merge), stale-cross-reference ×4-family (hourly→20-min cadence purge — top class again), attempt-row bookkeeping (loud ERROR + strict-vs-best-effort modes per path), wrong-code-proven ×1 (r11: the green run executed master, not the PR head — caught by the review's own log binding), prose-only-alarm-config ×1 (r9/r11: healthchecks period/grace could not rest on future prose → tools/assert_deadman_period.py, blocking, fail-closed), misbindable-ping-url ×1 (r17, the arc's sharpest: declared-name matching verified check A while the worker could ping check B — closed with the every-run /log probe that must move the verified check's n_pings), governance-prose-as-verification ×1 (r16 → docs/evidence/ARMING_SMOKE_RUN.json + tests/test_arming_smoke_binding.py, git half), self-authored-evidence ×1 (r21 → API half: the recorded run verified against the live Actions API, REQUIRED fail-closed in trust-gate), split-demanded ×1 (r21 → PR #44 carved out; this PR's final diff touched zero extraction surface) | assert_deadman_period (blocking precondition, triply-coupled cadence/declaration/live-check), the two-half arming-evidence binding harness (git + Actions API, REQUIRED in trust-gate), MAX_SOURCES structurally pinned in both workflow steps, rotation by least-recently-ATTEMPTED with attempt rows | ~22 evaluator calls + 8 smoke/verification runs | merged FULLY GREEN at APPROVE run 29881030319 — no exception needed on the final head; the cron is LIVE at 7,27,47 * * * *; M1=22 is the new high-water mark, driven by live-infrastructure truth-grounding (5 of the rounds were the evaluator correctly refusing evidence that proved the wrong code or rested on prose) |
| 2026-07-22 | — (session, PR #45 in flight: the cron-stall escalation) | — | founder(Red): slow-escalation ×1 — GitHub's scheduler missed its first cron slot at 01:07Z and the consolidated founder ask (the only path to the fix: a two-tap workflow disable/enable exposed nowhere in the agent's API surface) was not delivered until 04:02Z; three hours of multi-slot patience windows while bridging manually was a process defect, not diligence ("Do not let things run for so long without firing. Troubleshoot faster. Fix faster. No compromises on world class.") | escalation ladder codified in docs/OPERATING_RULES.md §4 (first miss = full config verification + every self-serve mitigation in the same pass; second miss = founder ask NOW when the fix needs founder hands; watch cadence = one expected-event interval + lag, never multi-interval windows) | — | founder catches mean every automated layer missed it — this one was a JUDGMENT gap (no gate could fire); the ladder converts the judgment to a stated rule the evaluator can hold against future stalls |
| 2026-07-22 | — (session, PRs #45/#46 in flight: the unarmed watch gap) | — | evaluator (#45 r6): stale-live-incident-state ×2 (STATE.md and RECORD.md both froze mid-incident at the 04:1x status while hours passed) — surfacing the underlying repeat: slow-escalation ×1 REPEAT (same day as the founder(Red) original): PR #46's APPROVE landed 05:28Z and sat unmerged ~6.5h because the awaiting turn ended with no wake-up armed and merge/CI-success events are never delivered | ladder amended in the same commit (OPERATING_RULES §4): "a watch turn never ends unarmed" — arm a wake-up for every awaited external outcome, with a stated fallback when the preferred scheduler is unavailable | — | the class fix is now stated rule + this row; a third same-class instance is an escape of the ladder itself |
| 2026-07-22 | — (repeat-class hardening, same commit as the #43 row above) | — | the #43 row's honest count re-fired the repeat-class alarm: the family recurred ×4 AFTER its row-61 structural fix, because that fix covered evidence-line cross-references while the r7 recurrences were CADENCE PROSE in live-state docs going stale when the cron changed — a surface the old gate never scanned | NEW MECHANICAL GATE for the recurrence surface: test_live_state_docs_never_contradict_the_armed_cadence (tests/test_ingest_workflow_contract.py, coupling #4) derives the cadence from ingest.yml's cron and fails on any contradicting stale-cross-reference claim ("hourly …", "N-minute cron/cadence/ingestion", "every N minutes") in STATE.md/TODOS.md/RECORD.md/CLAUDE.md; failure case demonstrated red before this row was written; append-only history deliberately out of scope | — | the alarm did exactly its job: an honest count may never be softened to dodge it — the fix moves to the recurrence's actual surface |
| 2026-07-22 | #46 (MERGED 76d2290 — cron re-registration + harness reporting fixes; row appended in the first post-merge bookkeeping commit, same day: the #42 late-row lesson held) | 6 | evaluator r1: dispatch-run-cannot-prove-registration ×1 (the PR's evidence claimed what a workflow_dispatch run structurally cannot show — claims rescoped, dead-man named as the fail-closed absence detector); r2: validate-label-as-approval-evidence ×1 (answered in writing citing the skip→Record mechanism, superseded by r5's in-PR fixes); r3: bare-queued-deferral ×1 (an unlogged "queued" violated the Record rule → R-022 opened in-PR with the base-owned structural answer); r4: evidence-prose-overclaim ×1 ("head identity" → precise runtime-identical-modulo-non-runtime wording); r5: deferral-itself-blocked ×1 (both harness fixes shipped in-PR instead: validate's truthful terminal states + the packet's conditional SCA claim, R-022 resolved in-PR); r6: false-EXECUTED-GATES-GREEN ×1 (validate printed GREEN with advisories present → the GREEN/ACKNOWLEDGED split) | validate now emits truthful terminal states (ANY_ADVISORY branches the verdict) and the review packet asserts SCA evidence only for web-touching diffs — both were latent harness-truthfulness gaps this PR happened to surface | ~7 evaluator calls + 2 verification-run cycles (self-inflicted: runtime edits landed AFTER evidence runs twice, forcing re-runs — edit runtime BEFORE the evidence run is the noted process fix) | merged at APPROVE run 29893875957 + trust-gate 29893875858 on final head; honest postscript recorded in R-008: the minute shift proved unnecessary for registration (the founder's disable/enable had already taken effect, first fire 07:07Z pre-merge) — the PR's lasting value is the harness fixes |
| 2026-07-22 | #45 (MERGED 6f8b12b — session close: FLOW v3.8, arming records, five signal-acquisition specs, convergence spec) | 6 | evaluator r1–r5: fixture-false-provenance family (design prototype sample cards/lenses read as real venue claims → per-surface then per-card FIXNOTE truth boundaries, content-bound by tests negative-tested red on planted defects); r6: the two mid-incident live-state staleness catches are COUNTED in the session row above (summary reference here per the #41 precedent — one count per catch, and the unarmed-watch rule is that row's fix); founder(Red) catches during the design rounds, the classes every automated layer missed: no-JS-viewer render defects ×2 (unreachable reveal threshold blanked tall cards; JS-dependent architecture showed only the map page → the fully-static v3.1+ rebuild) and delivery-channel-mangles-HTML ×1 (the founder's file preview strips structure → artifact-link + screenshots + PDF as the delivery protocol); process fix now standing: headless verification in BOTH no-JS and JS contexts before every founder delivery | per-card content-bound integrity tests (titles + fixnote text per room/lens, anchor resolution, :target CSS), generator-freshness byte-compare, and the delivery protocol | ~7 evaluator calls + CI across 28 commits | merged at APPROVE run 29924988068 + trust-gate 29924988004 green on final head 0e60bcf, ~35 min after the APPROVE (the watch-turn rule held); evaluator's non-blocking notes carried forward: split generated-artifact PRs from governance PRs (matches the standing speed-up protocol), no-JS voice-copy nit queued for the next design round |
| 2026-07-22 | — (session, PR #49 in flight: r1) | — | evaluator r1: stale-live-incident-state ×1 (THIRD instance, firing the threshold-3 class alarm exactly as designed — STATE.md's NEXT queue still opened with "observe the FIRST schedule-event run → flip R-008 with its id" after the same paragraph resolved R-008), active-deviation-buried-in-resolved-row ×1 (the sparse-delivery deviation lived inside R-008's RESOLVED row with no OPEN row, owner, or trigger — the canonical risk ledger read green during an active operational deviation → R-023 opened with a three-part objective trigger incl. alarm-fire verification, which the agent environment cannot perform and must not assume), reassuring-evidence-prose ×1 (the evidence field asserted the dead-man "covers gaps by design" while whether it actually alerted is unverified — evidence rescoped to registration-only with the deviation bound to R-023) | THE CLASS FIX the threshold-3 alarm demands, marker: stale-live-incident-state — NEW MECHANICAL GATE tests/test_live_state_consistency.py: STATE.md's NEXT queue may not direct work (flip/resolve/close) at a Record row RECORD.md already marks RESOLVED; the exact PR #49 defect shape is pinned as a red case, narration outside NEXT deliberately out of scope | 1 evaluator call | nits adopted: R-008's density prose reduced to a pointer at R-023 (row-length audit relief), STATE's stale "FLOW v3.1" corrected to the delivered v3.8 |
| 2026-07-22 | — (session, PR #49 in flight: r2) | — | evaluator r2: false-confidence-gate ×1 (the r1 class gate matched only verb-adjacent directives — "R-008 → RESOLVED", "close row R-008", "finish R-008", "work R-008", or a wrapped NEXT continuation line all slipped past, so validate could go green while the live handoff still pointed at a resolved row: the gate claimed a class fix it did not deliver) | gate REBUILT DEFAULT-DENY (structural fix for the gate family itself, marker: false-confidence-gate): in the live NEXT block, ANY mention of a RESOLVED Record row fails unless immediately tagged "(RESOLVED" — no verb list to evade, paragraph-aware (wrapped lines captured), historical sections and OPEN rows out of scope by design; the r1 shape AND all five r2 evasion shapes pinned red; STATE.md's live NEXT normalized to the tagged form | 1 evaluator call | nit adopted in full: R-008's incident narrative moved VERBATIM to docs/ops/INCIDENT_2026-07-22_cron-scheduler.md, the row now carries citations + scope boundary + pointers only |
| 2026-07-22 | — (session, PR #49 in flight: r3; also the append-only CORRECTION to the r2 row above) | — | evaluator r3: tag-launders-directive ×1 (false-confidence-gate family, third gate iteration — the r2 "(RESOLVED" tag was a blanket bypass: "flip R-008 (RESOLVED ...)" passed while still directing live work at a resolved row, and the negative tests only covered untagged shapes), overstated-provenance ×1 (FIFTH family instance: the incident file's header claimed a copy-fidelity extraction the diff disproves — later material added, earlier reassuring wording rescoped; the r2 ledger row above repeats the same false claim in its M6 column and THIS ROW IS ITS CORRECTION, append-only per convention: the file is an ASSEMBLED, edited narrative, provenance now stated in its header) | TWO structural fixes, markers named: (a) false-confidence-gate — the live-NEXT gate is now TWO LAYERS (default-deny tag + directive-verb deny-list that the tag cannot launder, all three r3 laundering shapes pinned red, missing-NEXT-block fails loud per the r3 nit) AND states its honest limit in its docstring (deny-lists are incomplete; the guarantee is no-untagged-mention + no-known-directive-laundering + every survivor visibly flagged — intent stays with the evaluator); (b) overstated-provenance — NEW MECHANICAL GATE tests/test_doc_provenance_claims.py: no docs/ops INCIDENT_*.md narrative may carry a copy-fidelity self-claim (assembled docs cite git commits instead; r3 defect shape pinned red; scope deliberately excludes design-canon docs whose "copy verbatim" is a product-copy requirement, not self-provenance — the repo-wide first draft of this gate false-positived on the 2026-07-12 changelog and was narrowed before commit) | 1 evaluator call | r3 nit adopted: R-023's alarm-verification trigger now names PATH A (RO-key flips read via an assert_deadman_period.py extension, readability untested and provable in one dispatch) with founder confirmation as the recorded-only fallback |
| 2026-07-22 | — (session, PR #49 in flight: r4) | — | evaluator r4: false-ops-arithmetic ×1 (one root, three surfaces — STATE/R-023/evidence all said "3 fires across ~44 slots ≈ one run per 2.5–3 h": 3 fires over ~14h40m is one per ~4.9 h; the 2.5–3 h figure is the registration PICKUP LAG, a different quantity conflated into a delivery rate — live incident math the founder decision depends on was wrong AND self-contradictory), reassuring-evidence-prose ×1 (second instance: the incident narrative still asserted "the dead-man alarm covers exactly this gap by design" unqualified while R-023 holds alarm-fire verification as PENDING) | single-source-of-truth structure for operational metrics: the R-023 row is now the ONLY holder of density numbers (corrected there with the measurement window and both quantities named); STATE names the correction and points at R-023; the evidence field carries NO operational numbers at all (an evidence artifact may not restate metrics it is not the authority for); the incident doc's coverage sentence carries an explicit SUPERSEDED marker binding it to R-023's pending verification | 1 evaluator call | r4 nits: provenance regex widened to the class's equivalent phrasings ("copied exactly", "unchanged excerpt", "exact copy"); the structural-parse suggestion for the NEXT gate noted for future churn — verb-list growth is bounded by the docstring's stated guarantee, not open-ended |
| 2026-07-22 | — (session, PR #51 in flight: parallel-agent sprint + the PATH A probe arc) | — | self/CI catches, no evaluator spent: external-api-shape-assumed-from-docs ×1 (the probe's parser trusted the upstream repo's documented bare-array flips body; the hosted service wraps it {"flips": [...]} — the probe's own fail-loud design caught it on the first live run 29963320514, which had ALREADY answered the R-023 readability question positively via HTTP 200 before failing); assertion-free-test ×1 (C1's float-dust test had zero asserts — test_audit's blocking mirror caught it pre-commit, the exact "cannot fail proves nothing" class); scanner-scans-scratch-clones ×1 (agent worktrees under .claude/ duplicated every allowlisted file at non-allowlisted paths → 32 false trust-gate violations; my own pre-attack then caught that the naive exclusion would hide COMMITTED code from the sweeps) | probe: tolerant-of-documented-shapes parse + structure-only diagnostics (types/key names, never values); C1: assertion restored + the audit gate's mirror stays blocking; scanners: SKIP_PARTS ".claude" shipped PAIRED with tests/test_scratch_tree_exclusion.py (tracked files under .claude/ forbidden outside the prose agent-definition allowlist — the exclusion cannot hide committed code) | 2 verification dispatches, 0 evaluator calls on these catches | R-023 trigger part 2 CLOSED mechanically by the probe: 30 flips / 15 DOWN in 24h — alarm works, crying-wolf is measured fact; the founder alarm decision now has hard evidence |
| 2026-07-22 | — (session, PR #51 in flight: r5, the evaluator's first full round on this PR) | — | evaluator r5: evidence-citation-mismatch ×1 (kin of r4's false-ops-arithmetic, same one-root shape: R-023's SATISFIED claim cited DOWN 12:27:15Z and 14:48:40Z as "covering both originally-questioned gaps" — 14:48 belongs to a LATER gap and nothing cited covered the FIRST gap; the true covering events exist in the same probe output (10:28:44Z for gap 1, 12:27:15Z for gap 2) so the conclusion survives, but a citation that does not prove its claim is false operational evidence and the evaluator was right to block it) | citations corrected in R-023 and STATE to the timestamps that each cover their own gap, with the correction named in-row; probe hardened per the r5 nit: "up" validation tightened from isinstance-int to the documented `0|1` domain (out-of-domain ints are malformed, never silently UP), with 2/-1 pinned as malformed tests | 1 evaluator call + 1 re-verification dispatch (the nit fix touches runtime → binding recycles) | r5's split-scanner-PR advice adopted as standing practice under the parallel-branches grant: future gate-surface changes ride their own small PRs |
| 2026-07-22 | — (session, PR #51 in flight: r6) | — | evaluator r6: relative-import-evasion ×1 (false-confidence-gate family, fourth gate iteration this arc — the isolation sweeps skipped node.level entirely, so `from .convergence import sl` in a worker/ sibling or `from ..ai_extract import x` inside the package evaded both directions of the "two-way" gate); mixed-pr-blocks-exception ×1 (process catch: the evaluator declined to exercise the NOT-manifest-bound per-class exception on a MIXED PR carrying the harness file — the thrice-repeated split advice became a blocker, and the demand is legitimate: the exception's compensating control IS this review, and a gate-surface change buried in a broad diff gets weaker review than the exception assumes) | (a) marker: false-confidence-gate — relative imports now RESOLVE to absolute names against each file's package location before both sweeps, with all three r6 evasion shapes pinned red (worker-sibling relative inbound, package parent-climb outbound, legitimate internal relative allowed); (b) split complied via the parallel-branches grant: the scanner-scope subset (trust_gate/deferral_scan SKIP_PARTS + true-scope guard + .gitignore + binding exemption) lands FIRST as its own single-purpose PR, after which this PR's diff sheds the harness file and golden-exam goes green — no exception needed at its merge | 1 evaluator call | r6 nits adopted: R-023's opening "UNVERIFIED" context marked HISTORICAL (settled by trigger part 2); bool acceptance in the probe's up-domain DOCUMENTED as deliberate (True==1 makes it implicit in any int check; rejecting bools would break the probe if the hosted service serializes booleans) |
| 2026-07-22..23 | — (session, PRs #52/#53 landed + PR #51 r7) | — | The split-and-brief arc: #52's four rounds caught mixed-pr-blocks-exception ×1 (r6, complied via the split), packet-misstates-ratified-discipline ×1 (the #52-r2 deadlock's root: the evaluator's briefing stated a stricter rule than the founder-ratified exception — fixed base-owned in #53, the R-022(b) remedy class, and the evaluator APPROVED the correction to its own briefing), evidence-prose-vs-gate-divergence ×1 (#52 r3: the purpose field's enumeration omitted the .gitignore exemption), quoted-pathname-guard-evasion ×1 (#52 r4, real fail-open: git ls-files quoting fragmented a newline pathname past the component check → NUL-delimited parsing, evasion pinned). #51 r7: auth-fail-open-probe ×1 (the 401/403 exit-0 "access answer" became fail-open the moment readability was live-proven — a revoked key yielded a green workflow; now every probe failure path is loud, tests re-pinned), sweep-scope-narrower-than-claim ×2 (false-confidence-gate family: top-level glob left subpackages invisible to BOTH the outbound sweep and the dynamic-import ban while prose claimed package-wide — now rglob with discovery itself pinned red) | markers: false-confidence-gate (recursive discovery + pinned subpackage shape), auth-fail-closed restored on every probe path, packet-truthfulness corrected base-owned (#53 merged at the examiner's own APPROVE) | ~6 evaluator calls + 2 verification dispatches across the arc | r7 nits adopted: stale 401/403/404 comment fixed; sl.py component bounds made EXACT (no epsilon — dust lives in sums only) with tiny-out-of-range shapes pinned; the recurring split advice is now standing practice and visibly earning deeper per-file scrutiny |
| 2026-07-24 | — (session, PR #60 in flight: r1 — universal dev/operating-model PROPOSAL, docs-only) | — | evaluator r1: evidence-scoping ×1 (BLOCKER, kin of the reassuring-evidence-prose family: STATE.md's session-close line RETYPED local-sandbox gate numbers — "1097 passed / 29 skipped @ a8e7b73" — as if they were THE gate record, contradicting the CI logs attached to the very review that judged it: 1098/28 on the synthetic merge head; the local run also stamped the PRE-commit head because validate ran on a dirty worktree, so the citation was doubly unanchored; "paste verbatim, never retype" is validate's own printed discipline and the close line violated it); r1 nits: provenance-source-conflation ×1 (the doc listed the unread-403 primary among "cross-checked write-ups" as if read — now split primary-not-read vs excerpt-read, with the excerpt-only basis stated), portable-doc-inherits-local-ratification ×1 (the kernel's merge rule read as pre-ratified for adopting projects — now explicitly OneLive-only, per-project ratification via overlay binding 7) | close-line evidence is now SCOPED BY CONSTRUCTION: CI logs on the PR head named authoritative, the local run named corroboration-only with its dirty-worktree/pre-commit-head caveat stated in the line itself — a future reader cannot mistake local numbers for the record | 1 evaluator call | the catch generalizes beyond this PR: any session whose sandbox env differs from CI will reproduce the numeric mismatch, so the counter-move is the scoping discipline, not fixing the numbers |
| 2026-07-24 | — (session, PR #60 in flight: r2) | — | evaluator r2: evidence-scoping ×1 REPEAT — same class as r1, caught in the r1 FIX itself: the corrected close line said CI evidence is "on the PR head" while the review's own attached golden-exam.log states the evidence commit is GitHub's SYNTHETIC MERGE COMMIT whose second parent is the PR head — the fix for a mis-anchored citation was itself mis-anchored one level up (a same-round repeat inside the counter-move is the sharpest form of the class) | close line re-anchored to the true commit topology (evidence = review checkout / synthetic merge commit, BOUND to the head, never "on" it), with the r2 correction named in-line so the record shows the class repeating inside its own fix; r2 nits adopted: Appendix A now preserves the VERBATIM search excerpts (labeled verbatim-title vs search-tool-synthesis) so provenance is auditable against what was actually seen, and founder ask 2 now makes template approval conditional on a NAMED OWNER + standing maintenance trigger (unowned-template rot promoted from prose tradeoff to ask) | 1 evaluator call | watch note: two consecutive rounds on the same class in one PR — if evidence-scoping fires again in ANY PR, the structural response is a mechanical gate (a validate check that STATE.md gate-evidence claims name their commit topology), not a third wording fix |
| 2026-07-24 | — (post-merge of #60: founder directive) | — | founder(Red): research-without-primary-source ×1 — PR #60's Part 1 reconstructed the Cherny article from search-result excerpts after every primary/mirror fetch 403'd, and PROCEEDED on that basis with caveats (provenance block, verbatim-excerpt appendix, spot-check ask); the founder's directive ("Don't ever assume or summarize or proceed to perform any strategic or deep research that you are unable to access the primary documents or files or information") names the whole pattern the defect: caveats never license proceeding without the primary — the correct move was STOP the article thread, blocker report + smallest founder unblock, continue only the repo-derived kernel | rule encoded verbatim in docs/OPERATING_RULES.md §1 ("No research without the primary source") + decision record docs/memory/decisions/2026-07-24_primary-source-gate.md; retroactive remedy in the same commit: Part 1 marked BLOCKED-ON-PRIMARY-VERIFICATION in the doc itself, unblock = founder pastes the text or confirms from a browser | — | founder catches mean every automated layer missed it — no gate scans for "research produced without primary access"; candidate mechanization deliberately NOT built now (a scanner cannot judge source primacy) — the rule is judgment + evaluator-holdable prose, and a repeat is an escape of this row |
| 2026-07-24 | — (PR #61 in flight: primary supplied, verification executed) | — | the primary-source-gate unblock fired same-day: the founder supplied the full article export (Boris_Cherny_Jul_16_2026.md) and directed "Review this" — verification of the excerpt-reconstructed Part 1 against the primary found FIVE substantive deviations (the hard evidence for the class): (a) the Steps 2 and 3 "unlocks next step" cells had listed those steps' own features instead of Cherny's actual 2→3/3→4 transitions; (b) "what separates the steps is trust, not the model" was an unlabeled paraphrase, not in the primary; (c) "the central trap of the whole model" over-generalized what the primary states as the Step-3 trap; (d) Step 4's bottleneck was missing its per-work-type-guardrails half; (e) two role names imprecise ("an org tree", "VP steering by intent") | Part 1 rewritten as VERIFIED with the corrections itemized in-doc (auditable delta); Part 3.7's transition column re-quoted from the primary (the corrected transitions strengthen the kernel mapping — Cherny's own lane-separation guardrail is I1); Appendix A marked SUPERSEDED, retained as the defect-class evidence; provenance block updated; Part 1b added reviewing the file's companion "final 10%" chart piece | — | five deviations from excerpts that "agreed with each other on every detail used" is the row's lesson stated plainly: excerpt agreement measures excerpt copying, not fidelity to the source |
| 2026-07-24 | — (PR #61 in flight: r2) | — | evaluator r2, four blockers all adopted: stale-live-incident-state ×2 kin (STATE's addendum and the decision record both still asserted Part 1 "is marked BLOCKED" in the same tree that verifies it — the fix's own paper trail lagged the fix, in the canonical session record AND in long-term memory); unauditable-verification-claim ×1 (the PR's central claim — five deviations corrected against the primary — rested on an artifact existing only in the session: "trust me, I saw it" research one level up, the very class this arc exists to kill); source-identity-conflation ×1 (Part 1 said Cherny published "on Anthropic's site" as flat fact when that claim belongs to the explainx author — the verified artifact is the explainx export, Cherny's own page remains unfetched) | STATE addendum + decision record now carry the full blocked→supplied→verified arc with the update dated (records stay historical, current-tree claims true); the primary artifact COMMITTED for audit at docs/research/sources/Boris_Cherny_Jul_16_2026.md + SHA-256 MANIFEST.json (per the eval/source_material precedent — internal audit copy, never republished); source-identity wording scoped to whose claim each fact is; nit adopted ("Kr$na" marked sic as the primary's own spelling) | 1 evaluator call | the unauditable-verification catch completes the arc's lesson: the primary-source gate needs BOTH halves — access the primary before researching, AND commit the primary (or its hash-bound record) so the verification is checkable by anyone after you |
| 2026-07-24 | — (repeat-class hardening, same commit as the PR #61 r2 row above) | — | the r2 row's honest count re-fired the repeat-class alarm exactly as designed (family stale-live-incident-state, 2x after its row-105 structural fix): root cause — the existing gates cover STATE's live NEXT block vs RECORD rows and cadence claims in live-state docs, but the r2 recurrences live on a THIRD surface neither scans: prose claims about ANOTHER doc's lifecycle marker contradicting that doc in the same tree (the fix's own paper trail lagging the fix) | NEW MECHANICAL GATE for the recurrence's actual surface, marker: stale-live-incident-state (same-diff paper-trail surface) — tests/test_live_state_consistency.py layer 3: DEFAULT-DENY on the lifecycle marker (when no doc carries the banner, every marker occurrence in STATE.md + docs/memory/decisions/ must read as history via a past-cue window; no verb list to evade, per this family's own r2/r3 lessons); BOTH PR #61 r2 defect shapes pinned red, past-tense history and banner-live present claims pinned allowed; honest limit stated in-code (cue proximity is a heuristic — semantics stay with the evaluator) | — | the alarm did its job twice in one arc: an honest count may never be softened to dodge it, and the fix moves to the recurrence's actual surface (row 114 precedent, now upheld a second time) |

## Class watch (M2 repeat classes — these must trend to zero)

- **empty-env fail-open**: seen PR #11 (model var), #12 (source cap), #14 r3–r4
  (review model). Gate response has now escalated three times (or-default →
  hard-fail → channel removed). If this class appears again, the process fix
  is structural (an env-contract linter for workflows), not another patch.
- **wrong-arithmetic (founder-facing sizing)**: PR #18 r10 + r12 (the fix
  itself failed). Mechanical gate covers numeric count × price forms; the
  STATED GAP is non-numeric counts ("S&P-500-class × $10–25k"). Trigger: if a
  wrong-arithmetic catch ever involves a non-numeric count form, extend the
  gate to require such phrasings be rewritten to numeric counts before commit.
- **fail-open (threshold/floor mismatch)**: r6 on PR #25 caught the exam's
  300-fact floor silently moved from ASSERTED to EXPECTED facts by an r5
  refactor — a rate-passing run at 295 asserted facts was minutes from
  certifying the gate open. Caught internally by the evaluator's first
  graded round after billing was restored; zero escapes. Same class family
  as #11/#12/#14 empty-env — fail-open now has TWO sub-classes on watch.
- **incomplete-enumeration (binding/trigger lists)**: PR #28 r22 (evidence
  didn't bind the golden set), r23 (didn't bind the scoring/provider files),
  r24 (didn't bind dependencies or the dispatch workflow) — three rounds of
  one class, each hand-adding one item to a list. Structural response
  shipped 2026-07-17 (founder-prompted meta-review): the exam's import
  closure is now COMPUTED by a test and required to be a subset of the
  manifest, and the release gate's trigger paths are cross-checked against
  the manifest by a second test — the derived checks immediately found two
  more instances a fourth hand-audit had missed (tools/__init__.py absent
  from both the manifest and the trigger list). Process rule added to
  docs/KAIZEN.md: in-flight per-round class tracking; every hand-maintained
  trust-guarding list gets a derivation/cross-check test. If this class
  appears again on any OTHER list, the response is a repo-wide enumerated-
  list audit (workflow paths filters, allowlists, skip lists), not a patch.
  TRIGGER FIRED same day (2026-07-17, evaluator r26): the dependency
  binding enumerated 2 of 23 resolved packages — another mirror-list
  instance, created pre-rule, discovered post-rule. Escalation executed:
  the instance became derived (full lockfile installed with --no-deps;
  verifier requires every locked entry recorded at its locked version),
  and the repo-wide enumerated-list audit ran. Audit method: classify
  each list as POLICY (the list IS ground truth: promote/exam allowlists,
  ads markers — nothing external to derive from) vs MIRROR (tracks
  external reality — must be derived or swept). Findings: the exam-
  critical mirrors were already closed by the day's derived tests; the
  SQL/ads/promote invariant scans were directory-enumerated (ai/
  production code and any future scripts/ dir unscanned) — widened to a
  repo-wide-minus-tests production sweep, single-sourced skip list
  (tests excluded by documented design: they build stub SQL and import
  promote to test the guard itself). The widened scans surfaced no
  production violations. Class considered structurally closed; a further
  instance is a process escape and gets a root-cause row here.
- **record-missing / untruthful-record**: #19 (untruthful-record), #24 (same),
  and 2026-07-15: rows for merged PRs #15/#23/#24 were absent from this table
  while the changelog claimed the #15 row existed (caught during #25 r5
  bookkeeping; backfilled same commit). Two more appearances of this class
  and the fix is structural: a validate-gate check that every merged PR
  number since #11 has a ledger row.

## M3 escapes (absolute-zero goal)

| Date | What escaped | Where found | Root cause | Gate-gap closed |
|---|---|---|---|---|
| — | none recorded to date | | | |

## M6 po harvests

| Date | Decision/plan | Provocations run | Ideas surviving gates |
|---|---|---|---|
| 2026-07-14 | Scale-out sensor architecture (RATIFIED) | Full battery, seed 20260714, word "beehive" — all P1–P8.6 | 5 candidates harvested into the doc appendix + TODOS triage item (multi-party authority; burst/hibernation lifecycle; per-source extraction templates; confirm-your-listing magic link; change-rate attention allocation) — survival through design gates TBD at Step 7 |
| 2026-07-14 | PR-aggregator venture market analysis (Contract #6) | Full battery, seed 20260714, word "beehive" — all P1–P8.6 + compact Six Hats convergent check | 16 candidates harvested (H1–H16 in PR_AGGREGATOR_MARKET_ANALYSIS.md §8): claim-lifecycle registry, executive credibility graph, say-vs-do pairing, issuer right-of-reply, IR pre-flight linter, silence detection, promise maturity calendar, open claim schema, disclosure-integrity ratings, etc. — all routed through the whitespace/combinatorial sections; build survival gated on venture go + golden-set precision |
| 2026-07-15 | FINANCIALS beachhead sector (founder-decided; battery run post-decision per charter) | Full battery, seed 20260715, word "anchor" — all P1–P8.6, ≥2 movement techniques each (`ventures/promise_ledger/docs/PO_BATTERY_SECTOR_20260715.txt` + complete working notes) | 8 harvested (H-S1–H-S8 in the working notes): bank claim taxonomy beyond guidance (capital-return/integration/remediation promises), 2023-regionals stress-window silence backtest, differentiation checklist vs Marvin/Visible Alpha before build, named ~130-bank universe, capital-return promises as first golden-set class, verdict cap ≤ likely as launch policy, guidance-drift view, macro-context field — all feed the sector build plan through the normal gates |
| 2026-07-14 | Global sensing challenge (founder research note — PROPOSAL) | Full battery, seed 20260715, word "anchor" — all P1–P8.6, 2 dead ends logged | 12 candidates H1–H12 in docs/strategy/ONE_LIVE_GLOBAL_SENSING_PO_AND_PEIRCE_NOTE_v1.md (two-phase crawl→push; coverage denominator; aggregators-first; second-county drill; zero-result queue; two-tier sensing; source-portfolio economics; anchor-institution bootstrap; community sighting channel; recurrence-as-polling-optimizer; link-graph discovery; sparse-market mode) — all screened against trust invariants, converge at Step 7 |
| 2026-07-15 | Design directions v1 (founder-directed in-house generation) | Full battery, seed 20260716, word "scaffold" — all P1–P8.6, 2 dead ends logged | Adopted into the mockups: first-door rule, fixed control geography, Doorlight signature (P5), Time Rule signature (P7), uniform glyph sky-corner (P8.6). Parked candidates: quiet-night sparse spec (→H12 tie), sequential preview autoplay (white-hat test needed). design/proposals/README.md |
| 2026-07-16 | Correction (append-only) to the 2026-07-14 market-analysis row above | — | that row's "(Contract #6)" refers to the ORIGINAL numbering; the contract is #15 (orig #6) after the 2026-07-15 merge reconciliation (STATE.md). Row preserved unedited per ledger convention; this row is the correction (evaluator r23) |
| 2026-07-16 | Dedicated-hat registry design (docs/hats/) | Full battery, seed 20260716, word "scaffold" — all P1–P8.6, 2 dead ends logged | 5 harvested, ALL adopted into the registry: H1 memory-informs-checklists-never-verdicts (P1); H2 vendor-agnostic model bindings via the router (P2); H3 two-tier ritual — sequential cheap mode vs dedicated-parallel for founder-crucial (P3-down); H4 Blue merge pre-registers the decision frame before lenses run (P4); H5 per-hat retirement condition (P7 "scaffold": removed when the building stands) |
| 2026-07-22 | Design direction v3 — founder redirect to FLOW ("flowing up down back forward… full width and height of mobile") | Full battery, seed 20260722, word "kite" — all P1–P8.6, ≥2 movements each | 5 harvested, all adopted into design/proposals/direction-4-flow.html: full-viewport snap rooms (P1 "no scroll" — you glide between rooms, not down a list); content rises to meet the thumb (P2 reversal — the city advances toward the still fan); the edition ENDS with a closing card (P3-down — bounded beats infinite); detail as forward-expansion of the same surface, never a page load (P4 distortion); the time-thread tether rail (P7 "kite": string = tether while flying). Static renders structurally cannot convey flow — the deliverable became an interactive phone prototype |
| 2026-07-21 | R-008 cron-arming Friction pre-work (FRICTION_LOG entry #3) — first mandatory battery-opened Friction firing | Full battery, seed 20260721, word "windmill" — all P1–P8.6, ≥2 movement techniques each | 5 harvested, all adopted into the arming PR or the merge protocol: least-recently-fetched rotation (P1 — the PR's main code change), overlap bounding via existing concurrency+timeout (P3), founder healthchecks period/grace step (P4), first-run token-cost watch item (P7 "windmill" feathering), smoke-run-measures-cost-before-cron + cadence-drop-as-logged-fallback (P5) |

## M8 Yellow-hat validated upside (docs/hats/yellow.md)

| Date | Decision | Upside argued | Validated (what shipped and performed) |
|---|---|---|---|
| — | none recorded to date (first firing: the R-008 cron-arming Friction pre-work — see TODOS hat shakedown) | | |
| 2026-07-21 | Arm the hourly ingestion cron (FRICTION_LOG entry #3, Yellow's first live firing) | Fresh same-night candidates for /tonight; real per-run cost curve to feed routing decisions; daily full-catalog sweep surfaces dead sources within 24h; starts R-012's "one cron week" maturity clock | PENDING — validated after the first cron week: full catalog swept daily, zero dead-man alarms, cost-per-run within the console cap's daily share (fill via a new row, append-only) |
| 2026-07-21 | Correction (append-only) to the 2026-07-21 M8 row above | — | that row's "hourly ingestion cron" and "daily full-catalog sweep" reflect the plan at first authoring; the founder amended cadence to every 20 minutes before merge (FRICTION_LOG entry #3, cadence amendment). Validation criteria update accordingly: ~3 visits/source/day capacity, zero dead-man alarms at a 20-min period, cost-per-run within the console cap's daily share. Row preserved unedited per ledger convention; this row is the correction (r7 stale-cross-reference catch) |
