# V1 — what it is, what is left, in order

**Status: CANON.** This replaces `docs/SPRINT_LIVE_SITE.md` (deleted — it still
claimed nothing had been executed) and `LIVE_READINESS.md` (deleted — its facts
were two weeks stale). The quality bar every item is held to is `docs/BAR.md`.
The evidence behind the ordering is `docs/V1_AUDIT_2026-07-26.md`.

---

## What v1 is

> **A stranger in Austin opens the site tonight and sees what is actually
> happening tonight — accurate, with honest uncertainty, refreshed without anyone
> touching it.**

That is the whole product. Deliberately thin.

## What v1 is not

Not: matching, group plans, Tastemaker posts, the Emotion/Vibe layer, voice
personas, native mobile, the Meta carousel marketing engine, the promise-ledger
venture, the knowledge-graph brain. All of those are built or specified to some
degree and **none of them is v1**. They stay in the tree, frozen, and they do not
get another line of work until the five items below are done.

## v1 is done when all five are true

| # | Done-criterion | How it is proven | Bar row |
|---|---|---|---|
| 1 | **The feed refreshes itself.** Both deterministic importers complete on their schedules, watched by dead-man alarms. | two consecutive green scheduled runs of each, and a healthchecks check that alarms if they stop | C1, C2, H1 |
| 2 | **A passing long-tail candidate reaches users without a human click**, except the ratified exceptions (conflict / unreliable source / fabrication risk → human review). | a real candidate auto-published at its earned confidence, visible on `/tonight`, with the exception paths proven by test | C3, A1–A6 |
| 3 | **Master is green.** | `bash tools/validate` exits 0 with no SKIP unaccounted for | G1 |
| 4 | **The experience is measured**, not assumed: LCP / INP / CLS and WCAG 2.2 AA on the deployed preview. | a CI run against the preview URL, numbers recorded | E1–E4 |
| 5 | **It is deployed and the URL is on disk.** | the URL in this file, `/api/health` returning `ok:true` with `eventCount>0` | H7 |

Note what is *not* on that list: no new feature, no new subsystem, no new
document. v1 is finished by connecting and verifying what exists.

---

## The order of work, and why this order

### Step 1 — the deterministic feed refreshes itself  *(in progress)*

**Why first:** it needs no AI, costs nothing per event, cannot hallucinate, and
it is the difference between a website and a demo. It also does not wait for the
Anthropic account to recover on 2026-08-01.

- **1a. DONE in this change.** `import_structured.yml` (venue / university /
  library / civic / museum calendars) failed on every scheduled run because its
  fetch bound came from `github.event.inputs.limit`, which is empty on a schedule
  event. Fixed, with `tests/test_scheduled_inputs_contract.py` now guarding the
  whole class across every scheduled workflow. *Verify by:* the next scheduled run
  at 08:17 or 20:17 UTC completing green.
- **1b. NEEDS THE FOUNDER (ask #1).** `import_licensed.yml` (Ticketmaster +
  SeatGeek — the bulk of the feed) has no schedule at all. Adding one requires a
  dead-man alarm per the charter, which needs one healthchecks.io check and one
  repository secret. Steps are in *Asks* below. Once the secret exists this is a
  15-line workflow change modelled exactly on `import_structured.yml`.

### Step 2 — a passing candidate publishes itself

**Why second:** it is the thing the founder explicitly asked for on 2026-07-25
and the only reason the long tail cannot reach users today. It is also the
largest remaining piece of real engineering.

The founder's ratification set three conditions. Honest status:

| Condition | Status | What it needs |
|---|---|---|
| 1. Source-reliability grading is real — scores updated from outcomes, so "graded unreliable" actually gates | **NOT MET** | nothing calls `worker/source_reliability.py` outside itself. An outcome-driven update on each run, persisted, with a test proving a degrading source falls below the threshold. |
| 2. Honest uncertainty display is live | **built, unproven end-to-end** | `web/lib/trust.ts` renders the quiet marker; confirm once auto-published rows flow through it |
| 3. No fabrication — sensor + schema guards stay in force | **MET** | unchanged |

Then: `worker/autopromote.py` — the only new promoter — reads
`worker/publish_policy.py`'s decision, is added to the promote-import allowlist in
the same change, and stays behind `AUTO_PUBLISH_RATIFIED` (fail-closed OFF). The
flag flips only after 1–3 are verified live on a preview.

**This is a trust-path change.** It goes through the mandatory independent review,
and the charter has been corrected in this change to stop contradicting the
ratified decision.

### Step 3 — master green

**Master is already green with full history — 1,665 tests, verified 2026-07-26.**
What is not fixed is that the agent's default environment manufactures four red
rows out of 19 that mean "your environment is incomplete," not "your code is
broken": three from missing interpreter packages, one from a shallow clone making
the arming binding unprovable (it fails closed by design — R-036). Presented in
the same red column as a real regression, they train everyone to scan past red.

So Step 3 is: a dev-dependency file plus a documented one-command bootstrap
(including full history), and `tools/validate` reporting `MISSING-TOOL` /
`UNPROVABLE-HERE` separately from `FAIL`. **The exit code does not change and no
check is loosened** — the gate still fails closed; it just stops lying about why.
(Audit D7 and D12, R-058.)

### Step 4 — deploy and record the URL

`docs/DEPLOY.md` documents a zero-config preview path: a Vercel preview needs no
environment variables at all, the gate auto-opens on preview (host-protected by
Vercel), `/ops` stays denied, and the Supabase read uses the committed public
publishable key. Deploy, hit `/api/health`, paste the URL into this file.

### Step 5 — measure the experience

A Lighthouse run in CI against the preview URL, recording LCP / INP / CLS at p75
and a WCAG 2.2 AA pass. Today **nothing** measures the part users actually
touch (bar rows E1–E4). Cheap, and it closes the largest honest gap in the
product.

---

## Asks — everything the founder needs to do, in one list

**Ask 0 arrived after this file was written and outranks the other three**, because
nothing — not this plan, not any PR — can merge until it is resolved.

### Ask 0 — GitHub Actions has stopped executing (blocks every merge)

Between 03:50Z and 04:25Z on 2026-07-26, Actions jobs in this repository stopped
running. They are accepted and fail in 1–4 seconds with **no steps, no runner and
no logs** — the signature of a job GitHub refused to schedule, not of a failing
check. Reproduced on PRs #73, #74, #75 and #76, across two different workflows,
and on an explicit re-run. Diagnosis and the full evidence table are in the
comment on PR #76; the register row is R-060.

1. Check <https://github.com/settings/billing> → **Actions** usage and
   **Spending limit**. On a private repository, exhausted minutes or a reached
   spending limit is by far the most common cause of this exact signature.
2. If that looks fine, check
   <https://github.com/schubertsean-ui/onelive/settings/actions> — an Actions
   permissions change produces an identical silent refusal.

Raising a spending limit is money, so it is yours. **The charter's merge rule is
not being bent for this:** APPROVE plus every required check green still stands,
and the work waits.

*Context worth one sentence:* this is the third provider ceiling hit in 24 hours —
Anthropic (ask 2), the Gemini free tier for the second review seat (R-052), and now
Actions. Velocity is currently bounded by billing, not by engineering.

---

Three further items. Nothing else is blocked on you.

### Ask 1 — one healthchecks.io check + one secret (2 minutes)

So the Ticketmaster/SeatGeek feed can refresh on a schedule instead of by hand.
This mirrors what already exists for the other two jobs.

1. Go to <https://healthchecks.io/checks/> → **Add Check**.
2. Name: `onelive-licensed-import`. Slug: `onelive-licensed-import`.
   **Period: 12 hours. Grace: 2 hours.**
3. Copy its ping URL.
4. Go to <https://github.com/schubertsean-ui/onelive/settings/secrets/actions> →
   **New repository secret**. Name: `LICENSED_IMPORT_PING_URL`. Value: the ping
   URL.

*Why this and not the alternative:* the alternative was to fold the Ticketmaster
import into the existing scheduled job and reuse its alarm. I did not, because one
alarm covering two different imports cannot tell you *which* one stopped — and a
dead-man alarm you can't act on is worse than none. The cost of the better option
is these two minutes.

### Ask 2 — a decision on the Anthropic cap

The account hit its usage limit on 2026-07-25; access returns **2026-08-01
00:00 UTC**. Until then AI extraction of long-tail venues cannot run. Options:

| Option | What happens | Cost |
|---|---|---|
| **(a) Launch v1 without the AI long tail** — recommended | The site ships on ticketed feeds + venue calendars, which is a real product. The long tail turns on by itself on 2026-08-01. | Fewer small/independent shows at launch. No money. |
| (b) Raise the console cap now | The long tail resumes within minutes of the cap change. | Real spend, at a rate the last few days show is being consumed quickly. |

*Recommendation: (a).* Steps 1–5 above are all deliverable without the AI path,
and shipping the deterministic product first also proves the promote → display
path before the noisier data source is pointed at it. The tradeoff, honestly:
launch-week coverage of small venues will be visibly thinner, and if you launch
publicly before 2026-08-01, that thinness is what a first visitor sees.

### Ask 3 — one word: does the auto-publish ratification stand?

On 2026-07-25 you ratified auto-publish at earned confidence. `CLAUDE.md` still
said the opposite; this change corrects the charter to match your decision. Since
it touches the trust invariant, I want it confirmed rather than assumed. **Reply
"stands" and Step 2 proceeds; reply "hold" and it stops and nothing is wired.**

---

## Deployment

**Live URL:** *not yet deployed — this line is the Step 4 done-criterion (bar row
H7). It stays exactly this honest until a real URL replaces it.*
