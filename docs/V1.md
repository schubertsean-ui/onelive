# V1 — what it is, what is left, in order

**Status: CANON.** This replaces `docs/SPRINT_LIVE_SITE.md` (content deleted, a
tombstone left in place — it still claimed nothing had been executed) and
`LIVE_READINESS.md` (deleted — its facts were two weeks stale). The quality bar every item is held to is `docs/BAR.md`.
The evidence behind the ordering is `docs/V1_AUDIT_2026-07-26.md`.

---

## What v1 is

> **A stranger in Austin opens the site tonight and sees what is actually
> happening tonight — accurate, with honest uncertainty, refreshed without anyone
> touching it — and locks their phone within ten seconds holding a decision they
> feel good about.**

That is the whole product. Deliberately thin.

The second half of that sentence is not a flourish. It is `docs/BAR.md` row P1, it
is the brief's own payoff (§5), and it is the half that has never been measured.
A v1 that satisfies the first half and fails the second is a data pipeline with a
web page attached, not the product described in
`docs/design/ONE_LIVE_MASTER_DESIGN_BRIEF_v2.4.md`.

## What v1 is not

Not: matching, group plans, Tastemaker posts, the Emotion/Vibe layer, voice
personas, native mobile, the Meta carousel marketing engine, the promise-ledger
venture, the knowledge-graph brain. All of those are built or specified to some
degree and **none of them is v1**. They stay in the tree, frozen, and they do not
get another line of work until the seven criteria below are met.

## v1 is done when all seven are true

The founder directive of 2026-07-26 — *everything is built toward the vision and how
it is supposed to make people feel* — changed this list. It previously had five
criteria, **all mechanical**: data flows, tests green, deployed. Those are necessary
and they are not sufficient. A v1 that ingests perfectly and fails the fan on the
sidewalk is not a v1. Criteria 6 and 7 are the ones that were missing.

| # | Done-criterion | How it is proven | Bar row |
|---|---|---|---|
| 1 | **The feed refreshes itself.** Both deterministic importers complete on their schedules, watched by dead-man alarms. | two consecutive green scheduled runs of each, and a healthchecks check that alarms if they stop | C1, C2, H1, **P13** |
| 2 | **A passing long-tail candidate reaches users without a human click**, except the ratified exceptions (conflict / unreliable source / fabrication risk → human review). | a real candidate auto-published at its earned confidence, visible on `/tonight`, with the exception paths proven by test | C3, A1–A6, **P8** |
| 3 | **Master is green.** | `bash tools/validate` exits 0 with no SKIP unaccounted for | G1 |
| 4 | **The experience is measured**, not assumed: load time, INP, CLS and WCAG 2.2 AA on the deployed preview. | a CI run against the preview URL, numbers recorded | E1–E4, **P2** |
| 5 | **It is deployed and the URL is on disk.** | the URL in this file, `/api/health` returning `ok:true` with `eventCount>0` | H7 |
| 6 | **The ten-second answer works.** A person who has never seen the site, on a phone, with no account, reaches a decision they feel good about — timed. | ≥ 3 first-time users, timed on a real phone on the deployed URL, **median ≤ 10 s**, results recorded here. Founder may be one of them; at least two must not be. | **P1** |
| 7 | **The feeling is scored, not assumed.** The shipped feed is graded against the brief's own 8-criterion rubric — including emotional fidelity to §3 and the white-hat reflection test. | rubric scored against the deployed surface, **≥ 4/5 on every criterion**, deltas logged never silent | **P14, P4, P12** |

Note what is *not* on that list: no new feature, no new subsystem, no new
document. v1 is finished by connecting and verifying what exists — and then by
checking that what exists actually delivers the moment it was built for.

**Criterion 6 is deliberately cheap and deliberately human.** Three people, a
phone, a stopwatch. There is no instrumentation to build, no framework to adopt,
and no excuse for it having never been done.

---

## The order of work, and why this order

### Step 1 — the deterministic feed refreshes itself  *(in progress)*

**Why first:** it needs no AI, costs nothing per event, cannot hallucinate, and
it is the difference between a website and a demo. It also does not wait on the
Anthropic cap at all — no step in this plan waits on a date.

- **1a. DONE in this change.** `import_structured.yml` (venue / university /
  library / civic / museum calendars) failed on every scheduled run because its
  fetch bound came from `github.event.inputs.limit`, which is empty on a schedule
  event. Fixed, with `tests/test_scheduled_inputs_contract.py` now guarding the
  whole class across every scheduled workflow. *Verify by:* the next scheduled run
  at 08:17 or 20:17 UTC completing green.
- **1b. DONE 2026-07-26 (founder: "build the watchdog", then "do 2").**
  `import_licensed.yml` (Ticketmaster + SeatGeek — the bulk of the feed) now carries
  `schedule: - cron: "23 2,14 * * *"`, 12 h apart and offset from 1a's 08:17/20:17 so
  the two importers never contend for the same database connection window. The
  charter's dead-man requirement is met by `.github/workflows/watchdog.yml` +
  `tools/watchdog_check.py` — **no healthchecks.io account and no repository secret**,
  because the automatic `GITHUB_TOKEN` is enough to read Actions history. It entered
  the watchdog's `WATCHED` table in the same change, so the alarm exists from the
  first scheduled run rather than being promised. **Verify by:** the next 02:23 or
  14:23 UTC run completing green (R-055). Its schedule path is safe from the 1a
  defect class: `MAX_PAGES` resolves through `|| '8'` and so is never empty on a
  schedule event.

**The watchdog only counts SCHEDULED successes** — `last_success()` filters
`event=schedule`, so a human clicking *Run workflow* cannot refresh the timestamp and
silence the alarm while the cron stays dead. That was a real blocker raised by the
independent reviewer on this PR, and it is precisely the 1a failure mode: four
successful runs, every one manual, the schedule never once working.

### Step 2 — a passing candidate publishes itself

**Why second:** it is the thing the founder explicitly asked for on 2026-07-25
and the only reason the long tail cannot reach users today. It is also the
largest remaining piece of real engineering.

The founder's ratification set three conditions. Honest status:

| Condition | Status | What it needs |
|---|---|---|
| 1. Source-reliability grading is real — scores updated from outcomes, so "graded unreliable" actually gates | **NOT MET** | nothing calls `worker/source_reliability.py` outside itself. An outcome-driven update on each run, persisted, with a test proving a degrading source falls below the threshold. |
| 2. Honest uncertainty display is live | **built, unproven end-to-end** | `web/lib/trust.ts` renders the quiet marker; confirm once auto-published rows flow through it |
| 3. No fabrication — sensor + schema guards stay in force | **MET** | unchanged |

Then: `worker/autopromote.py` — the only new promoter — reads
`worker/publish_policy.py`'s decision, is added to the promote-import allowlist in
the same change, and stays behind `AUTO_PUBLISH_RATIFIED` (fail-closed OFF). The
flag flips only after 1–3 are verified live on a preview.

**This is a trust-path change.** It goes through the mandatory independent review,
and the charter has been corrected in this change to stop contradicting the
ratified decision.

### Step 3 — master green  *(DONE 2026-07-26)*

**Master is green, and the gate no longer lies about why a row is red.** 1,821 tests
pass locally and `validate` reports zero FAIL rows.

- **`requirements-dev.txt`** — every dev dependency, each with the reason it is
  needed, and a note that these are deliberately *unpinned* unlike
  `worker/requirements.txt`, whose pins are bound into the golden exam's
  harness-manifest hash.
- **`tools/bootstrap_dev.sh`** — the one documented command: venv,
  install, `git fetch --unshallow`, then it runs the preflight to prove its own work.
- **`tools/env_preflight.py`** — names **MISSING-TOOL** and **UNPROVABLE-HERE** at the
  top of every `validate` run, so an incomplete environment is never mistaken for a
  regression. Verified against bare system python: it names exactly the three package
  rows the audit found (xdist, fastapi, uvicorn), plus the shallow-clone row with the
  statement that `test_arming_smoke_binding.py` failing closed there is **correct
  behaviour, not a defect** (R-036).

**Nothing was loosened, and this is the part that mattered.** The preflight always
exits 0, is wired bare with `|| true` rather than as a `run_check` or `run_advisory`,
and changes no verdict — a failing check still fails and `validate` still exits
non-zero. A test asserts that wiring, because making a real failure report as an
environment fault would be a gate relaxation and therefore founder-crucial.

R-058 and R-067 resolved. What remains for criterion 3 is not local: **PR #76 needs the
independent panel's APPROVE to reach master.**

### Step 4 — deploy and get it in front of friends  *(founder-directed 2026-07-26: "I want to go live, in the real world for friends to begin testing, asap")*

**This is now the top of the list, ahead of Steps 5–7, because Steps 6 and 7
cannot even start without a URL a stranger can open.** Nothing here waits on a
date, on the Anthropic cap, or on PR #76.

**4a. DONE by the founder, 2026-07-26T17:10Z — minutes after this section named it
as the blocker.** The Vercel project `onelive` exists
(`sss-projects-e4775771/onelive`, root directory `web`), it is connected to the
GitHub repo, and it **built and deployed green on the first attempt with zero
environment variables**, exactly as the zero-config path predicted: commit
`ebd9bd1`, status `Vercel — Deployment has completed / success`, state **Ready**.
Preview URL recorded in *Deployment* below.

> **Correcting the record, because this paragraph said the opposite an hour ago.**
> It read *"there is no Vercel project linked to this repo yet — verified
> 2026-07-26, no `vercel.json`, no `.vercel/`, no deployment ever recorded."* That
> was true when written and is now false. The underlying inference was also
> weaker than it sounded: **absence of `vercel.json` and `.vercel/` is not
> evidence of an unlinked project** — a Vercel↔GitHub integration lives in
> Vercel's dashboard, not in the repo, so a project could have existed all along
> and left no trace here. The claim happened to be right; the reasoning behind it
> was not sound. `false-confidence-gate`, and the honest reading is that a repo
> check cannot answer a dashboard question. What the *next* deploy question should
> be answered with: the commit status on the head SHA, which is authoritative and
> which is what confirmed this one.

**Why zero env vars is correct and not a shortcut:** the gate auto-opens on
`VERCEL_ENV=preview|development` (`web/middleware.ts` `_mode === "disabled"`
branch), `/ops` stays denied on that same branch regardless, and the feed reads
the **committed public** Supabase URL + publishable key (`web/lib/licensed.ts`) —
public by design, because RLS is the boundary, never the key's secrecy. A real
secret is never committed.

**4b. THE REMAINING BLOCKER — now CONFIRMED BY MEASUREMENT, not inferred.** The
deploy being green does **not** mean a friend can see it.
`.github/workflows/site_health.yml` fetched the live preview from a GitHub runner
on 2026-07-26 and got **HTTP 302 with a `Redirecting...` body** — Vercel Deployment
Protection bouncing to its SSO login. So this is measured fact: **a friend clicking
that link gets a Vercel login page, not the product.** Three ways out, in order of
how little they cost:

| Path | Who can see it | Founder cost | Verdict |
|---|---|---|---|
| **Preview + a Deployment-Protection bypass link** (Vercel → Project → Settings → Deployment Protection → *Protection Bypass* / Share) | anyone holding the link | one click per link | **Recommended.** Zero env vars, zero code, friends-only by link possession, `/ops` still denied, and the stealth gate stays unspent. |
| **Production with `NEXT_PUBLIC_AUTH_DISABLED=1`** | the entire internet | one env var | **Not recommended for a friends test.** It is a public launch wearing a test's clothes, and it is founder-crucial (`CLAUDE.md`: go-live). |
| **Production with the real stealth gate** (Clerk keys + `ONELIVE_ALLOWLIST`) | named people only | Clerk account + 3 vars | The right answer *before public launch*. Overkill to unblock three friends tonight. |

Production is deliberately not a soft option: with no auth provider and no
explicit disable it **fails closed and denies everyone** (`accessNotConfigured`).
That is the design working, not a bug to route around.

**4c. THE OTHER REMAINING BLOCKER — is there anything in the feed to look at?**
Open `/api/health` on the deployed URL. It is reachable even in the fail-closed
state and returns
`{ok, auth:{mode,disabledBy}, supabase:{configured,source,reachable,eventCount}, vercelEnv}`.
`auth.mode:"disabled"` + `supabase.reachable:true` + `eventCount>0` means the feed
will render. **This is the number that decides whether the friends test is worth
running tonight**, and it is one request.

**The agent can now make that request itself — this stopped being a founder
interrupt, and how it stopped is the part worth keeping (R-068, partly resolved).**
The sandbox's network policy denies both `onelive-git-*.vercel.app` and
`vqipjlvzfiwnandjumvx.supabase.co` with a 403 to CONNECT, verified against the live
preview. For a while that made every deployment question something only the founder
could answer. The fix was not to ask harder; it was
`.github/workflows/site_health.yml`. GitHub runners have open egress — the **same**
reason `import_licensed.yml` already runs there rather than in a session, stated in
its own header. An agent triggers the workflow and reads the verdict out of the job
log.

- **What that bought:** reachability and gating status, on demand, with nobody in
  the loop. It is what proved 4b above by measurement instead of inference.
- **What it did not:** `eventCount`, because the 302 redirect never reaches
  `/api/health`. **One founder action clears this AND 4b together** — disabling
  Deployment Protection for preview (~30 s) makes the endpoint readable *and* lets
  friends in. Alternatively a Vercel *Protection Bypass for Automation* secret added
  as the `VERCEL_AUTOMATION_BYPASS` repository secret (~90 s) unblocks the agent's
  read alone; the workflow already sends it when present and prints `ABSENT` as a
  stated value when not, never an empty string.
- **Step 5 was never affected.** Its Lighthouse + axe run executes on a GitHub
  runner too; it never depended on the sandbox reaching the site.

Both pipelines were red on 2026-07-26, so a low `eventCount` is the expected
case, not a surprise: the local-venue feed's scheduled runs are fixed in this
change but unproven until one completes green (R-054), and the
Ticketmaster/SeatGeek feed has **no schedule at all** and last ran by hand on
2026-07-24 (R-055, ask 1). If `eventCount` is low the fix is **one manual
`import_licensed` run**, not a code change.

### Step 5 — measure the experience (machine half)

A Lighthouse + axe run in CI against the preview URL, recording load time, INP and
CLS at p75 and a WCAG 2.2 AA pass. Today **nothing** measures the part users
actually touch (bar rows E1–E4, P2). The product bar for load is **2.0 s** — the
brief's number — with Core Web Vitals' 2.5 s as the floor of external
acceptability. The stricter number wins; that is a tightening, not a relaxation.

### Step 6 — the ten-second answer, timed on a real phone

**This is the step that has never been attempted, and it is the cheapest one on the
list.** Bar row P1 is the most important number in the product — a stranger
answering "what should I do tonight?" in under ten seconds — and it has never been
measured once, in any form, by anyone.

What it takes: three people who have not seen the site, one phone, a stopwatch, the
deployed URL. Start the clock when the page opens; stop it when they say what
they're doing tonight. Median ≤ 10 s. Write the three numbers into this file.

At least two of the three must not be the founder — the person who designed the
information architecture cannot measure how legible it is to someone who didn't.

Expect it to fail the first time. That is the point: it is the only step on this
list that can tell us the product works as *a product* rather than as a pipeline.

### Step 7 — score the feeling against the brief's own rubric

The brief carries its own 8-criterion rubric (PART C) and it has only ever been
applied to design *comps*, never to a shipped surface. Score the deployed feed:
ten-second answer · night-sidewalk legibility · trust-by-craft with zero badges ·
distinctiveness against the named competitors · **emotional fidelity to §3
(anticipation + calm certainty)** · accessibility evident · survivability ·
click-pull passing the white-hat reflection test.

≥ 4/5 on every criterion. Deltas logged, never silent. A criterion scoring below 4
is a v1 blocker, not a note for later — because the feeling is the spec
(`CLAUDE.md` prime directive 5).

---

## Asks — everything the founder needs to do, in one list

**Two items open.** Asks 5 and 1 both resolved 2026-07-26 and their work is merged
into this branch; ask 4 resolved earlier. **What is left is ask 6 (the Vercel
protection wall — the only thing between you and friends testing) and ask 2 (the
Anthropic cap, which nothing waits on).** Ask 7 folds into ask 6's recommended path.
Ask 0 (Actions refusing to execute) and ask 4 (the tagline) both resolved on
2026-07-26 and are kept below for the record.

**Every ask below carries the same six fields plus a recommendation**, enforced by
`tools/founder_ask_lint.py` (founder directive 2026-07-26: *"never ask me to do
something without the specific structure defined"*). Links come from
`docs/DEPLOY.md` §"The console links" — never reconstructed from memory.

### Ask 6 — get friends onto the site (~2 minutes, the only thing still blocking a real test)

**What:** create one Vercel "bypass" secret. It does two jobs at once: gives you a
link friends can open, and lets me read the site's health automatically from then on.

**Why this needs you:** it is generated inside your Vercel account. Verified I cannot
reach it — `vercel.com` is denied outright by this environment's network policy, and
even from a GitHub runner (where I confirmed the settings page returns HTTP 200)
creating a secret needs a Vercel API token, which is a credential only you can mint.
**What I automated instead:** everything downstream. `site_health.yml` already sends
the bypass header the moment the secret exists and prints `ABSENT` when it does not,
so nothing needs wiring after you paste it.

**Why not just switch protection off:** you tried, and **Vercel refused** — the
toggle reverts to required and Save stays greyed out, which means Vercel never
registered a pending change (most likely a team-level policy on "SS's projects"
overriding the project setting). **This route needs no toggle at all.**

**Options:**
1. **Protection Bypass secret** — no toggle, works today, covers friends *and* my
   verification with one value, revocable in one click.
2. **Shareable Link per deployment** — Vercel mints a link from a deployment's `⋯`
   menu. Also needs no toggle, but it dies on every push to the branch, so you would
   re-send it after each deploy.
3. **Chase the toggle at team level** — <https://vercel.com/teams/sss-projects-e4775771/settings>
   and look for a Deployment Protection policy overriding projects. **The bad option:
   it is a hunt with an uncertain outcome, and if a team policy exists it may exist
   for a reason.**

**Where:** <https://vercel.com/sss-projects-e4775771/onelive/settings/deployment-protection>
then <https://github.com/schubertsean-ui/onelive/settings/secrets/actions/new>

**Exactly what to enter:** the numbered walkthrough below — each step names what you
will see before it names what to do.

1. **[founder, ~20 s]** Open
   <https://vercel.com/sss-projects-e4775771/onelive/settings/deployment-protection>.
   **You will see** the page titled **Deployment Protection** with **Vercel
   Authentication** at the top (the toggle that would not save). **Scroll past it**,
   past **Password Protection** and **Trusted Sources**, to a section headed
   **Protection Bypass for Automation**.
2. **[founder, ~20 s]** Click **Add Secret**. **You will see** a generated value
   appear — a long random string. **Copy it.** Click **Save** if the page offers it.
   **If you see "Enable and Pay" or a $150/month upsell instead**, you are looking at
   *Advanced* Deployment Protection — the wrong section. Bypass for Automation is
   included; scroll further.
3. **[founder, ~40 s]** Open
   <https://github.com/schubertsean-ui/onelive/settings/secrets/actions/new>.
   **You will see** a form headed **New secret** with two fields, **Name** and
   **Secret**. Type the name **exactly** `VERCEL_AUTOMATION_BYPASS` (capitals and
   underscores as shown), paste the copied value into **Secret**, click **Add
   secret**. **You will then see** it listed under *Repository secrets* as
   `VERCEL_AUTOMATION_BYPASS` with an "Updated now" timestamp. **The value itself is
   never shown again — that is normal.** Do **not** paste it into chat; it belongs
   only in these two places.
4. **[founder, ~20 s]** To let a friend in, send them this, with `SECRET` replaced by
   the value you copied:
   `https://onelive-git-claude-onelive-v1-eval-90b9d1-sss-projects-e4775771.vercel.app/?x-vercel-protection-bypass=SECRET&x-vercel-set-bypass-cookie=true`
   **They will see** the OneLive feed directly. The `x-vercel-set-bypass-cookie=true`
   part is what makes every later click work too — without it only that first page
   loads. **If they see a Vercel login page instead**, the secret was mistyped in the
   URL; re-copy it.
5. **[agent, ~1 min, no action from you]** Tell me it is done. I re-run
   `site-health`, which reads `/api/health` and reports the event count. **You will
   see** me post the number, and whether the feed needs a top-up before you show
   anyone.

**What you will see:** when it has worked, `site-health` stops saying *"reachable but
GATED — A FRIEND CLICKING THIS LINK CANNOT SEE THE PRODUCT"* and instead prints
`protection_bypass_secret: present`, `http_status: 200` and an `event_count:` line.
**If it still says GATED after step 3**, the secret name is wrong — it must match
`VERCEL_AUTOMATION_BYPASS` character for character.

**Time:** ~2 minutes total, in one sitting.

**Unblocks:** friends testing tonight; the event-count answer with no further help
from you; and v1 done-criteria 6 and 7 (the ten-second-answer timing and the brief's
8-criterion score), neither of which can begin without a URL a stranger can open.

**If you decline:** no real test happens and criteria 6 and 7 stay unstarted. The
site stays deployed and correct — just unseeable by anyone but you.

**Implications and tradeoffs:** anyone holding that long URL can view the site. That
is the same exposure switching the toggle off would have given, except revocable in
one click and limited to people you send it to. `/ops` stays denied by the app's own
gate regardless (`web/middleware.ts`), and the real boundary is row-level security on
the database, never URL secrecy. It is **not** a public launch: production is a
separate deployment and still fails closed.

**Recommendation: option 1, now.** It is the only remaining blocker to a real test,
it sidesteps the toggle Vercel will not let you change, and it permanently removes
deployment questions from your plate.

### Ask 7 — SUPERSEDED 2026-07-26 by ask 6 (same secret, one action)

Ask 7 asked for a Protection Bypass secret purely so an agent could verify
deployments. Ask 6 now asks for **the same secret** and uses it for both jobs — the
friend-shareable link and the agent's read. Two asks for one value was exactly the
padding the founder's "as little manual work as possible" directive forbids. Do ask 6;
there is nothing separate to do here.

### Ask 5 — RESOLVED 2026-07-26 (founder: *"option a"*)

**Done, and it unblocked the merge.** The M3 alarm now blocks on an escape whose
`Gate-gap closed` column names no shipped mechanism, not on the all-time count.
**Nothing was softened:** the target is still **0, absolute**, the count still prints
every run and can never decrease, placeholder text and malformed rows fail closed,
and a closed escape does not excuse a later open one — 8 tests. `validate` went from
2 failing tests to **1,794 passed, 0 failed**. Record:
`docs/memory/decisions/2026-07-26_escape-alarm-semantics.md`. Original ask and the
full evidence pass, including the precedents that cut the other way, are preserved
below and in `docs/ASK_ANALYSIS_2026-07-26.md`.

<details><summary>Original ask 5 (kept for the record)</summary>


**What:** decide what an escaped defect should do to the gate. Recording the
project's first escape to the documented convention turned `validate` permanently
red, because an escape is permanent history.

**Where:** nothing to open — reply here, or in
<https://github.com/schubertsean-ui/onelive/pull/76>.

**Exactly what to enter:** one of these four words/phrases — **"option a"** (an
escape stops blocking once its `Gate-gap closed` column names a shipped fix),
**"option b"** (window the count), **"option c"** (keep permanent red), or
**"option d"** (you acknowledge each escape).

**Options:**
1. **option a** — an escape stops blocking once its `Gate-gap closed` column names
   a shipped fix. Count stays 1 and stays visible; a repeat after the fix alarms
   immediately.
2. **option b** — window the count (e.g. trailing 90 days). Gate goes green on its
   own eventually, but the ledger then lies by omission, which is the thing this
   repo exists not to do.
3. **option c** — keep permanent red. **Named as the bad option and here is why:**
   every future PR then merges on `--allow-skips` or your personal authority (the
   R-051 pattern made routine), and it creates a standing incentive to stop
   recording escapes at all.

*(A fourth exists — you acknowledge each escape by hand — but it puts you in the
loop on every future PR, the opposite of what you have asked for, so I am not
counting it among the three.)*

**Why this needs you:** it is a gate-threshold change, which `CLAUDE.md` lists as
founder-crucial — and this particular one would loosen the gate that is currently
blocking my own PR. An agent relaxing its own blocker is the exact conflict of
interest the rule exists to prevent, so I left it untouched even though I know the
fix. **What I did without you:** the full evidence pass, including finding that
your repo already implements option 1's semantics for the sibling alarm.

**Time:** ~1 minute to read the tradeoffs below, seconds to answer.

**Unblocks:** PR #76's merge AND its independent review — the review job runs the
test suite first, so this single decision is blocking both. Everything merged in
this session is waiting behind it.

**If you decline:** `validate` stays red forever, and every future PR merges on
`--allow-skips` or on your personal authority. That is the R-051 pattern made
routine — last time a gate blocked with no way to satisfy it, a whole session's
work merged with the mandatory review never having run. It also creates a standing
incentive to **not record escapes**, which is the one thing that would destroy the
measure.

**Recommendation: option (a).** Your repo already implements exactly these
semantics for the sibling alarm in the same file
(`tools/kaizen_trends.py::family_alarm`), and your independent reviewer approved
that design. The M3 target stays **0, absolute** — what changes is the blocking
condition, from "any escape ever" to "any escape whose gap is not closed." An
escape with no shipped fix still blocks forever, and a repeat after a claimed fix
alarms immediately. Full evidence, including the two precedents that cut the other
way: `docs/ASK_ANALYSIS_2026-07-26.md`.


**This is the harness working, not breaking**, and it needs a one-line decision.

> **Full recurrence history and costed consequences:
> `docs/ASK_ANALYSIS_2026-07-26.md`.** Written on founder request 2026-07-26. Two
> findings that were not in this section before, both from reading the code rather
> than the docs: (i) **this is the first M3 escape ever recorded**, so the alarm has
> never been argued down — but (ii) `tools/kaizen_trends.py::family_alarm` **already
> implements option (a)'s exact semantics** for the sibling repeat-class alarm, and
> those semantics were ratified by the independent evaluator at round 6 of an
> earlier PR. The M3 counter is the one meter in that file that did not get the
> treatment. The analysis also names the cost of doing nothing — permanent red
> creates a standing incentive **not to record escapes**, which is the one failure
> mode that would destroy the measure. My recommendation moved (a) → (d) → (a) as
> evidence arrived; that is flagged there as moderate rather than high confidence.

Recording the project's **first escaped defect** to the documented convention fires
**three independent mechanisms**, all of which were blind only because the required
`M3-ESCAPE` token was missing from my row:

1. `tools/kaizen_trends.py` → `m3_escapes: 1` → FINDINGS, which `tools/validate`
   turns RED outright and **deliberately refuses to let `--allow-skips` waive**,
   because *"an alarm is a due fix, not information."*
2. `tests/test_kaizen_trends.py::test_real_ledger_parses_and_has_zero_escapes` fails
   — its own comment reads *"if this fails on escapes, that is the alarm working."*
3. `tools/kpi_report.py --check` → **OFF-TARGET: All-time escaped defects (M3): 1,
   target 0, absolute (Deming zero-escaped-defects goal).**

Three independent detectors agreeing is the harness working well, and it means this
is a project-wide invariant rather than one tool's quirk. Here is the hard part:
**an escape is permanent history, and that KPI is explicitly "all-time" — so the
number can never return to 0.** As written, the gate is red forever and every future
PR is unmergeable.

The independent reviewer found this by catching the opposite failure: my escape row
originally omitted the required `M3-ESCAPE` token, so the meter printed CLEAN while
the table recorded an escape. That is the fail-open — *"a malicious insider can
record an escape in the M3 table while the mechanical trend gate still prints
CLEAN."* I fixed the record and deliberately did **not** touch the gate, because
loosening one is founder-crucial.

| Option | What happens | My view |
|---|---|---|
| **(a) An escape stops blocking once its `Gate-gap closed` column is filled** | Count stays 1 forever and stays visible. The alarm still fires instantly for any escape recorded *without* a shipped mechanism. This escape's gap **is** closed — `tests/test_scheduled_inputs_contract.py`. | **Recommended.** It keeps the record permanent and the alarm meaningful, and it is the only option that distinguishes "we escaped and fixed it" from "we escaped and shrugged". |
| (b) Window the count to the last N PR rows | Old escapes age out. | **Argued against on evidence:** the KPI is named *All-time* escaped defects, so windowing contradicts a ratified KPI as well as erasing the record. |
| (c) Keep permanent red as written | Every future PR merges under an acknowledged red gate. | This is the exact outcome the no-waiver rule exists to prevent, reached by another route. |

**Reply "(a)" and I implement it** (a small, reviewed change to `kaizen_trends.py`
plus its test), or name another option. Registered as R-064. **Until you answer,
`bash tools/validate` is red on this one check and PR #76 does not merge** — the
charter's merge rule is not being bent for it.

---

### ~~Ask 0 — GitHub Actions has stopped executing~~ — RESOLVED 2026-07-26T15:03Z

**Cleared without action from this session.** Run 30189681960 attempt 2 was assigned
a real runner at 15:03:32Z and trust-gate returned **success**. The refusal window
was roughly 03:50Z–15:03Z. **I cannot tell you which cause it was** — that answer
lives in the billing page, not in the API — so R-060 records it as
unexplained-but-recovered rather than attributing it. If it recurs, the diagnosis
below still applies verbatim.

<details><summary>The original diagnosis, for the record</summary>

Between 03:50Z and 04:25Z on 2026-07-26, Actions jobs in this repository stopped
running. They are accepted and fail in 1–4 seconds with **no steps, no runner and
no logs** — the signature of a job GitHub refused to schedule, not of a failing
check. Reproduced on PRs #73, #74, #75 and #76, across two different workflows,
and on an explicit re-run. Diagnosis and the full evidence table are in the
comment on PR #76; the register row is R-060.

1. Check <https://github.com/settings/billing> → **Actions** usage and
   **Spending limit**. On a private repository, exhausted minutes or a reached
   spending limit is by far the most common cause of this exact signature.
2. If that looks fine, check
   <https://github.com/schubertsean-ui/onelive/settings/actions> — an Actions
   permissions change produces an identical silent refusal.

Raising a spending limit is money, so it is yours. **The charter's merge rule was
not bent for this:** APPROVE plus every required check green still stood, and the
work waited.

</details>

*Context worth one sentence:* this is the third provider ceiling hit in 24 hours —
Anthropic (ask 2), the Gemini free tier for the second review seat (R-052), and now
Actions. Velocity is currently bounded by billing, not by engineering.

---

Three further items. Nothing else is blocked on you.

### Ask 1 — RESOLVED 2026-07-26 (founder: *"build the watchdog"*, then *"do 2"*)

**Both halves done.** (a) The dead-man alarm is `.github/workflows/watchdog.yml` +
`tools/watchdog_check.py` — GitHub's own API, zero signup, zero secrets, 21 tests,
alarm proven able to fire. (b) `import_licensed.yml` now runs on a **12-hourly
schedule** (02:23 / 14:23 UTC, offset from the venue feed so the two importers never
contend for the same DB window) and is in the watchdog's `WATCHED` table from the
same change. The healthchecks.io signup and the `LICENSED_IMPORT_PING_URL` secret
are **no longer needed at all**. R-055 resolved; verify on the next 02:23 or 14:23
run. Records: `docs/memory/decisions/2026-07-26_github-native-watchdog.md`.

<details><summary>Original ask 1 (kept for the record)</summary>


> **DONE 2026-07-26, founder: *"build the watchdog"*.** The signup and the secret are
> gone. `.github/workflows/watchdog.yml` + `tools/watchdog_check.py` (18 tests, alarm
> proven able to fire on stale / never-run / unscheduled) asks GitHub's own Actions
> API when each watched job last succeeded and fails the scheduled run if it has gone
> silent — which emails you. Zero account, zero secrets. `CLAUDE.md`'s Sentinel clause
> is amended so the requirement is **the alarm, not the vendor**; nothing was
> loosened, and a job watched by neither mechanism is still forbidden. Weakness stated
> rather than buried: it lives inside Actions, so it dies with Actions (R-060), and
> GitHub disables schedules in repos idle 60 days. Record:
> `docs/memory/decisions/2026-07-26_github-native-watchdog.md`.
>
> **What remains is one word, not two minutes.** Scheduling `import_licensed.yml`
> puts real writes to the production database on a timer, so it is a separate,
> deliberate act — but its blocker (the charter's dead-man requirement) is now gone.
> Reply **"schedule the licensed feed"** and v1 done-criterion 1 is complete. The
> fields below describe that remaining step.

**What:** give `import_licensed.yml` (Ticketmaster + SeatGeek) a schedule, so the
bulk of the feed refreshes itself instead of only when someone clicks it.

**Where:** nothing to open — reply here. The change is mine to make.

**Exactly what to enter:** the words **"schedule the licensed feed"**.

**Why this needs you:** the code change is a ~10-line `on.schedule` block I can
write in a minute. What needs you is the *decision*: it puts recurring writes to the
production database on a timer, which `CLAUDE.md` treats as an irreversible action
requiring a friction pass and an explicit go — not something an agent arms on its
own initiative. **What I already removed from your plate:** the dead-man alarm this
depended on, which used to be a third-party signup and a secret and is now built
(see the DONE note above).

**Options:**
1. **"schedule the licensed feed"** — I add a 12-hourly schedule, move
   `import_licensed.yml` from the watchdog's `EXPECTED_SOON` list to `WATCHED` in the
   same change, and v1 done-criterion 1 is complete. Your cost: three words.
2. **Leave it manual and launch anyway.** **Correcting myself: I said the venue
   feed "already refreshes twice daily so the site is not static." That was wrong.**
   Measured 2026-07-26T18:08Z against the live Actions API: `import_structured.yml`
   has **4 successful runs ever, every one a manual `workflow_dispatch`, none from a
   schedule**, and the most recent was 2026-07-25T19:22Z — **22.8 hours ago, past the
   14-hour staleness limit.** The reason is exact and it matters: **the D1 cron fix is
   on this unmerged branch, not on master.** `origin/master` still reads the bare
   `github.event.inputs.limit`, so every scheduled run still dies in its own
   fail-closed guard. So under this option BOTH feeds are manual, not one. Your cost:
   a click per refresh, on both.
3. **Schedule it, but write to a staging table first and promote after a manual
   look** — maximum caution. **Named as the bad option and here is why:** it adds a
   review step to a *deterministic, licensed, non-AI* feed that cannot hallucinate,
   which is exactly the human-click bottleneck you have already said does not scale.

**Time:** seconds for you; the change and its verification are mine.

**Unblocks:** v1 done-criterion 1 ("the feed refreshes itself"), and it retires
R-055.

**If you decline:** the ticketed feed — the bulk of the listings — only refreshes
when a human remembers. That is the difference between a site and a demo, but it is
survivable for a friends test because the venue feed is already live and watched.

**Recommendation: option 1.** The importer has already run 9 times against this same
database with no incident, it is deterministic and free, the alarm that was missing
now exists, and the blast radius is one table.

</details>

### Ask 2 — raise the Anthropic cap (it is a setting, not a date)

**What:** raise the Anthropic monthly spend cap so AI extraction can run again.
**It is a console setting, not a date** — the API's own words are *"you have
reached **your specified** API usage limits."*

**Where:** <https://console.anthropic.com/settings/limits>

**Exactly what to enter:** raise the **monthly spend limit** to whatever ceiling you
are comfortable with. Nothing else changes; the key is already valid.

**Options:**
1. **Raise the monthly cap now** — the long tail resumes as soon as the next
   scheduled extraction runs.
2. **Leave it and launch on the deterministic feeds** — Ticketmaster, SeatGeek,
   Eventbrite and venue calendars need no AI, cost nothing per event and cannot
   hallucinate. Nothing in the v1 plan waits on this.
3. **Raise it a small amount as a deliberate experiment** — enough to certify
   extraction against the golden exam and measure real cost per verified event
   before committing to a bigger ceiling. Slower than (1), more informative.

**Why this needs you:** it is a spend ceiling on your account. Money is
founder-crucial by charter, and agents never mint or raise credentials or limits.
Nothing about it is automatable, and nothing should be.

**What you will see:** the page is headed **Limits** (or **Usage limits**) with a
**Monthly spend limit** figure and an **Edit**/pencil control beside it. After saving
you will see the new figure and no warning banner. **If you instead see a "reached
your usage limit" banner with no editable field**, you are on the *Usage* page rather
than *Limits* — use the left-hand **Limits** link. Nothing in this repo changes; the
next scheduled extraction run simply stops failing.

**Time:** ~1 minute.

**Unblocks:** **v1 done-criterion 2** and **BAR C3** — the AI long-tail (small
venues, weak-signal pages) that the deterministic feeds cannot reach. It unblocks
*coverage*, not launch: no other criterion waits on it. It does **not** unblock anything else — no step in
this plan waits on it.

**If you decline:** v1 ships on the deterministic feeds alone (Ticketmaster,
SeatGeek, Eventbrite, venue calendars), which is a genuine "what's on tonight in
Austin" product with zero AI cost and zero hallucination risk. You lose the long
tail, not the launch.

**Recommendation:** launch on the feeds now and raise the cap whenever you want the
long tail. Nothing is waiting on this, so it is not urgent — but it is also one
minute, so do it whenever it suits you.


**Correction, 2026-07-26:** earlier versions of this file and the audit said
extraction was *"offline until 2026-08-01"*, as though that were a fact of nature.
It is not. The API's own words are *"You have reached **your specified** API usage
limits"* — a **cap configured in the Anthropic console**, whose billing period
happens to reset 2026-08-01. **You can lift it in about a minute and extraction
resumes immediately.** Treating the reset date as a plan input was wrong: it made a
setting you control look like a wait you had to endure, and it let the plan drift
toward a date instead of toward a decision.

Agents never touch billing (`CLAUDE.md`, Environment), so this is yours either way.
Options:

| Option | What happens | Cost |
|---|---|---|
| **(a) Raise the cap now** | The long tail resumes within minutes. Friends testing see small/independent venues, not just ticketed ones. | Real spend, at a rate the last few days consumed quickly. Needs a ceiling you are comfortable with, not an unlimited one. |
| (b) Launch on the deterministic feeds first, raise it later | The site ships on ticketed feeds + venue calendars — a real product either way. Nothing waits on it. | Launch-week coverage of small venues is visibly thinner. |

*Recommendation, given you want friends testing ASAP:* **do both, in that order.**
The deterministic feeds are what get a usable site in front of people today and
they cost nothing per event; raise the cap whenever you want the long tail in the
picture. Neither one blocks the other, and **nothing in the plan waits on a date.**

### Ask 3 — one word: does the auto-publish ratification stand?

**What:** confirm in one word that your 2026-07-25 auto-publish ratification still
stands, so a gate-passing long-tail event can reach users without a human click.

**Where:** nothing to open — reply here.

**Exactly what to enter:** **"stands"** or **"hold"**.

**Options:**
1. **"stands"** — Step 2 proceeds in the ratified order, flag still OFF, flip
   still gated on safeguard 1.
2. **"hold"** — nothing is wired; every long-tail event keeps needing a click.
   Legitimate if you would rather see the deterministic feed's quality first.
3. **"stands, but anchor-source only"** — a narrower yes: auto-publish only
   `confirmed` events from anchor sources, leaving single-source `unverified` rows
   on human review. Less reach than (1), and it re-opens the exact question you
   have already answered once, which is its cost.

**Why this needs you:** it touches a trust invariant, and `CLAUDE.md` prime
directive 1 makes those yours alone. I deliberately did not read your earlier
message quoting my own scope paragraph as consent — resolving an ambiguity about a
trust invariant by assuming agreement is exactly what the charter forbids. **What I
did without you:** verified the safeguards' real status, found the rollback path,
and measured the blast radius, so this is a one-word answer rather than an
investigation.

**What you will see:** nothing to look at — this is a reply, not a page. After you
answer, you will see me either start Step 2 (the source-reliability grading loop
first, then `worker/autopromote.py`) or stop and record "hold" with no code written.
Either way the `AUTO_PUBLISH_RATIFIED` flag stays **OFF**; you will see no change on
the site from this answer alone.

**Time:** seconds.

**Unblocks:** **v1 done-criterion 2** ("a passing long-tail candidate reaches users
without a human click") and **BAR C3 / P8** — Step 2, the largest remaining piece of
real engineering. It does
**not** flip anything on: `AUTO_PUBLISH_RATIFIED` stays fail-closed OFF either way,
and the flip is a separate, later, deliberate action gated on safeguard 1.

**If you decline:** every long-tail event keeps needing a click, which is the thing
you have now said three times does not scale. This is a legitimate choice, not a
failure — it is right if you would rather see the deterministic feed's quality with
your own eyes first.

**Implications and tradeoffs:** the risk you accept is a gate-passing but wrong
event reaching the feed before a human sees it. It is recoverable — `worker/promote.py`
carries `set_event_confidence` and `mark_event_disputed`, both audit-logged, so a bad
row is demoted or marked disputed rather than deleted (never deleting is a trust
invariant, not a gap). Blast radius is verified small: the scheduled import is
capped at 40 events per run, the licensed import has no schedule at all, and
extraction is capped off at the provider.

**Recommendation: "stands".** It costs nothing today, unblocks the largest remaining
work, and the moment of actual risk stays behind a separate switch you control.
Detail and the three-times recurrence history: `docs/ASK_ANALYSIS_2026-07-26.md`.


On 2026-07-25 you ratified auto-publish at earned confidence. `CLAUDE.md` still
said the opposite; this change corrects the charter to match your decision. Since
it touches the trust invariant, I want it confirmed rather than assumed. **Reply
"stands" and Step 2 proceeds; reply "hold" and it stops and nothing is wired.**

> **Full recurrence history and costed consequences:
> `docs/ASK_ANALYSIS_2026-07-26.md`.** Three things there that change how this ask
> should be read:
>
> 1. **You have given this instruction three times** — before 2026-07-25 (quoted
>    inside the ratification record itself: *"I told you to prep to enable AI to
>    post without human approval"*), at the 2026-07-25 ratification, and again now.
>    The honest reading is not that you are repeating yourself; it is that the
>    system failed to execute a decision three times.
> 2. **A correction to something I told you: a rollback path DOES partially
>    exist.** `worker/promote.py` carries `set_event_confidence` (any of the four
>    states, validated, audit-logged) and `mark_event_disputed`. I had searched for
>    un-promote/retract verbs, found none, and reported that recovery was
>    impossible. The recovery verb in this system is **demote**, not **remove** —
>    and the absence of delete is a trust invariant working, not a gap.
> 3. **"Stands" costs nothing today.** `AUTO_PUBLISH_RATIFIED` stays OFF either
>    way; what it unblocks is *building*, with the flip still gated on safeguard 1
>    (the source-reliability update loop, currently a no-op default). Blast radius
>    meanwhile is verified small: `LIMIT` is pinned to **40** per scheduled
>    structured run, the licensed import has no schedule at all, and extraction is
>    capped off at the provider.

</details>

### Ask 4 — RESOLVED 2026-07-26 (founder: *"Use the new description for the tagline. Remove the old."*)

**Done.** Brief §3 amended: no tagline on any product surface, the thrive framing is
canon, the old line struck from the brief, all three design comps (and their now-dead
CSS), the proposals README, and the reference prototype. The removal is mechanical,
not a note — the old string moved from the REQUIRED list to a new FORBIDDEN list in
`tests/test_design_proposals.py`, proven red against the pre-amendment comps before
being made green. Nothing further needed from you.

<details><summary>The original ask, for the record</summary>

On 2026-07-22 (FLOW round 6) you removed **"Less chaos. Real shows."** from product
surfaces and reframed it in your own words: *"This is about finding and engaging in
experiences, helping individuals and the culture thrive."* That delta was logged at
the time, but the **ratified brief still mandates the old tagline as verbatim copy**
(§3) — so canon currently contradicts your own instruction.

`docs/BAR.md` §0 now carries your thrive framing as the current intent. The brief
itself is a founder-ratified document, so an agent does not rewrite it. This is the
proposal instead:

> **Proposed brief v2.5 edit, §3:** replace *"The tagline is: 'Less chaos. Real
> shows.' — the design is that sentence made visual"* with the thrive framing, and
> add that copy decisions pull toward what a night can **give** rather than what the
> app removes.

*Why this was more than a tagline:* the old line is defensive (what we remove), the
new one is generative (what a night gives) — and under the 2026-07-26 directive that
framing is what every copy and design decision is measured against, so leaving the
two in conflict meant the bar pointed two ways.

</details>

---

## Deployment

**Preview URL (branch `claude/onelive-v1-evaluation-gjer35`, deployed
2026-07-26T17:10Z, commit `ebd9bd1`, state Ready):**
<https://onelive-git-claude-onelive-v1-eval-90b9d1-sss-projects-e4775771.vercel.app>

Vercel project `sss-projects-e4775771/onelive`, root directory `web`, zero
environment variables. Verified via the GitHub commit status on `ebd9bd1`
(`Vercel — Deployment has completed`, `state: success`), which is the
authoritative source; **not** verified by fetching the page, because this agent's
network policy denies the host (R-068).

**Live URL: still not recorded, and the distinction is the point.** Bar row H7 asks
for a URL that is *the product*, and the line above is not that yet, for three
concrete reasons: (1) it is a **branch alias** — it dies when this branch merges;
(2) it is **host-protected**, so a friend gets Vercel's login wall rather than the
feed (Step 4b); (3) **`eventCount` is unverified**, so it is not yet known to show
anything (Step 4c). H7 flips when a stable URL a stranger can open renders real
events. This line stays exactly this honest until then.
