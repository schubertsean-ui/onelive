# Class D claim queue — sources we must NOT fetch

Generated by `python tools/class_d_queue.py` from `sources/master_sources_catalog_120.json` plus any walls observed at fetch time. Do not hand-edit: re-run the tool.

**Class D** (ONE-LIVE-COVERAGE-LAW.md) is a closed door — login, paywall, or bot wall. Coverage Law's rule is absolute and this file is how we obey it: **do not fetch; open a claim/submit path instead.** Nothing here is scraped, retried, or worked around.

`found` is how we learned the door was shut:

- **declared** — the source catalog itself says so (credential, partnership, manual step, or an explicit no-automated-ingest rule). Known before any network contact.
- **observed** — the source answered one polite unauthenticated request with a wall (401/402/403/407/429, or a redirect to sign-in). We knocked once, recorded it, and stopped.

The suggested path is a task for a person. No tool in this repo sends these asks automatically.

## Class D — declared by the catalog (14)

| source | url | why class D | found | suggested claim path |
| --- | --- | --- | --- | --- |
| Ticketmaster Discovery API | <https://developer.ticketmaster.com/> | access_method 'api_key' — needs a credential, a partnership, or a human step; no public URL to read | declared | Register a developer application with Ticketmaster Discovery API and store the credential as a repo secret (founder-crucial: credential minting). Until then, ask for a public ICS/RSS export instead. |
| Eventbrite API | <https://www.eventbrite.com/platform/api> | access_method 'oauth_api' — needs a credential, a partnership, or a human step; no public URL to read | declared | Register a developer application with Eventbrite API and store the credential as a repo secret (founder-crucial: credential minting). Until then, ask for a public ICS/RSS export instead. |
| AXS | <https://www.axs.com/> | access_method 'partner_preferred' — needs a credential, a partnership, or a human step; no public URL to read | declared | Request a partner/data-sharing agreement with AXS; ask specifically whether they can expose a public ICS or a nightly CSV drop, which needs no partnership at all. |
| DICE | <https://dice.fm/> | access_method 'partner_preferred' — needs a credential, a partnership, or a human step; no public URL to read | declared | Request a partner/data-sharing agreement with DICE; ask specifically whether they can expose a public ICS or a nightly CSV drop, which needs no partnership at all. |
| See Tickets | <https://www.seetickets.us/> | access_method 'partner_preferred' — needs a credential, a partnership, or a human step; no public URL to read | declared | Request a partner/data-sharing agreement with See Tickets; ask specifically whether they can expose a public ICS or a nightly CSV drop, which needs no partnership at all. |
| Tixr | <https://www.tixr.com/> | access_method 'partner_preferred' — needs a credential, a partnership, or a human step; no public URL to read | declared | Request a partner/data-sharing agreement with Tixr; ask specifically whether they can expose a public ICS or a nightly CSV drop, which needs no partnership at all. |
| TicketWeb | <https://www.ticketweb.com/> | access_method 'partner_preferred' — needs a credential, a partnership, or a human step; no public URL to read | declared | Request a partner/data-sharing agreement with TicketWeb; ask specifically whether they can expose a public ICS or a nightly CSV drop, which needs no partnership at all. |
| StubHub | <https://www.stubhub.com/> | access_method 'partner_preferred' — needs a credential, a partnership, or a human step; no public URL to read | declared | Request a partner/data-sharing agreement with StubHub; ask specifically whether they can expose a public ICS or a nightly CSV drop, which needs no partnership at all. |
| YouTube Data API | <https://developers.google.com/youtube/v3> | access_method 'api_key_oauth' — needs a credential, a partnership, or a human step; no public URL to read | declared | Register a developer application with YouTube Data API and store the credential as a repo secret (founder-crucial: credential minting). Until then, ask for a public ICS/RSS export instead. |
| Spotify Web API | <https://developer.spotify.com/documentation/web-api> | access_method 'oauth_api' — needs a credential, a partnership, or a human step; no public URL to read | declared | Register a developer application with Spotify Web API and store the credential as a repo secret (founder-crucial: credential minting). Until then, ask for a public ICS/RSS export instead. |
| Google Calendar (Claimed OAuth) | <https://developers.google.com/calendar> | access_method 'oauth_connect' — needs a credential, a partnership, or a human step; no public URL to read | declared | Register a developer application with Google Calendar (Claimed OAuth) and store the credential as a repo secret (founder-crucial: credential minting). Until then, ask for a public ICS/RSS export instead. |
| Google Places API (Venue Identity) | <https://developers.google.com/maps/documentation/places> | access_method 'api_key' — needs a credential, a partnership, or a human step; no public URL to read | declared | Register a developer application with Google Places API (Venue Identity) and store the credential as a repo secret (founder-crucial: credential minting). Until then, ask for a public ICS/RSS export instead. |
| Bing Search (Benchmark Only) | <https://www.bing.com/> | explicitly_disallowed contains 'automated_ingest' — the source forbids automated ingest; claim/submit path only | declared | Do not fetch. Ask Bing Search (Benchmark Only) in writing for an explicit feed permission or a first-party export (ICS or CSV); if they decline, the source stays out of the catalog. |
| DuckDuckGo Search (Benchmark Only) | <https://duckduckgo.com/> | explicitly_disallowed contains 'automated_ingest' — the source forbids automated ingest; claim/submit path only | declared | Do not fetch. Ask DuckDuckGo Search (Benchmark Only) in writing for an explicit feed permission or a first-party export (ICS or CSV); if they decline, the source stays out of the catalog. |

## Class D — observed at fetch time (0)

_None recorded yet. A run that meets a wall appends its row here._

## Appendix — class E, first-party opt-in (5)

Not class D: these are invitations, not walls. They need the same thing from a human (someone must opt in or claim the listing), so they are listed here to keep the claim work in one document.

| source | url | why class E | found | suggested claim path |
| --- | --- | --- | --- | --- |
| Instagram Graph API | <https://developers.facebook.com/docs/instagram-api/> | allowed contains 'opt_in_links' — first-party claimed intake | declared | Contact Instagram Graph API for a first-party feed (ICS preferred, CSV acceptable, email opt-in as a fallback). |
| Facebook Graph API | <https://developers.facebook.com/docs/graph-api/> | allowed contains 'opt_in_links' — first-party claimed intake | declared | Contact Facebook Graph API for a first-party feed (ICS preferred, CSV acceptable, email opt-in as a fallback). |
| TikTok for Developers | <https://developers.tiktok.com/> | allowed contains 'opt_in_links' — first-party claimed intake | declared | Contact TikTok for Developers for a first-party feed (ICS preferred, CSV acceptable, email opt-in as a fallback). |
| Claimed Venue Upload (ICS/CSV) | _(none recorded)_ | access_method 'claimed_upload' — first-party claimed intake | declared | Contact Claimed Venue Upload (ICS/CSV) for a first-party feed (ICS preferred, CSV acceptable, email opt-in as a fallback). |
| Opt-in Email Forwarding (Venue Schedules) | _(none recorded)_ | access_method 'opt_in_email_forward' — first-party claimed intake | declared | Contact Opt-in Email Forwarding (Venue Schedules) for a first-party feed (ICS preferred, CSV acceptable, email opt-in as a fallback). |
