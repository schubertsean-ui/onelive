R-006 ratified at 1% (field-assertion unit, KAIZEN M7 one-way ratchet); extraction BLOCKED at every entry point until Step 6's golden-set exam ships and passes (R-013). Sensor canon: watcher records + pull/push/investigate + first-party = confirmed (verified channels or authorized accounts; disputes never collapse). First real run proved infra end-to-end; runs with zero useful work now raise so the dead-man pages.

Gotcha worth remembering: GitHub renders unset and set-but-empty repo variables identically — a workflow can never fail closed on that difference; delete the channel instead. And the empty-env fail-open class is at 4 appearances: the 5th requires the structural env-contract linter, not a patch.

---

**Codified by:** `ai/exam_thresholds.py` (the 1% ratchet, one-way) + `docs/KAIZEN.md` M7 + `docs/RECORD.md` R-006/R-013.
